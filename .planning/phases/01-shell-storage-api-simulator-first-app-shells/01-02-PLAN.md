---
phase: 01-shell-storage-api-simulator-first-app-shells
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ratimos/theme.h
  - src/ratimos/row_list.c
autonomous: true
requirements: [SHELL-03]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every RATIMOS_COLOR_* macro in theme.h uses the D-17 logo-sampled hex values (#000000 bg, #2a123f panel, #4e2277 panel_active, #e6010f accent, #f5f2f8 text, #a997ba text_muted) and no other screen/app file references a raw hex color literal"
    - "row_list.c's title and subtitle labels truncate with an ellipsis (LV_LABEL_LONG_MODE_DOT) instead of overflowing or wrapping when fixture-sourced text is longer than the row width"
  artifacts:
    - src/ratimos/theme.h (palette values updated)
    - src/ratimos/row_list.c (long-mode truncation added)
  key_links:
    - "theme.h's RATIMOS_COLOR_* macros -> every ratimos_panel_create()/ratimos_badge_create()/ratimos_row_create()/ratimos_theme_apply_screen() consumer (single edit point — verified no other file holds a raw hex color literal)"
    - "row_list.c's title_lbl/sub_lbl long-mode setting -> every app screen that calls ratimos_row_create() (cartas/musica/jogos/config), inherited automatically without per-caller changes"
  prohibitions:
    - statement: "Never derive the new palette or any visual asset from the 'colombiaOS' reference project's own colors/assets — every value must trace back to logo/RatimOS.png sampling (D-17), keeping RatimOS's identity distinct per the future VISUAL-01 intent."
      verification: flagged-unverified
---

<objective>
Repaint the RatimOS shell's color palette from the newly-adopted official logo (`logo/RatimOS.png`, D-17) and add ellipsis truncation to the shared row-list component so real fixture-sourced titles (arriving via the Storage API in this phase's other plans) never overflow or break row layout.

Purpose: D-17 pulled the whole-UI palette rework forward from Phase 9 into Phase 1 — this is a self-contained, single-edit-point change (theme.h) that every existing and new screen already consumes through macros, so it can land independently of the board/storage/splash work in plan 01-01. The row-list truncation closes a UI-SPEC "unresolved" gap that only becomes a real risk once real (non-placeholder) titles start flowing through row_list.c later in this phase.
Output: `theme.h` with the 6 new hex values, `row_list.c` with truncation-safe labels.
</objective>

## Artifacts This Phase Produces

**New files:** none — this plan only modifies existing files.

**Modified files:**
- `src/ratimos/theme.h` — `RATIMOS_COLOR_BG/PANEL/PANEL_ACTIVE/ACCENT/TEXT/TEXT_MUTED` macro values repainted to D-17's logo-sampled hex values
- `src/ratimos/row_list.c` — `title_lbl`/`sub_lbl` inside `ratimos_row_create()` gain `lv_obj_set_width(..., lv_pct(100))` + `lv_label_set_long_mode(..., LV_LABEL_LONG_MODE_DOT)`

No new functions/structs are introduced by this plan.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-shell-storage-api-simulator-first-app-shells/01-CONTEXT.md
@.planning/phases/01-shell-storage-api-simulator-first-app-shells/01-UI-SPEC.md
@src/ratimos/theme.h
@src/ratimos/row_list.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repaint palette from logo/RatimOS.png (D-17)</name>
  <files>src/ratimos/theme.h</files>
  <read_first>
    src/ratimos/theme.h (current placeholder palette, lines 6-16), .planning/phases/01-shell-storage-api-simulator-first-app-shells/01-CONTEXT.md D-17, .planning/phases/01-shell-storage-api-simulator-first-app-shells/01-UI-SPEC.md "Color" section (exact hex values + sampling methodology + contrast ratios)
  </read_first>
  <action>
    In `src/ratimos/theme.h`, replace the 6 `RATIMOS_COLOR_*` macro values per D-17/UI-SPEC's logo-sampled palette: `RATIMOS_COLOR_BG` from `0x14102b` to `0x000000`; `RATIMOS_COLOR_PANEL` from `0x2b2358` to `0x2a123f`; `RATIMOS_COLOR_PANEL_ACTIVE` from `0x4a3f8f` to `0x4e2277`; `RATIMOS_COLOR_ACCENT` from `0x6dd6c4` to `0xe6010f`; `RATIMOS_COLOR_TEXT` from `0xf2eeff` to `0xf5f2f8`; `RATIMOS_COLOR_TEXT_MUTED` from `0xa89fd1` to `0xa997ba`. Update the header's top doc-comment (currently describing "indigo profundo + acento teal") to instead state the palette is sampled directly from `logo/RatimOS.png` per D-17, replacing the prior Phase-0 placeholder. Do not touch `theme.c` — it only references these macros by name and needs no changes. Do not add any new color tokens (no "destructive" token — none is needed this phase per the Copywriting Contract).
  </action>
  <verify>
    <automated>pio run -e native_sim &amp;&amp; grep -c "0x14102b\|0x2b2358\|0x4a3f8f\|0x6dd6c4\|0xf2eeff\|0xa89fd1" src/ratimos/theme.h</automated>
  </verify>
  <done>theme.h's 6 color macros hold the new D-17 hex values; build stays green.</done>
  <acceptance_criteria>
    - `grep -c "0x14102b\|0x2b2358\|0x4a3f8f\|0x6dd6c4\|0xf2eeff\|0xa89fd1" src/ratimos/theme.h` outputs `0` (no old placeholder values remain).
    - `grep -c "0x000000\|0x2a123f\|0x4e2277\|0xe6010f\|0xf5f2f8\|0xa997ba" src/ratimos/theme.h` outputs `6` (all 6 new values present).
    - `grep -rn "lv_color_hex(0x" src/ratimos | grep -v "src/ratimos/theme.h"` returns zero lines (no other file holds a raw hex literal outside theme.h).
    - `pio run -e native_sim` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">D-17 is explicitly noted as low-reversibility-risk in CONTEXT.md ("expected to hold through to delivery"), but technically a single-file macro edit — trivially revertable if ever needed, so rated reversible here despite the decision's own intent to be final.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Add ellipsis truncation to shared row-list labels</name>
  <files>src/ratimos/row_list.c</files>
  <read_first>
    src/ratimos/row_list.c (full file, 45 lines — title_lbl/sub_lbl creation), .planning/phases/01-shell-storage-api-simulator-first-app-shells/01-UI-SPEC.md "UI Considerations" row "overflow / long-text" (unresolved item this task resolves)
  </read_first>
  <action>
    In `src/ratimos/row_list.c`'s `ratimos_row_create()`, after `lv_label_create(text_col)` creates `title_lbl` and before/after setting its text, add `lv_obj_set_width(title_lbl, lv_pct(100));` followed by `lv_label_set_long_mode(title_lbl, LV_LABEL_LONG_MODE_DOT);` (DOT mode requires an explicit width to activate truncation — content-sized labels never truncate). Apply the identical two calls to `sub_lbl` inside the `if (subtitle)` block. This is a planner assumption (DOT over WRAP, since `text_col`'s flex-column rows are height-driven by `LV_SIZE_CONTENT` and WRAP could grow row height unpredictably) — flagged in this plan's must_haves.assumptions inherited from plan 01-01; revisit in Phase 9 if this reads poorly once real fixture titles are visible.
  </action>
  <verify>
    <automated>pio run -e native_sim &amp;&amp; grep -c "LV_LABEL_LONG_MODE_DOT" src/ratimos/row_list.c</automated>
  </verify>
  <done>row_list.c's title and subtitle labels truncate with an ellipsis instead of overflowing; build stays green.</done>
  <acceptance_criteria>
    - `grep -c "LV_LABEL_LONG_MODE_DOT" src/ratimos/row_list.c` outputs `2` (title_lbl and sub_lbl both covered).
    - `pio run -e native_sim` exits 0.
    - Manual check: any row rendering a title/subtitle longer than the row's available width shows a trailing ellipsis, not clipped/overflowing text (verified once real fixture titles are wired in plan 01-03).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure local styling constants (theme.h) and a label-rendering flag (row_list.c) — no I/O, no external input, no new trust boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-04 | Tampering | src/ratimos/theme.h color values | low | accept | Static compile-time constants sourced from `logo/RatimOS.png` sampling (D-17); no runtime input, no security surface. |
| T-1-05 | Information Disclosure / brand integrity | palette derivation source | medium | mitigate | Every new hex value traces back to logo/RatimOS.png sampling per D-17/UI-SPEC's documented methodology, never copied from the "colombiaOS" reference project's own palette — keeps VISUAL-01's future "visually distinct" requirement honest from the very first palette commit. |

</threat_model>

<verification>
- `pio run -e native_sim` builds cleanly with the new palette and truncation change.
- `grep` checks confirm old palette values are fully gone and no raw hex literals exist outside theme.h.
- Manual visual check: home screen and all 5 app screens render in the new near-black/violet/red palette; long titles truncate with an ellipsis.
</verification>

<success_criteria>
theme.h holds exactly the 6 D-17-derived hex values with no stray old values or raw hex literals elsewhere; row_list.c's shared row component truncates long text safely for every caller.
</success_criteria>

<output>
Create `.planning/phases/01-shell-storage-api-simulator-first-app-shells/01-02-SUMMARY.md` when done
</output>
