---
phase: 01-shell-storage-api-simulator-first-app-shells
plan: 3
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/storage/photos.c
  - src/storage/tracks.c
  - src/storage/games.c
  - src/storage/settings.c
  - src/ratimos/splash.c
  - assets/mock/photos/foto1.txt
  - assets/mock/photos/foto2.txt
  - assets/mock/photos/foto3.txt
  - assets/mock/tracks/faixa1.txt
  - assets/mock/tracks/faixa2.txt
  - assets/mock/tracks/faixa3.txt
  - test/test_storage/test_storage.c
  - src/ratimos/apps/musica_app.c
  - src/ratimos/apps/album_app.c
  - src/ratimos/apps/jogos_app.c
  - src/ratimos/apps/config_app.c
autonomous: false
requirements: [SHELL-01]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Opening jogos/musica/album/config from home shows real Storage-API-backed content (3 games, 3 tracks, 3 photos with titles, real brightness/volume/firmware/storage-used settings) instead of any hardcoded placeholder"
    - "musica's playlist row subtitle reads '1 faixa' when exactly one track exists and 'N faixas' when N != 1, never a hardcoded '0 faixas'"
    - "album and jogos empty-state rows render the new Copywriting-Contract copy ('nenhuma foto ainda' / 'tire uma foto ou aguarde a sincronizacao', 'nenhum jogo disponivel' / 'verifique a instalacao do RatimOS') only when their real Storage API list count is 0"
    - "album_app.c's photo tile title label truncates with LV_LABEL_LONG_MODE_DOT instead of overflowing"
    - "All app hint strings are now the canonical 'toque para abrir' — album_app.c's former 'toque pra abrir' and musica_app.c's former 'toque na musica' are fixed; jogos_app.c and cartas_app.c already conformed"
    - "pio test -e native_sim (full suite) passes, covering all 5 storage domains' list/get functions, not just letters"
    - "The splash's loading bar now reflects all 5 real domain-index steps (mount, photos, tracks, letters, games, settings) via the extensible step table built in plan 01-01"
  artifacts:
    - src/storage/photos.c
    - src/storage/tracks.c
    - src/storage/games.c
    - src/storage/settings.c
    - assets/mock/photos/foto1.txt
    - assets/mock/photos/foto2.txt
    - assets/mock/photos/foto3.txt
    - assets/mock/tracks/faixa1.txt
    - assets/mock/tracks/faixa2.txt
    - assets/mock/tracks/faixa3.txt
  key_links:
    - "splash.c's s_steps[] array extended with 4 more entries (index_photos/index_tracks/index_games/index_settings) so SPLASH_STEP_COUNT grows to 6 and the boot progress bar auto-repaces via SPLASH_TOTAL_MS / SPLASH_STEP_COUNT with zero change to the timer callback itself"
    - "musica_app.c/album_app.c/jogos_app.c/config_app.c -> ratimos_storage_list_tracks()/list_photos()/list_games()/get_settings() -> each domain .c file's own static cache populated once during the splash"
  prohibitions:
    - statement: "Never include real personal photos/messages as fixture content anywhere under assets/mock/photos/ or assets/mock/tracks/ — generic/placeholder text only (D-14); this repository is public on GitHub."
      verification: flagged-unverified
    - statement: "Never add a hidden/undocumented shortcut, debug menu, or bypass while wiring these 4 apps to the Storage API — only the documented tap-to-open row/tile interactions and the existing 'voltar' back-to-home action may exist."
      verification: flagged-unverified
---

<objective>
Expand the Storage/Content API to its remaining 4 domains (photos, tracks, games, settings), wire the remaining 4 apps (musica/album/jogos/config) to read exclusively through it, and close out the phase with a full automated-suite run plus a manual phase-gate sign-off — completing the vertical slice plan 01-01 started for "cartas"/letters.

Purpose: plan 01-01 proved the board/storage/splash architecture end-to-end on the simplest domain; this plan repeats the now-proven pattern for the remaining 4 domains and apps, which is mechanical repetition rather than new architectural risk, then verifies the whole phase goal (ROADMAP Phase 1 success criteria 1-4) is genuinely met before handing off.
Output: 4 more storage domain files + their fixtures, 4 apps wired, full test suite green, phase-gate human sign-off.
</objective>

## Artifacts This Phase Produces

**New files:**
- `src/storage/photos.c`, `src/storage/tracks.c`, `src/storage/games.c`, `src/storage/settings.c` — the 4 remaining Storage API domain implementations
- `assets/mock/photos/foto1.txt`, `foto2.txt`, `foto3.txt` — photo fixtures (metadata-only, per Pitfall 3)
- `assets/mock/tracks/faixa1.txt`, `faixa2.txt`, `faixa3.txt` — track fixtures

**Functions/symbols introduced (implementations of contracts declared in plan 01-01's content_api.h):**
- `ratimos_storage_index_photos(void)` / `ratimos_storage_list_photos(ratimos_photo_t *, size_t)`
- `ratimos_storage_index_tracks(void)` / `ratimos_storage_list_tracks(ratimos_track_t *, size_t)`
- `ratimos_storage_index_games(void)` / `ratimos_storage_list_games(ratimos_game_t *, size_t)`
- `ratimos_storage_index_settings(void)` / `ratimos_storage_get_settings(void)`

**Modified files:**
- `src/ratimos/splash.c` — `s_steps[]` array extended from 2 to 6 entries
- `test/test_storage/test_storage.c` — 5 more test cases appended
- `src/ratimos/apps/musica_app.c`, `album_app.c`, `jogos_app.c`, `config_app.c` — wired to the Storage API, hardcoded content removed

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-shell-storage-api-simulator-first-app-shells/01-CONTEXT.md
@.planning/phases/01-shell-storage-api-simulator-first-app-shells/01-RESEARCH.md
@.planning/phases/01-shell-storage-api-simulator-first-app-shells/01-PATTERNS.md
@.planning/phases/01-shell-storage-api-simulator-first-app-shells/01-UI-SPEC.md
@.planning/phases/01-shell-storage-api-simulator-first-app-shells/01-VALIDATION.md
@src/storage/content_api.h
@src/storage/letters.c
@src/ratimos/splash.c
@src/ratimos/apps/jogos_app.c
@src/ratimos/apps/musica_app.c
@src/ratimos/apps/album_app.c
@src/ratimos/apps/config_app.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand Storage API to photos/tracks/games/settings + fixtures + splash step wiring</name>
  <files>src/storage/photos.c, src/storage/tracks.c, src/storage/games.c, src/storage/settings.c, src/ratimos/splash.c, assets/mock/photos/foto1.txt, assets/mock/photos/foto2.txt, assets/mock/photos/foto3.txt, assets/mock/tracks/faixa1.txt, assets/mock/tracks/faixa2.txt, assets/mock/tracks/faixa3.txt, test/test_storage/test_storage.c</files>
  <read_first>
    src/storage/content_api.h (all 5 domains already declared by plan 01-01), src/storage/letters.c (the exact cache-on-index + hardcoded-manifest + safe-default-title pattern to repeat), src/ratimos/splash.c (the `s_steps[]` array and `SPLASH_STEP_COUNT`/`SPLASH_TOTAL_MS` mechanism built in plan 01-01, designed for this exact extension), test/test_storage/test_storage.c (existing letters tests to extend, not replace), .planning/phases/01-shell-storage-api-simulator-first-app-shells/01-RESEARCH.md Pitfall 3 (photos are metadata-only this phase, no JPEG decode) and Pitfall 4 (hardcoded manifest)
  </read_first>
  <behavior>
    - Test 1: `ratimos_storage_list_photos()` after mount+index returns exactly 3 items; item 0's title equals the first line of `assets/mock/photos/foto1.txt`.
    - Test 2: `ratimos_storage_list_tracks()` after mount+index returns exactly 3 items; item 0's title equals the first line of `assets/mock/tracks/faixa1.txt`.
    - Test 3: `ratimos_storage_list_games()` returns exactly 3 items with titles `"sudoku"`, `"car jam"`, `"paciencia"` in that order (compiled-in, not file-backed).
    - Test 4: `ratimos_storage_get_settings()` returns a `brightness_pct` and `volume_pct` both greater than 0, and a non-empty `firmware_version` string.
    - Test 5: a domain's list function returns the safe default title `"sem titulo"` for an entry whose fixture file exists but has a blank first line (or is unreadable) — matching letters.c's existing defensive behavior.
  </behavior>
  <action>
    Create `src/storage/photos.c` mirroring `letters.c`'s exact structure: hardcoded manifest `static const char * const s_photo_files[3]` listing `"assets/mock/photos/foto1.txt"`, `foto2.txt`, `foto3.txt`; `ratimos_storage_index_photos(void)` reads each file's first line into a static `ratimos_photo_t s_photos[3]` cache (id `"photo1"`/`"photo2"`/`"photo3"`), defaulting to `"sem titulo"` on failure/blank line exactly like letters.c; `ratimos_storage_list_photos(out, max_count)` copies from the cache only. Per Pitfall 3, this is metadata-only (title/id) — do NOT add any image-decode dependency or attempt to render real pixels; the existing `photo_tile_create()` placeholder-tile pattern in album_app.c (Task 2) stays a themed panel + label, just now populated with a real title.

    Create `src/storage/tracks.c` identically shaped for the tracks domain: manifest `assets/mock/tracks/faixa1.txt`/`faixa2.txt`/`faixa3.txt`, cache `ratimos_track_t s_tracks[3]`, ids `"track1"`/`"track2"`/`"track3"`, same safe-default-title behavior.

    Create `src/storage/games.c` — NOT file-backed (games ship compiled into firmware, per the UI-SPEC Copywriting Contract's note on the jogos empty-state row being unlikely-but-contractually-required). Define a file-scope `static const char * const s_game_titles[3] = { "sudoku", "car jam", "paciencia" };` array. `ratimos_storage_index_games(void)` can be a no-op (nothing to index — the list is already compiled-in) or trivially copy into a cache; `ratimos_storage_list_games(out, max_count)` copies up to `max_count` entries from `s_game_titles` into `out[i].title`, with `out[i].id` set to `"game1"`/`"game2"`/`"game3"` by index.

    Create `src/storage/settings.c` with real placeholder defaults (per D-09 — settings ARE placeholders this phase, but must be real, non-zero values now that config_app.c will read them in Task 2): a static `ratimos_settings_t s_settings`. `ratimos_storage_index_settings(void)` sets `brightness_pct = 80`, `volume_pct = 50`, `firmware_version = "0.1.0-sim"` (via `snprintf`), `storage_used_label = "12 MB / 512 MB (simulado)"` (via `snprintf`). `ratimos_storage_get_settings(void)` returns `s_settings` by value.

    Create the 6 new fixture files: `assets/mock/photos/foto1.txt` first line `Foto de teste 1` (+1 generic placeholder line), `foto2.txt` → `Foto de teste 2`, `foto3.txt` → `Foto de teste 3`; `assets/mock/tracks/faixa1.txt` first line `Faixa de teste 1`, `faixa2.txt` → `Faixa de teste 2`, `faixa3.txt` → `Faixa de teste 3`. Generic/placeholder only (D-14) — no `.bin` extension (Pitfall 2).

    Extend `src/ratimos/splash.c`'s `s_steps[]` array (built extensibly in plan 01-01 for exactly this purpose) by appending 4 entries: `{ ratimos_storage_index_photos }, { ratimos_storage_index_tracks }, { ratimos_storage_index_games }, { ratimos_storage_index_settings }` — after the existing mount/index_letters entries. Do not touch the timer callback or `SPLASH_TOTAL_MS` constant; `SPLASH_STEP_COUNT` recomputes automatically from the array size, and the per-step period (`SPLASH_TOTAL_MS / SPLASH_STEP_COUNT`) auto-repaces to keep the total near 2s now that there are 6 steps instead of 2.

    Extend `test/test_storage/test_storage.c` (append, do not remove the existing 2 letters tests) with 5 more `RUN_TEST` entries covering photos/tracks/games/settings/safe-default-title per the Behavior block above.
  </action>
  <verify>
    <automated>pio test -e native_sim -f test_storage &amp;&amp; pio run -e native_sim</automated>
  </verify>
  <done>All 5 storage domains are implemented and covered by passing Unity tests; the splash now stages all 6 real init steps.</done>
  <acceptance_criteria>
    - `pio test -e native_sim -f test_storage` exits 0 with all 7 tests (2 from plan 01-01 + 5 new) reported PASS.
    - `grep -c "ratimos_storage_index_photos\|ratimos_storage_index_tracks\|ratimos_storage_index_games\|ratimos_storage_index_settings" src/ratimos/splash.c` outputs at least `4`.
    - `git ls-files assets/mock/photos/ assets/mock/tracks/` lists all 6 new `.txt` fixtures.
    - `pio run -e native_sim` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire musica/album/jogos/config apps to the Storage API</name>
  <files>src/ratimos/apps/musica_app.c, src/ratimos/apps/album_app.c, src/ratimos/apps/jogos_app.c, src/ratimos/apps/config_app.c</files>
  <read_first>
    src/ratimos/apps/musica_app.c, src/ratimos/apps/album_app.c, src/ratimos/apps/jogos_app.c, src/ratimos/apps/config_app.c (current hardcoded content, all 4 files), src/ratimos/apps/cartas_app.c (the already-wired reference shape from plan 01-01), .planning/phases/01-shell-storage-api-simulator-first-app-shells/01-PATTERNS.md section "src/ratimos/apps/*.c" (target loop+conditional-empty-state shape, exact copy fixups required), .planning/phases/01-shell-storage-api-simulator-first-app-shells/01-UI-SPEC.md "Copywriting Contract" (canonical hint string + all empty-state copy) and "UI Considerations" row "zero-one-many" (singular/plural)
  </read_first>
  <action>
    Wire `src/ratimos/apps/jogos_app.c`: replace the 3 hardcoded `ratimos_row_create` calls with `ratimos_game_t games[4]; size_t n = ratimos_storage_list_games(games, 4);` then, if `n == 0`, render `ratimos_row_create(shell.content, "!", "nenhum jogo disponivel", "verifique a instalacao do RatimOS", NULL);`, else loop rendering `ratimos_row_create(shell.content, "J", games[i].title, "abrir", NULL);` for each. Add `#include "../../storage/content_api.h"`. Hint string `"toque para abrir"` is already canonical — no fixup needed.

    Wire `src/ratimos/apps/musica_app.c`: replace both hardcoded rows with `ratimos_track_t tracks[4]; size_t n = ratimos_storage_list_tracks(tracks, 4);`. If `n == 0`, render only the existing empty-state row (`"!"`, `"nenhuma musica ainda"`, `"adicione via SD ou sync"`) — do not also render the "playlist" summary row in this case. If `n > 0`, render a "playlist" summary row first with a subtitle built via `snprintf` branching singular/plural: exactly `"1 faixa"` when `n == 1`, else `"%zu faixas"` with `n` — never a hardcoded `"0 faixas"`. Follow it with one row per track: `ratimos_row_create(shell.content, "T", tracks[i].title, "tocar", NULL);`. Add `#include "../../storage/content_api.h"`. Fix the bottom-right hint string from `"toque na musica"` to the canonical `"toque para abrir"`.

    Wire `src/ratimos/apps/album_app.c`: modify `photo_tile_create()` to accept a `const char * title` parameter, setting the tile's label text to it (instead of the hardcoded `"sem foto"`), and add `lv_obj_set_width(lbl, lv_pct(100));` + `lv_label_set_long_mode(lbl, LV_LABEL_LONG_MODE_DOT);` on that label per the UI-SPEC's overflow/long-text resolution. In `ratimos_album_show()`, replace the hardcoded `for (int i = 0; i < 4; i++) photo_tile_create(shell.content);` loop with `ratimos_photo_t photos[4]; size_t n = ratimos_storage_list_photos(photos, 4);`; if `n == 0`, add `#include "../row_list.h"` and render `ratimos_row_create(shell.content, "!", "nenhuma foto ainda", "tire uma foto ou aguarde a sincronizacao", NULL);`, else loop calling `photo_tile_create(shell.content, photos[i].title);`. Add `#include "../../storage/content_api.h"`. Fix the bottom-right hint string from `"toque pra abrir"` to the canonical `"toque para abrir"`.

    Wire `src/ratimos/apps/config_app.c`: replace the 4 hardcoded rows (wifi/perfil/audio/render — themselves Phase-0 placeholders already marked TODO in the existing code) with real settings per D-09: `ratimos_settings_t s = ratimos_storage_get_settings();`, then build 4 rows via `snprintf` into local buffers for the numeric ones: `ratimos_row_create(shell.content, "B", "brilho", <brightness_pct>%%, NULL);`, `"V"`/`"volume"`/`<volume_pct>%%`, `"F"`/`"firmware"`/`s.firmware_version`, `"S"`/`"armazenamento"`/`s.storage_used_label`. Add `#include "../../storage/content_api.h"`.
  </action>
  <verify>
    <automated>pio run -e native_sim</automated>
  </verify>
  <done>All 5 apps (cartas from plan 01-01, plus these 4) now read exclusively through the Storage/Content API; zero hardcoded content rows remain in any app file.</done>
  <acceptance_criteria>
    - `grep -rln "ratimos_storage_list_\|ratimos_storage_get_settings" src/ratimos/apps/` lists all 5 app files (jogos, musica, album, cartas, config).
    - `grep -c "toque para abrir" src/ratimos/apps/album_app.c src/ratimos/apps/musica_app.c` shows both files now contain the canonical string (no more "toque pra abrir"/"toque na musica").
    - `pio run -e native_sim` exits 0.
    - Manual check: musica shows "1 faixa" when exactly 1 fixture track exists, or "3 faixas" with the current 3-item fixture set — never "0 faixas" while tracks exist.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final phase-gate manual UAT</name>
  <what-built>The complete RatimOS Phase 1 shell: a real boot splash (logo fade-in + genuine 6-step progress bar) leading into a home screen in the new D-17 palette, with all 5 sections (jogos/musica/album/cartas/config) reading exclusively through the Storage/Content API and rendering 3 real fixture-backed items each (except config, which shows real placeholder settings values).</what-built>
  <how-to-verify>
    Build and run the simulator: `pio run -e native_sim -t exec` (or run `.pio/build/native_sim/program` directly from the project root, so relative fixture paths under `assets/mock/` resolve). Then confirm, in order:
    1. Splash: the RatimOS logo fades in, a progress bar fills smoothly over roughly 2 seconds while genuinely stepping through 6 stages, and clicking/tapping anywhere during this time has zero effect (it never skips).
    2. Home: 5 tiles render in the new near-black/violet/red palette (not the old indigo/teal one).
    3. Jogos: shows 3 rows — sudoku, car jam, paciencia. Tap "voltar" — returns to home.
    4. Musica: shows a "playlist" row with a real singular/plural count (currently "3 faixas") plus 3 track rows. Tap "voltar" — returns to home.
    5. Album: shows 3 photo tiles with real titles ("Foto de teste 1/2/3"), truncated with an ellipsis if too long for the tile. Tap "voltar" — returns to home.
    6. Cartas: shows 3 letter rows ("Carta de teste 1/2/3"). Tap "voltar" — returns to home.
    7. Config: shows brilho/volume/firmware/armazenamento rows with real (non-placeholder-looking) values. Tap "voltar" — returns to home.
  </how-to-verify>
  <resume-signal>Reply "approved" once all 7 checks pass, or describe exactly which step failed and what you observed instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Fixture files (assets/mock/photos/*.txt, assets/mock/tracks/*.txt) → Storage API → UI | Same local, developer-authored, fixed-manifest read path established in plan 01-01, now repeated for 2 more domains. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-06 | Tampering / Information Disclosure | src/storage/photos.c, tracks.c fixture manifests | medium | mitigate | Same fixed-manifest discipline as T-1-01 (plan 01-01), applied to the 2 newly file-backed domains — no directory scanning, no dynamic path construction. |
| T-1-07 | Information Disclosure | assets/mock/photos/*.txt, assets/mock/tracks/*.txt content | high | mitigate | Same placeholder-only discipline as T-1-02 — generic titles only, never real personal photos/song references (D-14). |
| T-1-08 | Tampering | src/ratimos/apps/*.c wiring | medium | mitigate | No hidden skip/debug/backdoor input path may be added while wiring these 4 apps to the Storage API — only the documented tap-to-open row/tile interactions and the existing "voltar" back-to-home action exist, verified in Task 3's manual UAT. |

</threat_model>

<verification>
- `pio test -e native_sim` (full suite, all 5 domains) passes.
- `pio run -e native_sim` builds cleanly.
- Task 3's manual UAT confirms all 4 phase-level ROADMAP success criteria (splash before home, 5-section navigation, storage-API-only content, board/HAL structural parity) hold end-to-end.
</verification>

<success_criteria>
All 5 apps read exclusively through the Storage/Content API with real 3-item fixture-backed content (settings excepted, which is a single real record); the full automated test suite passes; a human has confirmed the complete boot-to-navigation flow works as specified.
</success_criteria>

<output>
Create `.planning/phases/01-shell-storage-api-simulator-first-app-shells/01-03-SUMMARY.md` when done
</output>
