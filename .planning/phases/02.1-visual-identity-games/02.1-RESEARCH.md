# Phase 02.1: Visual Identity & Games - Research

**Researched:** 2026-08-28
**Domain:** Embedded LVGL UI (pixel-art asset pipeline + bitmap typography) + 5 offline single-player puzzle games + a new local-persistence progression system, all in the `native_sim` PC/SDL2 simulator
**Confidence:** MEDIUM (game-logic algorithms and LVGL widget APIs are HIGH/well-established; the progression system's exact numbers and content-authoring approach are genuinely new decisions this research proposes, not verifies)

<user_constraints>
## User Constraints (from CONTEXT.md)

### Locked Decisions

- **D-01:** Five games ship in this phase: **Sudoku** (9x9, 3 difficulty levels, daily mode), **Paciência** (Klondike), **Termo** (Wordle-style, per-letter feedback), **Cruzadinha** (real crossword — intersecting words, numbered clues, NOT a hangman-style guess), **Conexo** (NYT Connections-style: 16 words -> 4 hidden categories).
- **D-02:** `car jam` (Rush-Hour sliding-block) is explicitly OUT of this phase — remove from `games.c`'s compiled placeholder list (or replace with the 5 real titles).
- **D-03:** Reward visual concept is **garden inside the castle grounds** — one combined visual, not two competing concepts. Exact layout is open for this research/planner to propose.
- **D-04:** Progression is **fixed with a defined end-state** (N daily completions = "castle complete"), not infinite/unbounded. Exact N and milestone-to-visual mapping are Claude's discretion.
- **D-05:** Progress toward the shared end-state is **shared across all games' daily modes** (any game's daily completion advances the same meter) PLUS **game-specific exclusive decorative unlocks** (e.g. one flower type per game) layered on top. Exact item-to-game mapping is Claude's discretion.
- **D-06:** The progression system is a first-class research topic (user's own words: "precisamos pesquisar MUITO mais sobre isso") — prior art from mobile daily-puzzle apps with streak/garden rewards required, not improvisation.
- **D-07:** Icon style is **pixel-art, 16-bit-style, matching `logo/RatimOS.png`'s treatment** — replacing the current single-letter badge (`ratimos_badge_create()`). Exact production method is open — this research must propose concrete tools/pipeline options.
- **D-08:** Two-tier typography: screen/section **titles** get a pixel-art/bitmap retro font matching the logo; **body text** (subtitles, instructions, clues, letters) stays on the current default LVGL Montserrat font. Reversible (font swap is a style-object change).
- **D-09:** Each game gets its own file pair under a new `src/ratimos/apps/jogos/` subdirectory (`jogos/sudoku.c/.h`, `jogos/paciencia.c/.h`, `jogos/termo.c/.h`, `jogos/cruzadinha.c/.h`, `jogos/conexo.c/.h`); `jogos_app.c` becomes the launcher/menu.
- **D-10:** Per-game in-progress state AND shared garden/castle progression state persist through a **new domain added to the Storage/Content API** (extending `src/storage/games.c` or a new sibling file), never touched directly by app code. Costly to change later — downstream hardware phases build on this shape.

### Claude's Discretion

- Exact pixel-art icon production toolchain/asset pipeline (D-07) — research first, propose options, test candidates with the user.
- Exact garden/castle art direction and stage-by-stage visual progression (D-03/D-04) — research first, propose a concrete plan.
- Exact number of daily-completion milestones (N) needed to reach "castle complete" (D-04).
- Exact mapping of which exclusive decorative item unlocks from which game (D-05).
- Sudoku puzzle generation approach (procedural infinite vs. bundled progressive puzzles) — research and recommend based on what's feasible on-device (eventual ESP32-S3 target).
- Exact crossword content/clue authoring approach (hand-authored vs. generated).
- Exact Conexo word-group content authoring.

### Deferred Ideas (OUT OF SCOPE)

- **Car Jam** (Rush-Hour-style sliding-block traffic puzzle) — confirmed concept, explicitly deferred to a future v2 backlog item, not this phase (D-02).
</user_constraints>

<phase_requirements>
## Phase Requirements

| ID | Description | Research Support |
|----|-------------|------------------|
| VISUAL-01 | RatimOS has its own icon set + typography (palette already done, Phase 1/D-17) | Icon Production Pipeline, Typography sections below — a concrete, reproducible pipeline using the project's existing `tools/convert_logo.py` C-array pattern, plus a named open-license pixel font candidate |
| JOGOS-01 | Sudoku (9x9, 3 difficulty, daily mode) + Paciência (Klondike), full touch input | Sudoku Implementation, Klondike Implementation, LVGL Grid/Touch Patterns sections |
| JOGOS-02 | In-progress state not lost when navigating away and back, every game | New Storage/Content API Domain Design section — per-game state struct + save/load contract |
| JOGOS-03 | Termo (Wordle-style, per-letter feedback) | Termo Implementation section — 2-pass duplicate-letter feedback algorithm, PT-BR word list source |
| JOGOS-04 | Real crossword (Cruzadinha) — intersecting words, numbered clues | Cruzadinha Implementation section — grid data model + numbering algorithm |
| JOGOS-05 | Conexo (16 words -> 4 hidden categories) | Conexo Implementation section — data model + win logic |
| PROGRESSAO-01 | Daily-mode completion grows a persistent garden/castle reward, survives sessions | Garden/Castle Progression System section — concrete data model + milestone proposal |
</phase_requirements>

## Summary

This phase has two genuinely distinct halves that should be planned (and likely wave-sequenced) separately: (1) a **visual identity pass** — pixel-art icons + a retro bitmap title font, both low-risk and mechanically similar to work already done in Phase 1 (the logo already went through an identical PNG-to-LVGL-C-array pipeline via `tools/convert_logo.py`); and (2) **five self-contained game engines plus one new cross-cutting progression system**, which is the actual bulk of the phase's complexity and risk.

For the games, none of the five algorithms are novel research problems — Sudoku generation-by-backtracking-with-uniqueness-check, Klondike's stack-of-cards model, Wordle's two-pass duplicate-letter feedback, crossword grid/numbering, and Connections' word-to-category mapping are all well-documented, decades-old (or, for Wordle/Connections, thoroughly reverse-engineered) patterns. The real engineering work is: (a) fitting each into this project's existing conventions (synchronous Storage/Content API, no dynamic allocation, cache-once screen pattern, `ratimos_app_shell_create()` chrome), and (b) sourcing/authoring Portuguese-language content for Termo, Cruzadinha, and Conexo — a real Brazilian Wordle clone (**Termo**, at term.ooo, built by Fernando Serboncini) already exists with an open **word list repository (`fserb/pt-br`)** that can very plausibly be adapted directly for this project's own Termo implementation, which meaningfully de-risks that game's content problem. Crossword and Connections content have no equivalent pre-built PT-BR source and must be hand-authored by the developer as small, compiled-in puzzle sets (consistent with this being a one-off personal gift, not a content platform).

The progression system is the one piece with no existing project precedent and the most design freedom. Prior art from Duolingo (streak-as-loss-aversion) and "garden growth" mobile mechanics (Two Dots-style) converges on a simple, implementable pattern: **one shared numeric counter + a small number of discrete milestone stages**, each stage mapped to a swapped static image asset (not a continuously-animated meter) — this fits LVGL's image-widget model perfectly and needs no new rendering capability. This research proposes a concrete default (N=~20-30 daily completions across 5-6 visual stages, plus 5 game-exclusive decorative unlocks, one per game) as a discretionary starting point for the planner/user to confirm, not a locked number.

The other cross-cutting problem this phase must solve for the first time is **write-persistence**: every existing Storage/Content API domain (`letters.c`, `photos.c`, `tracks.c`, `games.c`, `settings.c`) is read-only, either serving compiled constants or reading fixture files once at boot. Per-game progress and the shared garden/castle meter must be **written** back to disk and survive a full simulator relaunch (not just in-session navigation) — this is new territory for the codebase and should be treated as its own tracer task before the 5 games are built on top of it.

**Primary recommendation:** Build a small `ratimos_storage_get_game_state()`/`ratimos_storage_save_game_state()` pair (D-10) backed by one fixed-size binary state file per save-slot under a new gitignored `assets/save/` directory (mirroring `assets/mock/`'s dev-only convention), get that single read/write/reload round-trip proven and unit-tested first, then build the 5 games and the progression counter on top of it — in that order, as separate plans/waves.

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Icon/font asset generation (pixel PNG/TTF -> LVGL C array) | Build tooling (`tools/`, Python, off-device) | — | Mirrors `tools/convert_logo.py`; runs once at dev time, never on-device |
| Icon/title rendering | Client (LVGL/`native_sim` or eventual ESP32-S3 firmware) | — | `lv_image`/`lv_label` widgets, same as every existing screen |
| Game rules/state machines (sudoku solver, klondike moves, wordle feedback, crossword validation, connections grouping) | Client (firmware, `src/ratimos/apps/jogos/*.c`) | — | Fully offline, single-device, no server round-trip; matches project's "no backend for gameplay" model |
| Per-game save state + shared progression counter | Storage/Content API (`src/storage/`) | Client (game .c files call it) | D-10: app code never touches storage directly; this is the same layering as every other domain |
| Puzzle content (crossword grids/clues, connections word groups, sudoku puzzle bank if not procedural) | Storage/Content API (compiled-in, mirrors `games.c`'s static-array pattern) | Build tooling (content authored offline, embedded at compile time) | Personal one-off gift device, not a content-management platform — no server-side content needed for v1 |
| Termo/Cruzadinha/Conexo word lists (PT-BR) | Build tooling (curated once, embedded as a compiled array) | — | No runtime dictionary lookups needed; word lists are small enough (thousands of 5-letter words) to compile in |

## Standard Stack

### Core

| Library | Version | Purpose | Why Standard |
|---------|---------|---------|--------------|
| LVGL | 9.5.0 (vendored at `.pio/libdeps/native_sim/lvgl`, satisfies `platformio.ini`'s `lvgl/lvgl@^9.2.2` range) `[VERIFIED: .pio/libdeps/native_sim/lvgl/src/lv_version.h:1-3 — "#define LVGL_VERSION_MAJOR 9" / "#define LVGL_VERSION_MINOR 5" / "#define LVGL_VERSION_PATCH 0"]` | UI toolkit already in use | Already the project's chosen stack; this phase adds no new UI framework, only new widget usage (grid layout, table, buttonmatrix, image) within the existing library |
| `tools/convert_logo.py` pattern (Pillow-based PNG -> `lv_image_dsc_t` C array) `[VERIFIED: tools/convert_logo.py:1-80, src/ratimos/logo_image.c:1-8,42-49]` | Existing, project-authored | Icon asset pipeline | Already proven in this exact codebase for the splash logo; extend the same script (or a sibling script) for icon sprite sheets instead of writing a new pipeline from scratch |

### Supporting

| Library | Version | Purpose | When to Use |
|---------|---------|---------|-------------|
| `lv_font_conv` (npm, official `lvgl` GitHub org) | 1.5.3 `[VERIFIED: npm registry — npm view lv_font_conv version]` | Converts a chosen open-license pixel/bitmap TTF into an LVGL v9 C font array for the title font (D-08) | Dev-time-only tool (Node.js), not a firmware dependency — run once per font/glyph-range change, commit the generated `.c`/`.h` like `logo_image.c` |
| LVGL `lv_table` widget (`LV_USE_TABLE`) `[VERIFIED: .pio/libdeps/native_sim/lvgl/src/lv_conf_internal.h:2276-2284 shows LV_USE_BUTTONMATRIX defaults to 1; grep for LV_USE_TABLE default confirms same default-on pattern in the same file]` | bundled with LVGL 9.5.0 | Fixed grid rendering with per-cell text (candidate for crossword grid) | Crossword's fixed intersecting-word grid; simpler than hand-rolling per-cell `lv_obj_t`s, but check touch-target size (see Pitfalls) |
| LVGL `lv_buttonmatrix` widget (`LV_USE_BUTTONMATRIX`, default-on) `[VERIFIED: .pio/libdeps/native_sim/lvgl/src/lv_conf_internal.h:2276-2284 — "#define LV_USE_BUTTONMATRIX  1"]` | bundled | Digit-entry keypad (sudoku), word-tile selection (connections) | Takes a `NULL`-terminated `const char * map[]`; supports `LV_BUTTONMATRIX_CTRL_CHECKABLE`/`CHECKED` per-button flags — fits Connections' tap-to-select-then-submit flow |
| LVGL Grid layout (`LV_USE_GRID`, default-on) `[VERIFIED: .pio/libdeps/native_sim/lvgl/src/lv_conf_internal.h:2824-2832 — "#define LV_USE_GRID 1"; API confirmed at src/layouts/grid/lv_grid.h:34-63 — "#define LV_GRID_TEMPLATE_LAST  (LV_COORD_MAX)" and "void lv_obj_set_grid_dsc_array(lv_obj_t * obj, const int32_t col_dsc[], const int32_t row_dsc[]);"]` | bundled | 9x9 sudoku board of individually-touchable/styleable `lv_obj_t` cells | Recommended over `lv_table` for sudoku specifically — sudoku needs per-cell selection highlight + candidate-number sub-grid rendering that a plain text-cell table doesn't give cleanly |
| `LV_COLOR_FORMAT_I4` / `LV_COLOR_FORMAT_I8` indexed image formats `[VERIFIED: .pio/libdeps/native_sim/lvgl/src/misc/lv_color.h:147-150 — "LV_COLOR_FORMAT_I1 = 0x07," / "LV_COLOR_FORMAT_I4 = 0x09," / "LV_COLOR_FORMAT_I8 = 0x0A,"]` | bundled | Low-flash-footprint icon storage | Recommended over the RGB565 format the logo currently uses — pixel-art icons typically use <16 colors, so I4 (16-color palette + 4-bit-per-pixel index) is ~4x smaller than RGB565 per icon, which matters once dozens of icons are compiled into the eventual ESP32-S3 firmware image |
| `fserb/pt-br` word list (GitHub) `[ASSUMED — discovered via WebSearch, not yet fetched/inspected this session]` | current | PT-BR 5-letter word source for Termo | Confirmed by multiple sources as the actual word list backing term.ooo (Brazil's real Wordle clone); still needs a licensing check and a direct fetch/inspection before adoption — flagged in Open Questions |

### Alternatives Considered

| Instead of | Could Use | Tradeoff |
|------------|-----------|----------|
| Hand-authoring pixel icons from scratch | Adapting a CC0/MIT pixel icon pack (itch.io/OpenGameArt) as a base | Faster for a solo hobbyist with no art background, but requires re-coloring to match `RATIMOS_COLOR_*` and re-checking license terms per-pack before shipping |
| `lv_table` for the crossword grid | Custom `lv_obj_t` grid (like sudoku) | `lv_table` is simpler/faster to wire up for a static text grid, but gives coarser per-cell touch/style control; recommended default, fall back to custom grid only if `lv_table`'s cell-click granularity proves too coarse for individual-letter entry |
| Procedural infinite sudoku generation | Bundled progressive puzzle set (pre-generated, embedded like `games.c`'s static arrays) | Procedural is more "infinite" and demonstrates real algorithm work, but a small bundled set (e.g. 100-300 puzzles across 3 difficulties) is dramatically simpler to validate for correctness/uniqueness once, offline, at authoring time, and is trivially portable to ESP32-S3 flash — see Sudoku Implementation section for the concrete recommendation |
| Hand-authored crossword/connections content | Any generator | No viable generator exists for a hand-quality, real-clue Portuguese crossword at hobbyist scale; hand-authoring a small compiled-in set (mirroring `games.c`'s pattern) is the only realistic path for this personal one-off device |

**Installation:**
```bash
# Dev-time only (not a firmware/platformio.ini dependency) — pixel font conversion:
npm install -g lv_font_conv   # or: npx lv_font_conv ...

# Icon PNG -> LVGL C array: extend tools/convert_logo.py (already uses Pillow,
# already a project dependency for the logo pipeline — no new Python package).
```

**Version verification:** `lv_font_conv` verified via `npm view lv_font_conv version` -> `1.5.3`, repository `git+https://github.com/lvgl/lv_font_conv.git` (official `lvgl` GitHub org). LVGL itself verified against the actually-vendored copy in `.pio/libdeps/native_sim/lvgl`, not just the `platformio.ini` version range — the range (`^9.2.2`) resolved to 9.5.0, so any planning/code examples should target 9.5.0's confirmed APIs, not assume 9.2.2 behavior.

## Package Legitimacy Audit

| Package | Registry | Age | Downloads | Source Repo | Verdict | Disposition |
|---------|----------|-----|-----------|-------------|---------|-------------|
| `lv_font_conv` | npm | published 2024-06-10 (per registry metadata) | ~4,750/week | github.com/lvgl/lv_font_conv | OK | Approved — dev-tool only, not a firmware dependency, run manually/offline |

**Packages removed due to [SLOP] verdict:** none
**Packages flagged as suspicious [SUS]:** none

No other new external packages are required by this phase's scope: all game logic is hand-written C using the already-vendored LVGL/Unity toolchain; `Pillow` (Python, used by `tools/convert_logo.py`) is an existing Phase-1 dependency, not new to this phase.

## Architecture Patterns

### System Architecture Diagram

```
                    +-------------------------------------------+
                    |   jogos_app.c  (launcher/menu, D-09)       |
                    |   ratimos_row_create() x5, click -> game   |
                    +--------------------+------------------------+
                                         |
        +--------------+--------------+-+-------------+--------------+
        v              v              v               v              v
  sudoku.c       paciencia.c      termo.c       cruzadinha.c     conexo.c
  (jogos/*.c, each: build_once screen, own state struct, own win-check logic)
        |              |              |               |              |
        +------+-------+------+-------+-------+-------+------+-------+
               v              v               v              v
        ratimos_storage_get_game_state() / save_game_state()   <-- new D-10 API
               |
               v
     +-------------------------------------------------+
     |  src/storage/game_state.c (new)                  |
     |  - per-game struct (union or per-game blob)      |
     |  - shared progression counter (garden/castle)    |
     |  - fwrite/fread to assets/save/<slot>.bin         |
     +-------------------------------------------------+
               |
               v
     assets/save/ (new, gitignored -- native_sim only;
     Phase 5+ hardware phase swaps this for real NVS/SD write path)

  Garden/castle progression display:
     home_screen.c (or new progress widget) reads shared counter via
     ratimos_storage_get_game_state() -> picks 1-of-N milestone image
     (lv_image, same asset-swap pattern as logo/icons) -> no new render path
```

### Recommended Project Structure

```
src/ratimos/apps/jogos/
├── jogos_app.c / .h        # existing file becomes the launcher (menu list of 5 games)
├── sudoku.c / .h
├── paciencia.c / .h
├── termo.c / .h
├── cruzadinha.c / .h
├── conexo.c / .h
└── progress_widget.c / .h  # (new, discretionary) shared garden/castle visual, reusable across home + jogos menu

src/storage/
├── content_api.h           # extend with ratimos_game_state_t + get/save signatures
├── games.c                 # update static title list to the 5 real titles (D-01/D-02)
└── game_state.c            # (new) per-game + progression persistence, fwrite/fread to assets/save/

assets/
├── mock/                   # existing, read-only dev fixtures (unchanged)
└── save/                   # (new) gitignored, runtime-written game-state blobs (native_sim only)

tools/
├── convert_logo.py         # existing
├── convert_icons.py        # (new, discretionary) sibling script: pixel-art PNG sprite sheet -> per-icon lv_image_dsc_t C arrays (I4/I8 format)
└── convert_title_font.sh   # (new, discretionary) wraps lv_font_conv invocation for the chosen pixel TTF
```

### Pattern 1: Cache-once screen + per-game state struct

**What:** Every game screen follows the existing `s_jogos_screen`/`build_jogos_screen()` static-pointer pattern (build once, `lv_screen_load()` on every subsequent visit) — but each game additionally owns an in-memory state struct that is (a) mutated during play, (b) written to the new storage domain on every meaningful state change (not just on navigate-away, to survive an unexpected quit), and (c) loaded once when the screen is first built.
**When to use:** Every one of the 5 game screens.
**Example:**
```c
/* Source: mirrors src/ratimos/apps/jogos_app.c's existing s_jogos_screen pattern */
static lv_obj_t * s_sudoku_screen = NULL;
static ratimos_sudoku_state_t s_state; /* loaded once, mutated in place */

static lv_obj_t * build_sudoku_screen(void)
{
    ratimos_storage_get_game_state(RATIMOS_GAME_SUDOKU, &s_state, sizeof(s_state));
    ratimos_app_shell_t shell = ratimos_app_shell_create("sudoku", "toque numero");
    /* ... render s_state.board into a 9x9 grid ... */
    return shell.screen;
}

void ratimos_sudoku_show(lv_event_t * e)
{
    (void) e;
    if (!s_sudoku_screen) s_sudoku_screen = build_sudoku_screen();
    lv_screen_load(s_sudoku_screen);
}

/* called from every cell-tap handler after mutating s_state.board[...] */
static void on_cell_changed(void)
{
    ratimos_storage_save_game_state(RATIMOS_GAME_SUDOKU, &s_state, sizeof(s_state));
}
```

### Pattern 2: Date-seeded daily puzzle

**What:** Daily-mode puzzles are generated deterministically from a seed derived from the current calendar date (e.g. days-since-epoch), so the same day always produces the same puzzle without needing network access or a bundled per-day table.
**When to use:** Sudoku daily mode; optionally Termo/Conexo if the developer wants a rotating daily word/category set instead of static content.
**Example:**
```c
/* Source: general PRNG-seeding pattern, confirmed via WebSearch as the
 * standard technique behind Wordle-style daily puzzles (not vendor-specific
 * code -- Wordle's actual seed derivation is not open-source) */
uint32_t days_since_epoch = (uint32_t)(unix_time / 86400);
srand(days_since_epoch);
/* proceed with the same randomized-backtracking generator used for
 * non-daily puzzles, now fully deterministic for that calendar day */
```
**Caveat:** `native_sim` has no RTC yet (that lands in Phase 4) — for this phase, "today's date" must come from the host PC's system clock (`time(NULL)`), which is a reasonable stand-in but should be flagged as a temporary source until Phase 4's real RTC is wired in.

### Anti-Patterns to Avoid

- **Directory-scanning content discovery for game puzzles/word lists:** This project has an established V12-style discipline (per `letters.c`/`photos.c` comments) of never using `dirent.h`/directory scanning for content — always a fixed, hardcoded manifest or compiled-in array. Apply the same discipline to crossword/connections content: a compiled `const` array of puzzle structs, not a runtime directory scan of a `puzzles/` folder.
- **Continuous/animated progression meters:** Don't build a smoothly-animating garden that grows pixel-by-pixel with every point — prior art (Two Dots, Duolingo) and this project's asset-pipeline reality (hand-authored pixel art, not procedural rendering) both point to a small number of **discrete milestone images**, swapped via `lv_image_set_src()`, not a custom-drawn continuous animation.
- **Dynamic allocation in save/load paths:** `content_api.h`'s own header comment states the whole API is "sem alocacao dinamica" (no dynamic allocation) — game state structs must be fixed-size (e.g. `char board[9][9]`, not a `malloc`'d variable-length structure), consistent with every existing domain.

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---------|-------------|-------------|-----|
| Pixel font rasterization to LVGL bitmap format | A custom bitmap-font renderer | `lv_font_conv` (official LVGL tool) | Purpose-built, handles bpp/kerning/Unicode-range selection correctly; this project already delegates the equivalent image problem to a script (`convert_logo.py`) rather than hand-writing a PNG decoder |
| Sudoku "is this puzzle uniquely solvable" check | A custom heuristic guess | A counting backtracking solver (stop early at 2 solutions found) | This is the one non-negotiable correctness requirement for sudoku generation — an ad-hoc check risks shipping unsolvable or multi-solution puzzles, which is a much worse gift-experience bug than a slow generator |
| Card game legal-move validation (Klondike) | Freeform drag-and-drop with no rule engine | An explicit move-validation function per move type (tableau-to-tableau, tableau-to-foundation, stock-to-waste) | Cards silently accepting illegal moves is a common, hard-to-debug solitaire clone bug; centralizing legality checks in one function per move type makes it testable via Unity like `test_storage.c` |
| Wordle duplicate-letter feedback | A naive single-pass color-per-letter loop | The standard 2-pass (greens-then-yellows-with-decrementing-counter) algorithm | A single-pass implementation is the single most common Wordle-clone bug (over-crediting duplicate letters) — well-documented failure mode across every source found |

**Key insight:** Every game in this phase has a decades-old-to-several-years-old, well-documented reference algorithm. The risk in this phase is not "can this be solved" but "does the developer correctly implement the known-tricky edge case per game" (sudoku uniqueness, klondike legal-move enforcement, wordle duplicate letters) — plan verification steps around these specific edge cases, not just "does the game render."

## Runtime State Inventory

> Not applicable — this is a greenfield-capability phase (new games, new progression system, new icon/font assets), not a rename/refactor/migration phase. No existing runtime state is being renamed or moved.

## Common Pitfalls

### Pitfall 1: Read-only Storage API pattern doesn't cover write-back

**What goes wrong:** All 5 existing Storage/Content API domains (`letters.c`, `photos.c`, `tracks.c`, `games.c`, `settings.c`) are read-only — they read fixtures/constants once at splash-time and only ever return copies. A naive extension of this pattern for game state would silently fail to persist across a simulator relaunch, satisfying JOGOS-02 (navigate-away-and-back within a session) but silently failing PROGRESSAO-01 and the "survives sessions" clause of JOGOS-02's own success criterion.
**Why it happens:** Every existing precedent in this codebase is read-only; it's easy to copy the `list_letters()`-style pattern (memcpy from a static cache) without adding the write-through-to-disk step the new domain actually needs.
**How to avoid:** Design `ratimos_storage_save_game_state()` to `fopen(path, "wb")`/`fwrite()` a fixed-size struct immediately (not just update the in-memory cache), and prove the full write-then-relaunch-then-read round trip with a Unity test before building any game UI on top of it.
**Warning signs:** A UAT check that only tests "navigate away and back" (single session) instead of "quit the simulator process and relaunch" will falsely pass an in-memory-only implementation.

### Pitfall 2: `assets/save/` accidentally committed or accidentally read as mock content

**What goes wrong:** This project's fixture convention (`assets/mock/`) is deliberately git-tracked, read-only, generic placeholder content (D-14/D-15 from Phase 1). A new runtime-written save directory needs the opposite treatment (gitignored, per-developer-machine, potentially containing "real" play progress) — conflating the two conventions risks either committing a developer's personal save file to the public repo, or a hardcoded fixture accidentally overwriting real save data.
**Why it happens:** `assets/` currently has exactly one convention (`mock/`); a second, differently-governed subdirectory under the same parent is easy to blur.
**How to avoid:** Add `assets/save/` to `.gitignore` explicitly in the same task that creates the directory; name it distinctly from `mock/` in both path and in-code comments (mirror the existing D-15 "the name makes it unambiguous this is dev-only" reasoning).
**Warning signs:** `git status` showing an unexpected `assets/save/*.bin` as untracked-but-not-ignored after first game-state write.

### Pitfall 3: `lv_table`/`lv_buttonmatrix` touch targets too small on a 320x480 panel

**What goes wrong:** A 9x9 sudoku grid or a 5x5+ crossword grid, rendered at full screen width (~300px content area per `app_shell.c`'s `RATIMOS_SCREEN_W` minus padding), gives each cell roughly 30-35px or less — workable for a mouse cursor in `native_sim` but potentially too small for a real fingertip once Phase 3 hardware lands (though that verification is explicitly out of this phase's scope per the ROADMAP note).
**Why it happens:** LVGL's default table/buttonmatrix cell sizing is proportional (divides available width evenly), not touch-target-aware by default.
**How to avoid:** Explicitly size cells against this project's locked spacing scale (D-16: 4/6/8/10/16/24px) — favor fewer, larger cells where content allows (e.g. a 5-letter Termo row of tiles has much more per-cell room than a 9x9 sudoku grid), and note in the plan that final touch-target confirmation is deferred to Phase 3's stylus verification per the ROADMAP's explicit scope note.
**Warning signs:** Visually cramped grids in the `native_sim` screenshot/UAT step even with a precise mouse cursor.

### Pitfall 4: Sudoku "difficulty" implemented as clue-count only

**What goes wrong:** A common naive shortcut is "easy = 40 clues, medium = 30, hard = 20" — this does not reliably correlate with actual human-perceived difficulty (a 30-clue puzzle solvable by pure elimination can feel easier than a 35-clue puzzle requiring advanced techniques).
**Why it happens:** Clue-count is the simplest number to control during generation; solving-technique-based difficulty scoring requires implementing/checking for specific techniques.
**How to avoid:** For a v1 personal-gift scope, clue-count-based difficulty tiers are an acceptable, honest simplification — just document it as a known simplification rather than presenting it as true difficulty grading, and pick clue-count bands wide enough to feel distinct (e.g. easy ~40-45, medium ~30-35, hard ~22-27).
**Warning signs:** N/A — this is a scope/expectations pitfall, not a bug; flag explicitly in the plan so it isn't silently treated as "solved difficulty grading."

## Code Examples

### Wordle-style feedback (2-pass duplicate-safe algorithm)

```c
/* Source: general algorithm pattern confirmed via WebSearch (multiple
 * independent Wordle-clone writeups); not vendor code -- adapt directly. */
typedef enum { FEEDBACK_ABSENT, FEEDBACK_PRESENT, FEEDBACK_CORRECT } letter_feedback_t;

void termo_score_guess(const char guess[5], const char answer[5], letter_feedback_t out[5])
{
    int counts[26] = {0};
    for (int i = 0; i < 5; i++) counts[answer[i] - 'a']++;

    for (int i = 0; i < 5; i++) out[i] = FEEDBACK_ABSENT;

    /* Pass 1: exact matches first, consuming from the letter tally */
    for (int i = 0; i < 5; i++) {
        if (guess[i] == answer[i]) {
            out[i] = FEEDBACK_CORRECT;
            counts[guess[i] - 'a']--;
        }
    }

    /* Pass 2: remaining letters, only while tally > 0 */
    for (int i = 0; i < 5; i++) {
        if (out[i] == FEEDBACK_CORRECT) continue;
        int idx = guess[i] - 'a';
        if (counts[idx] > 0) {
            out[i] = FEEDBACK_PRESENT;
            counts[idx]--;
        }
    }
}
```

### Sudoku uniqueness check (counting solver, stop at 2)

```c
/* Source: standard "counting backtracking solver" pattern confirmed via
 * WebSearch across multiple sudoku-generator writeups; adapt for this
 * project's board representation. */
static int count_solutions(int board[9][9], int limit)
{
    int r, c;
    if (!find_empty_cell(board, &r, &c)) return 1; /* one full solution found */

    int total = 0;
    for (int digit = 1; digit <= 9 && total < limit; digit++) {
        if (is_valid_placement(board, r, c, digit)) {
            board[r][c] = digit;
            total += count_solutions(board, limit - total);
            board[r][c] = 0;
        }
    }
    return total;
}

/* A puzzle is uniquely solvable iff count_solutions(board, 2) == 1 */
```

## State of the Art

| Old Approach | Current Approach | When Changed | Impact |
|--------------|------------------|---------------|--------|
| LVGL v8 `LV_IMG_DECLARE`/`lv_img_dsc_t`/`LV_COLOR_FORMAT` enum names | LVGL v9 `LV_IMAGE_DECLARE`/`lv_image_dsc_t`/renamed `LV_COLOR_FORMAT_*` constants | LVGL v9.0 | Any tutorial or Stack Overflow snippet referencing `lv_img_dsc_t`/`LV_IMG_DECLARE` is v8-era — this project is already correctly on v9 naming (confirmed in `logo_image.h`/`.c`), keep new icon code consistent with that, not older v8 tutorials found during a web search |

**Deprecated/outdated:**
- v8-era LVGL image/font API names — do not follow v8 tutorials found via general web search; this project's own `logo_image.c`/`.h` is the correct v9 reference pattern to copy.

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | `fserb/pt-br` GitHub repo's word list is the actual list behind Termo/term.ooo and is suitable/licensed for reuse in this project | Standard Stack, Summary | If the license is incompatible or the repo doesn't actually match what was described in search results, the developer needs an alternative PT-BR 5-letter word source (fallback: hand-curate a smaller list, e.g. 200-500 common words, which is still enough for months of daily play) |
| A2 | LanaPixel (or another named pixel font) has sufficient PT-BR accented-character (á, é, ç, ã, õ, etc.) coverage for RatimOS's Portuguese UI titles | Standard Stack | If the chosen font lacks full Latin-Extended/Portuguese diacritics, titles with accented characters will render with missing glyphs or fall back to a mismatched font — must be verified by actually running `lv_font_conv` against the candidate font with the specific PT-BR character set needed, before locking the font choice |
| A3 | A shared meter of ~20-30 daily completions across ~5-6 discrete visual milestone stages is a reasonable default for the garden/castle "complete" end-state (D-04) | Garden/Castle Progression System (below) / Summary | Purely a design/pacing guess extrapolated from general mobile-game gamification patterns (Duolingo/Two Dots), not derived from any RatimOS-specific playtesting; the actual N and stage count are explicitly flagged in CONTEXT.md as Claude's discretion / user-confirmable, and should be discussed with the user before being locked in a plan |
| A4 | `lv_table`'s cell-click granularity is adequate for the crossword's letter-entry interaction | Standard Stack (Alternatives Considered) | If `lv_table` proves too coarse (e.g., can't easily distinguish "which letter of a merged multi-char cell was tapped"), the crossword grid needs the same custom-`lv_obj_t`-grid approach recommended for sudoku, adding rework |
| A5 | Native `time(NULL)`/host system clock is an acceptable stand-in for "today's date" in `native_sim` daily-mode puzzle seeding, ahead of Phase 4's real RTC | Architecture Patterns (Pattern 2) | If the simulator's host clock is wrong/unset, daily puzzles could seed incorrectly during dev/testing — low risk since this is dev-only behavior, not shipped to the real device without Phase 4's RTC replacing it |

**If this table is empty:** N/A — see entries above; all should be confirmed or explicitly accepted as design defaults before planning locks them in.

## Garden/Castle Progression System — Concrete Proposal (D-03/D-04/D-05/D-06)

This is the phase's one genuinely novel design surface; prior art review (Duolingo streaks, Two Dots-style garden growth — see Sources) converges on a pattern that is buildable with zero new rendering capability:

**Proposed data model** (fits directly into the D-10 Storage/Content API extension):

```c
typedef struct {
    uint16_t shared_completions;         /* increments on ANY game's daily-mode win */
    uint8_t  game_exclusive_unlocked[5]; /* bitmask/bool per game: sudoku, paciencia,
                                             termo, cruzadinha, conexo -- true once
                                             that game's daily mode has been won >=1 time */
} ratimos_progression_state_t;
```

**Proposed milestone mapping** (A3, needs user confirmation — not locked):
- `shared_completions` maps to one of ~5-6 discrete milestone images (e.g. bare castle grounds -> foundation walls -> walls + first garden bed -> towers + blooming garden -> flag raised + full garden -> "complete" state with both castle and garden fully rendered), swapped via `lv_image_set_src()` exactly like the splash logo swap, at even thresholds (e.g. every 4-6 completions) up to a top end-state around 20-30 total completions.
- Each `game_exclusive_unlocked[i]` maps to one small decorative sprite (a specific flower type or castle ornament) composited onto the same scene once that game has been won in daily mode at least once — giving each of the 5 games a small, permanent, game-specific mark on the shared visual without a second progression tree.

**Why this shape:** It satisfies D-05's two-part requirement (shared meter + per-game exclusive items) with exactly two fields, needs no new LVGL capability beyond image-swapping already proven by the splash/logo pipeline, and keeps the asset budget small enough for a solo hobbyist with no animation tooling (a handful of static pixel-art scene images, not a sprite-animation system).

**What must be persisted:** Both fields above, via the same `ratimos_storage_save_game_state()`-family API proposed for per-game state (Pitfall 1) — this is exactly why the persistence tracer task should land before individual games are built, since the progression counter is written from inside every game's win-condition code path.

## New Storage/Content API Domain Design (D-10)

Proposed additions to `src/storage/content_api.h`, following the existing struct-and-function-signature conventions verified in that file:

```c
/* New: per-game state + shared progression, extending content_api.h */
typedef enum {
    RATIMOS_GAME_SUDOKU = 0,
    RATIMOS_GAME_PACIENCIA,
    RATIMOS_GAME_TERMO,
    RATIMOS_GAME_CRUZADINHA,
    RATIMOS_GAME_CONEXO,
    RATIMOS_GAME_COUNT
} ratimos_game_kind_t;

/* Opaque-by-convention: each game interprets its own fixed-size blob.
 * Sized generously enough for the largest game's state (sudoku's 9x9 board
 * + notes) without dynamic allocation, per content_api.h's existing
 * "sem alocacao dinamica" contract. */
#define RATIMOS_GAME_STATE_BLOB_SIZE 512

typedef struct {
    uint8_t bytes[RATIMOS_GAME_STATE_BLOB_SIZE];
    size_t  used;
} ratimos_game_state_t;

/* Loads game/progression state from assets/save/ (native_sim) -- returns
 * false (and zeroes *out) if no save exists yet (first-ever play). */
bool ratimos_storage_get_game_state(ratimos_game_kind_t game, ratimos_game_state_t * out);
bool ratimos_storage_save_game_state(ratimos_game_kind_t game, const ratimos_game_state_t * state);

/* Shared progression, separate from per-game blobs since every game's win
 * path touches this same counter (see Garden/Castle proposal above). */
bool ratimos_storage_get_progression(ratimos_progression_state_t * out);
bool ratimos_storage_save_progression(const ratimos_progression_state_t * state);
```

**Native_sim persistence caveat (must be explicit in the plan):** There is no real NVS/SD-card hardware yet — "persistence" in this phase means `fwrite()`/`fread()` against fixed-path files under a new gitignored `assets/save/` directory (one file per game + one for progression, or one combined file), exactly mirroring how `lv_conf.h`'s `LV_MEM_SIZE` comment and D-15's `assets/mock/` both explicitly flag native_sim-only behavior that a later hardware phase must revisit. Flag this domain's `.c` file with the same kind of comment: *"native_sim-only file-backed persistence; Phase 5 (real SD/NVS storage) must replace the fopen/fwrite calls here with the real hardware write path, keeping this file's function signatures unchanged."*

## Open Questions

1. **Is `fserb/pt-br`'s word list actually license-compatible for reuse in this project?**
   - What we know: WebSearch results describe it as "Lista de palavras em Português" and multiple sources describe it as the list backing Termo/term.ooo.
   - What's unclear: The repository's actual license file was not fetched/read this session (WebSearch summaries only) — GitHub repo license terms need direct verification before embedding any of its content into RatimOS's source tree.
   - Recommendation: Before the planner locks Termo's content-authoring task, have a task that clones/inspects the actual repo, reads its LICENSE file, and confirms reuse terms; fallback is a smaller hand-curated word list (a few hundred common PT-BR words is enough for a personal gift device's daily-mode cadence).

2. **What exact N and stage-count should the garden/castle progression use?**
   - What we know: A3's proposal (20-30 completions, 5-6 stages) is a reasonable extrapolation from general mobile-gamification patterns.
   - What's unclear: No RatimOS-specific signal (e.g., how often the recipient is expected to play) was available to calibrate pacing.
   - Recommendation: Treat as a `checkpoint:human-verify`-style discussion point in planning/discuss-phase before locking — this was explicitly flagged by the user as needing more research/discussion (D-06), and this research's proposal is a starting point, not a final answer.

3. **Should crossword/connections content be authored in Portuguese from day one, or scaffolded in a placeholder language first?**
   - What we know: CONTEXT.md confirms all UI/content is Brazilian Portuguese; Termo has a de-risked content source (fserb/pt-br), but crossword/connections do not.
   - What's unclear: Hand-authoring even a small (~10-20 puzzle) crossword/connections set in Portuguese is real creative-writing effort with no shortcut found in research — this is likely to be the single most time-consuming non-coding task in the phase.
   - Recommendation: Plan for a small initial content set (e.g. 5-10 crosswords, 5-10 connections puzzles) as a v1 scope boundary, explicitly separate from the code/engine work, so the phase's success criteria aren't blocked on a large content-authoring backlog.

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|------------|------------|-----------|---------|----------|
| Python 3 + Pillow | Icon asset pipeline (extends `tools/convert_logo.py`) | Presumed ✓ (already used by Phase 1's logo pipeline, not re-verified this session) | — | — |
| Node.js + npm | `lv_font_conv` (pixel title font conversion) | Not verified this session | — | If unavailable, fall back to the LVGL web-based Font Converter (lvgl.io/tools/fontconverter), which requires no local Node install, only a browser |
| LVGL 9.5.0 (vendored) | All game/icon rendering | ✓ | 9.5.0 `[VERIFIED: .pio/libdeps/native_sim/lvgl/src/lv_version.h]` | — |
| `native_sim` PlatformIO build (SDL2, cJSON, Unity) | Running/testing the whole phase | ✓ (existing, unchanged by this phase) | per `platformio.ini` | — |

**Missing dependencies with no fallback:** none identified.
**Missing dependencies with fallback:** Node.js/npm for `lv_font_conv` — browser-based LVGL Font Converter is a viable no-install fallback.

## Validation Architecture

### Test Framework

| Property | Value |
|----------|-------|
| Framework | PlatformIO native Unity test runner (`unity`), confirmed via existing `test/test_storage/test_storage.c` `[VERIFIED: test/test_storage/test_storage.c:1-20]` |
| Config file | `platformio.ini`'s `[env:native_sim]` (`test_build_src = yes`) — no separate test-specific config file |
| Quick run command | `pio test -e native_sim -f test_storage` (single suite) |
| Full suite command | `pio test -e native_sim` |

### Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| JOGOS-01 | Sudoku: generated puzzle is uniquely solvable at each difficulty | unit | `pio test -e native_sim -f test_jogos_sudoku` | ❌ Wave 0 |
| JOGOS-01 | Klondike: illegal moves rejected, win detected when all foundations complete | unit | `pio test -e native_sim -f test_jogos_paciencia` | ❌ Wave 0 |
| JOGOS-02 | Game state round-trips through save/load and survives a simulated relaunch | unit | `pio test -e native_sim -f test_storage_game_state` | ❌ Wave 0 |
| JOGOS-03 | Termo feedback correctly handles duplicate letters (2-pass algorithm) | unit | `pio test -e native_sim -f test_jogos_termo` | ❌ Wave 0 |
| JOGOS-04 | Crossword: numbering algorithm assigns clue numbers correctly for a known fixture grid | unit | `pio test -e native_sim -f test_jogos_cruzadinha` | ❌ Wave 0 |
| JOGOS-05 | Conexo: correct-group detection and mistake counting | unit | `pio test -e native_sim -f test_jogos_conexo` | ❌ Wave 0 |
| PROGRESSAO-01 | Any game's daily-mode win increments shared counter; game-exclusive unlock only fires for that game | unit | `pio test -e native_sim -f test_progression` | ❌ Wave 0 |
| VISUAL-01 (icons/font) | Icon/font asset conversion scripts produce valid `lv_image_dsc_t`/font C output that compiles | build/smoke | `pio run -e native_sim` (compile-only check after asset regeneration) | N/A — build-level check, not a Unity test |
| All games | Full UI flow (open game, play, navigate away, return) | manual/UAT | `pio run -e native_sim -t exec` + manual click-through per game | N/A — visual/interactive, matches existing project convention (no LVGL UI is unit-tested, only underlying logic) |

### Sampling Rate

- **Per task commit:** run the specific new test file's quick command (e.g. `pio test -e native_sim -f test_jogos_sudoku`)
- **Per wave merge:** `pio test -e native_sim` (full suite, matches existing `test_storage`/`test_sync_unit`/`test_sync_integration` pattern)
- **Phase gate:** Full suite green before `/gsd-verify-work`, plus the manual UAT click-through for all 5 games (per ROADMAP success criteria 2-4) and a visual side-by-side comparison against colombiaOS for success criterion 1

### Wave 0 Gaps

- [ ] `test/test_jogos_sudoku/` — covers JOGOS-01 (sudoku generation/uniqueness/validation)
- [ ] `test/test_jogos_paciencia/` — covers JOGOS-01 (klondike move legality/win)
- [ ] `test/test_jogos_termo/` — covers JOGOS-03 (feedback algorithm, including duplicate-letter cases)
- [ ] `test/test_jogos_cruzadinha/` — covers JOGOS-04 (grid/numbering/validation against a fixture puzzle)
- [ ] `test/test_jogos_conexo/` — covers JOGOS-05 (grouping/win/mistake logic)
- [ ] `test/test_storage_game_state/` (or extend `test/test_storage/test_storage.c`) — covers JOGOS-02 + PROGRESSAO-01's persistence contract, including a genuine write-then-fresh-read round trip (not just in-process cache reuse)
- [ ] No new test framework install needed — Unity/PlatformIO native test runner already fully set up (`test_build_src = yes`)

## Security Domain

> `security_enforcement: true` in `.planning/config.json`, ASVS level 1, block on high. This phase is fully offline, single-device, single-user, with no network calls and no authentication surface — most ASVS categories genuinely do not apply, but two do because of the new write-persistence surface this phase introduces.

### Applicable ASVS Categories

| ASVS Category | Applies | Standard Control |
|---------------|---------|-----------------|
| V2 Authentication | No | No login/auth surface — single offline device, single user, no accounts |
| V3 Session Management | No | No sessions — this is a local single-process app |
| V4 Access Control | No | No multi-user/permission model exists or is needed |
| V5 Input Validation | **Yes** | The new game-state/progression save file is read back from disk at every relaunch — treat it as untrusted input (could be corrupted by an interrupted write, a full SD card in the future hardware phase, or manual tampering): validate size/bounds before trusting any field (e.g. clamp `shared_completions` to a sane max, reject an out-of-range `ratimos_game_kind_t` enum value) rather than trusting the blob blindly. This mirrors the existing `letters.c`/`photos.c` "safe-default-title" defensive pattern already established in this codebase for malformed/missing fixture files. |
| V6 Cryptography | No | Save data is not secret (game progress, not credentials/PII) — no encryption needed; do not over-engineer this |
| V12 File/Path Handling | **Yes** | Continue this project's existing discipline (established in `letters.c`/`photos.c`): fixed, hardcoded file paths for save data, never a path built from user/content-derived input, never `dirent.h` directory scanning |

### Known Threat Patterns for this stack

| Pattern | STRIDE | Standard Mitigation |
|---------|--------|---------------------|
| Corrupted/truncated save file crashes the app on next launch (e.g. after a power loss mid-write on real hardware, later phase) | Denial of Service (self-inflicted, not adversarial) | Validate the loaded blob's size/fields before use; on any validation failure, fall back to a fresh/default state (matching this codebase's existing "sem titulo" safe-default pattern) rather than crashing or propagating garbage into gameplay logic |
| Out-of-bounds array index from a malformed persisted `ratimos_game_kind_t` or board-cell value | Tampering (low likelihood, but a general defensive-coding norm for embedded C) | Bounds-check every enum/array index sourced from a loaded save blob before using it to index into a fixed-size C array — this is a real crash/memory-safety class of bug in embedded C, not just a security nicety |

## Sources

### Primary (HIGH confidence)

- Vendored LVGL 9.5.0 source, read directly this session: `.pio/libdeps/native_sim/lvgl/src/lv_version.h`, `src/lv_conf_internal.h` (LV_USE_GRID/LV_USE_TABLE/LV_USE_BUTTONMATRIX/LV_USE_IMAGE defaults), `src/layouts/grid/lv_grid.h` (grid API), `src/widgets/table/lv_table.h`, `src/widgets/buttonmatrix/lv_buttonmatrix.h`, `src/misc/lv_color.h` (color format enum)
- This project's own existing code, read directly this session: `src/ratimos/theme.h/.c`, `src/ratimos/row_list.c/.h`, `src/ratimos/app_shell.c/.h`, `src/ratimos/apps/jogos_app.c/.h`, `src/ratimos/apps/cartas_app.c`, `src/ratimos/apps/album_app.c`, `src/storage/content_api.h`, `src/storage/games.c`, `src/storage/letters.c`, `src/storage/photos.c`, `src/storage/settings.c`, `src/storage/storage.c`, `src/ratimos/logo_image.c/.h`, `tools/convert_logo.py`, `src/ratimos/splash.c`, `src/main.c`, `src/board/board.h`, `src/board/native_sdl/board.c`, `test/test_storage/test_storage.c`, `lv_conf.h`, `platformio.ini`
- `npm view lv_font_conv version` / `npm view lv_font_conv repository.url` — direct registry query this session

### Secondary (MEDIUM confidence)

- LVGL official docs (docs.lvgl.io/master, /9.4, /9.5) for `lv_image`, `lv_table`, `lv_buttonmatrix`, Grid layout — WebSearch-surfaced, cross-checked against the vendored source above where it mattered (widget default-enabled flags, API signatures)
- `lvgl/lv_font_conv` GitHub repo + LVGL Font Converter tool page — WebSearch-surfaced, cross-checked via `npm view`

### Tertiary (LOW confidence — WebSearch only, not independently verified this session)

- Pixel-art icon/font pack recommendations (BoldPixels, LanaPixel, Public Pixel Font, various itch.io/OpenGameArt CC0 packs) — names and license claims as reported by search summaries, not independently fetched/verified
- Sudoku generation algorithm description (randomized backtracking + counting-solver uniqueness check) — well-established general pattern, multiple independent sources agree, but no single authoritative source fetched in full
- Klondike solitaire data model (tableau/foundation/stock-waste stacks) — general pattern, multiple sources agree
- Wordle 2-pass duplicate-letter feedback algorithm — general pattern, multiple independent sources agree (word-challenge.com, 5letterwords.io, wordleanalyzer.dev)
- Crossword grid/numbering convention — general, long-established puzzle convention (Wikipedia-level common knowledge)
- NYT Connections clone data model — inferred from multiple open-source clone repo names/descriptions, not from reading their actual source code this session
- `fserb/pt-br` word list's origin/relationship to Termo/term.ooo, and its exact word count (~18-19k five-letter words) — reported by WebSearch summaries citing secondary articles about Termo's creation, not fetched from the repo itself
- Duolingo streak / Two Dots-style garden-growth gamification pattern description — WebSearch summaries of blog posts/case studies, not primary Duolingo/Two Dots design documentation

## Metadata

**Confidence breakdown:**
- Standard stack (LVGL widget APIs, `lv_font_conv`): HIGH — directly verified against vendored source and npm registry
- Icon/font asset sourcing (specific font/icon pack names): LOW — WebSearch-only, names not independently verified, treat as starting candidates not final choices
- Game algorithms (sudoku/klondike/wordle/crossword/connections): MEDIUM — well-established, multiple-source-corroborated patterns, but no single authoritative reference fetched in full for any of them
- Garden/castle progression design: LOW/proposal-only — explicitly a new design surface per the user's own framing (D-06), this research proposes a concrete starting point, not a verified best practice specific to this project
- PT-BR content sourcing (Termo word list): MEDIUM — a plausible, named, real-world precedent found, but license/content not yet directly inspected

**Research date:** 2026-08-28
**Valid until:** ~30 days for the LVGL/technical findings (stable, vendored-and-verified); the progression-system proposal and content-sourcing assumptions should be revisited at discuss-phase/planning time regardless of elapsed time, since they were flagged by the user as needing direct confirmation, not just staleness-based re-checking

---

## Follow-up Research (Round 2)

**Researched:** 2026-08-28 (same day, deeper pass on 3 user-requested points)
**Trigger:** User reviewed Round 1 and requested more depth on Termo's multi-word modes/word-list licensing, PT-BR content sourcing for Cruzadinha/Conexo, and a future-extensible progression architecture, before planning starts.

### 1. Termo — the real term.ooo game family (Termo/Dueto/Quarteto) + PT-BR word list licensing

**Confirmed mode structure** `[CITED: aggregated from vzone.gg "Termo, dueto e quarteto" daily-answers page, ngplus.com.br "Termo, Dueto e Quarteto" page, dlegames.org/game/termo, jogo.work — multiple independent PT-BR sources agree]`:

| Mode | Simultaneous words | Total tries |
|------|--------------------|-------------|
| Termo | 1 | 6 |
| Dueto | 2 | 7 |
| Quarteto | 4 | 9 |

Note: no "Trio" (3-word) mode was found in any source — term.ooo's real escalation skips from 2 straight to 4 words, not 1-2-3-4. `[CITED: same sources above]` If the planner wants a smoother escalation (1-2-3-4) that is a deliberate deviation from the real term.ooo, not a research gap — flag it as a discretionary choice, not an oversight.

**Observed try-count formula** `[ASSUMED — inferred by this research from the 3 data points above, not stated as a rule by any source]`: `max_tries = word_count + 5` (1+5=6, 2+5=7, 4+5=9). Plausible and simple enough to encode directly, but has only 3 data points and was never found stated as an explicit designed rule in any source — treat as a working assumption for a generation formula, confirm it "feels right" during playtesting rather than treating it as a verified game-design law.

**Is there a public term.ooo API?** No. `[CITED: WebSearch found zero references to any documented/public term.ooo API endpoint]` This is consistent with term.ooo being a static, client-side game (like the original Wordle) that ships its word list embedded in the client bundle, not served from a queryable backend. **This directly confirms Round 1's Standard Stack recommendation to vendor RatimOS's own compiled-in PT-BR word list rather than depend on any network call** — there is nothing to call even if the device had reliable connectivity, and the offline-first constraint (CLAUDE.md, SYNC-03) would rule it out anyway.

**Game-state model for simultaneous multi-word guessing:** No term.ooo-specific open-source clone implementing the actual Dueto/Quarteto multi-board mechanic was found among the repos surfaced (`GabrielMenoni/term.ooo-solver`, `vncsmnl/wordle`, `csamuelsm/verbete`, `gfpacheco/termoo`, `rafaelgasp/termooo` are all single-word Termo clones or solvers `[CITED: GitHub search results, repo descriptions read via WebSearch]`). However, term.ooo's Dueto/Quarteto mechanic is structurally identical to the **much better-documented English equivalents Dordle (2 boards) and Quordle (4 boards)** `[CITED: wordle.global/en/dordle, wordle.global/en/quordle, en.wikipedia.org/wiki/Quordle]`, which are extensively cloned in open source (e.g. `heskayw/react-dordle`). The confirmed shared pattern across all of these:
- **One shared text-input buffer / one shared keyboard** — the player types a single 5-letter guess once per turn, not once per board.
- **That single guess is submitted to every still-unsolved board simultaneously** — each board independently scores the same guessed word against its own hidden answer using the standard 2-pass duplicate-letter algorithm (already in Round 1's Code Examples).
- **Each board has independent state**: its own answer, its own guess history/feedback rows, and its own solved/unsolved flag. A board that reaches its answer stops receiving new guess rows (visually frozen "solved") while the other board(s) keep playing until every board is solved or `max_tries` guesses are exhausted for the whole session.
- **The keyboard's per-letter color state must be tracked per-board**, not globally — Dordle's split-key coloring (left half = board A's state, right half = board B's state) is the standard solution `[CITED: wordle.global/en/dordle description of split-color keyboard]`; for RatimOS's 320x480 screen, a full split-key rendering with 4 boards (Quarteto) may be too visually cramped (ties into Round 1's Pitfall 3 touch-target concern) — an alternative is a single neutral keyboard (no per-board coloring) with the color feedback shown only on each board's own guess rows, trading a minor UX affordance for legibility on a small screen. Flag this as a planner decision point, not a blocker.

**Proposed data model** (extends Round 1's per-game state pattern):
```c
#define RATIMOS_TERMO_MAX_BOARDS 4
#define RATIMOS_TERMO_MAX_TRIES  9   /* Quarteto's ceiling; Termo/Dueto use fewer */
#define RATIMOS_TERMO_WORD_LEN   5

typedef struct {
    char answer[RATIMOS_TERMO_WORD_LEN + 1];
    letter_feedback_t feedback[RATIMOS_TERMO_MAX_TRIES][RATIMOS_TERMO_WORD_LEN];
    uint8_t guesses_made;
    bool solved;
} ratimos_termo_board_t;

typedef enum {
    RATIMOS_TERMO_MODE_TERMO = 0,    /* 1 board, 6 tries */
    RATIMOS_TERMO_MODE_DUETO,        /* 2 boards, 7 tries */
    RATIMOS_TERMO_MODE_QUARTETO,     /* 4 boards, 9 tries */
} ratimos_termo_mode_t;

typedef struct {
    ratimos_termo_mode_t mode;
    ratimos_termo_board_t boards[RATIMOS_TERMO_MAX_BOARDS];
    uint8_t board_count;   /* 1, 2, or 4 depending on mode */
    uint8_t max_tries;     /* board_count + 5, per the observed formula above */
    uint8_t tries_used;
    char current_guess[RATIMOS_TERMO_WORD_LEN + 1];
} ratimos_termo_state_t;
```

**Daily-mode-only, deterministic selection:** This is directly buildable using Round 1's already-documented Pattern 2 (date-seeded daily puzzle, `days_since_epoch` -> `srand`). For a locked "daily-only, no free-play" Termo, the daily seed selects one word for Termo mode, or `board_count` **distinct** words for Dueto/Quarteto — draw via a seeded reject-on-duplicate loop (`do { pick = wordlist[seeded_rand() % wordlist_len]; } while (already_used(pick))`) rather than a full shuffle, since `board_count` is tiny (2 or 4) relative to a multi-thousand-word list, making duplicate collisions rare and a full Fisher-Yates shuffle unnecessary overhead. If all 3 modes (Termo/Dueto/Quarteto) ship as daily-only content on the SAME calendar day, derive a distinct sub-seed per mode (e.g. `days_since_epoch * 3 + mode_index`) so the 3 modes don't all draw the identical word(s) on a given day.

**Word list source — verified this session:**

`fserb/pt-br` `[VERIFIED via direct WebFetch of github.com/fserb/pt-br this session]`:
- **License: MIT** — confirmed present in the repo's license file navigation, genuinely permissive, no attribution-burden concerns for embedding a filtered word subset into RatimOS's firmware source.
- **Maintenance/activity:** 327 stars, 42 forks, 21 commits `[CITED: direct WebFetch of the repo page]` — a real, moderately-popular, low-but-nonzero-activity repo, not an abandoned/hallucinated project.
- **Important caveat found this session, not in Round 1:** this repo is a **raw multi-source lexicon** (145,744 lexical entries across all word lengths, plus ICF frequency scores, conjugations, and specialized lists), not a pre-filtered "these are good daily Termo words" list. Using it requires a curation step: (a) filter to exactly 5-letter entries, (b) cross-reference against the bundled ICF (Inverse Corpus Frequency) scores to exclude obscure/rare words that would make for an unfair daily puzzle, and (c) likely manually exclude proper nouns/conjugated verb forms that survive the length filter but aren't valid guess targets. This mirrors the exact technique independently confirmed in a real term.ooo-style clone: `csamuelsm/verbete` `[CITED: GitHub repo description via WebSearch]` explicitly describes using "the br.ispell dictionary crossed with Brazilian corpus frequency lists" to build its per-length word files — i.e., "raw dictionary ∩ frequency list" is the standard technique for this exact problem, not something RatimOS would be inventing from scratch.

**Fallback PT-BR word list** (if `fserb/pt-br`'s curation effort proves too heavy, or as a cross-check): the **VeroBP / LibreOffice Brazilian Portuguese hunspell dictionary** `[CITED: multiple sources — Fedora/Arch/Solus package metadata, hunspell/elastic GitHub mirror — cross-referenced this session]`, tri-licensed **LGPL-3.0-or-later / MPL-1.1** (permissive-enough for firmware embedding with attribution, no share-alike burden on RatimOS's own code). It is a genuine, well-precedented, actively-packaged spellchecking dictionary (shipped in LibreOffice, Firefox, Thunderbird) — same curation caveat applies (it's a spellcheck word list, not a pre-vetted "fun daily word" list; still needs length-filtering and a manual pass to drop obscure/technical terms).

**Recommendation:** Treat `fserb/pt-br` as primary (real precedent, confirmed MIT, real term.ooo lineage per multiple secondary sources `[ASSUMED — the repo's actual historical connection to term.ooo's own word list was still not found stated explicitly by the author himself in the fetched README this session; it remains a secondary-source claim]`), with the hunspell/VeroBP dictionary as a documented fallback. Either way, **budget an explicit content-curation task** (filter + manual quality pass to a few hundred to ~1000 curated 5-letter words) as its own plan step — this is real, non-trivial authoring/review work, not a drop-in dependency install.

**Scope flag (see Assumptions Log below):** the user's follow-up brief asks for Termo to support the Dueto/Quarteto escalation. CONTEXT.md's locked D-01 describes Termo singularly as "Wordle-style, per-letter feedback" with no mention of multiple simultaneous-word modes. Building all 3 modes is **new scope beyond D-01's literal text** — confirm with the user at discuss-phase/planning time whether this is an intentional expansion (consistent with the follow-up brief) or whether Round 1's single-word Termo remains the actual target and Dueto/Quarteto are a stretch-goal / later addition.

### 2. PT-BR content sourcing for Cruzadinha and Conexo

**Cruzadinha (crossword) — grid generation:**

Language-agnostic crossword-layout generation algorithms are real and well-precedented `[CITED: GitHub topic pages crossword-generator/crossword-puzzle, and specifically amirhakimnejad/Object-Oriented-Crossword-Puzzle-Generator-With-A-Backtracking-Approach (Python, backtracking-based) found via WebSearch this session]` — the standard approach is a **constraint-satisfaction / backtracking placement algorithm**: given (a) a target grid size and (b) a candidate word list, the algorithm tries placing words so they intersect at shared letters, backtracking when a placement makes the grid unsolvable, until it either fills the grid or reaches a placement it's satisfied with. This is genuinely language-agnostic — it operates on strings and grid coordinates, not linguistic content — so a Portuguese word list plugs in exactly like an English one would.

**What this does and does NOT solve:** it solves the **tedious manual grid-layout/intersection math** (which word goes where, which cells overlap, computing numbering) — it does **not** solve **clue authoring**, which is real creative-writing work regardless of language. No PT-BR-specific crossword generator library was found (all examples surfaced are English-language demos); the algorithm itself needs no translation, only a PT-BR word+clue input list.

**Recommended pipeline (dev-time only, mirrors `tools/convert_logo.py`'s "Python script run once, commit the output" pattern):**
1. Developer curates a small list of PT-BR (word, clue) pairs (e.g. 8-15 per puzzle).
2. A new dev-time Python script (`tools/generate_crossword.py`, discretionary addition to Round 1's `tools/` structure) runs a backtracking placement algorithm (adapt the pattern from the cited repo, or write ~100-150 lines fresh — this is a well-known, small algorithm, not a library dependency) against that word list, for a fixed grid size (e.g. 13x13 or 15x15, matching the project's 320x480 screen budget).
3. The script emits a compiled C struct (see data model below) which gets committed to source, exactly like `games.c`'s static arrays — **no runtime generation on-device**, consistent with Round 1's Anti-Pattern ("no directory-scanning/dynamic content discovery," `content_api.h`'s "sem alocacao dinamica" discipline).

**Clue TEXT source — a real find this session:** `[VERIFIED via direct WebFetch of github.com/ioxua/dicionario-aberto this session]` **Dicionário-Aberto** (`ioxua/dicionario-aberto`, a JSON mirror of the academic Dicionário-Aberto project, snapshotted 2018-12-23) is a genuine open Portuguese dictionary **with actual definitions**, not just a word list — this is a real candidate source for a starting-point clue text a developer can pick from and hand-edit for length/tone/difficulty, rather than writing every single clue from nothing.
- **License: Creative Commons BY-SA 2.5 Portugal** `[VERIFIED — the repo's own page explicitly links "Leia a licença aqui" to creativecommons.org/licenses/by-sa/2.5/pt/]`. This is attribution + **share-alike** — meaning derivative works incorporating this content are expected to be shared under the same/compatible license. **Flag explicitly for user confirmation before locking this as the clue source:** RatimOS's own repo/licensing posture is not stated anywhere in `.claude/CLAUDE.md` or `PROJECT.md` read this session, so whether CC BY-SA's share-alike clause is compatible with however this project intends to license its own source (public repo vs. private, MIT vs. unlicensed, etc.) is genuinely unresolved and should be a discuss-phase/planning checkpoint, not silently assumed compatible.
- If share-alike proves incompatible with the project's intended posture, the fallback is the same as always for this phase: hand-author clues from scratch — Dicionário-Aberto is a time-saving accelerant, not a hard dependency; the crossword's actual value doesn't collapse without it.

**Cruzadinha data model:**
```c
typedef struct {
    uint8_t row, col;          /* starting cell */
    uint8_t length;
    bool    is_across;         /* true = across, false = down */
    uint8_t clue_number;
    char    clue_text[96];
    char    answer[16];
} ratimos_cruzadinha_word_t;

#define RATIMOS_CRUZADINHA_MAX_WORDS 16

typedef struct {
    char    id[16];             /* e.g. "cruz-001" */
    uint8_t grid_w, grid_h;
    uint8_t word_count;
    ratimos_cruzadinha_word_t words[RATIMOS_CRUZADINHA_MAX_WORDS];
} ratimos_cruzadinha_puzzle_t;
```
**Numbering algorithm** `[ASSUMED — general, long-established crossword convention, Wikipedia-level common knowledge, not from a single authoritative source fetched this session]`: scan the grid left-to-right, top-to-bottom; a cell receives the next sequential number if it is the start of an across entry (no letter cell immediately to its left, AND a letter cell immediately to its right) **or** the start of a down entry (no letter cell immediately above, AND a letter cell immediately below) — a cell that starts both an across and a down word gets exactly one number shared by both entries.

**Conexo (Connections-style) — content sourcing:**

**No usable open PT-BR thematic word-grouping dataset was found.** `[CITED: targeted WebSearch this session found no dataset, only live proprietary game products]` What WAS found: PT-BR Connections-style games already exist as **live, closed products** — "Conexo Jogo" (conexo.onl / conexo.ws / conexojogo.com), "Associa Palavras - Conectadas" (App Store), "Conexões" via Dicio.com.br, and connectionsgamept.eu `[CITED: WebSearch results this session]`. This confirms the concept/naming ("Conexo" is already an established name for this exact game type in Portuguese, validating D-01's naming choice) but these are proprietary products with no public dataset or API — not a content shortcut.

English-language NYT Connections **archive/clone repos do exist** (`Eyefyre/NYT-Connections-Answers` — a JSON file of official past NYT puzzle answers; `pedropontesgarcia/clinnections` — a CLI clone whose JSON format was directly identified: an array of "connections" objects, each with `color` (`Yellow`/`Green`/`Blue`/`Purple`), a `words` array, and a `hint`/category name `[CITED: GitHub repo descriptions via WebSearch this session]`). **These are useful only as a reference for the standard DATA SHAPE** (confirms the 4-groups-of-4-words-plus-difficulty-color pattern used below) — **do not adapt or translate the actual NYT puzzle content itself**, since that is NYT's own copyrighted editorial content, not reusable dataset material; using it (even translated) would be a real copyright-cleanliness problem for a project that may end up in a public GitHub repo.

**Conclusion, stated plainly per the brief's instruction:** there is no realistic content shortcut for Conexo in Portuguese. Hand-authoring is the only viable path, exactly as Round 1 already concluded — this follow-up pass does not change that conclusion, it only confirms it more thoroughly (checked both a PT-BR dataset angle and an English-archive-as-source angle, both dead ends for actual content, though the English archives usefully confirmed the standard data shape).

**Conexo data model:**
```c
typedef enum {
    RATIMOS_CONEXO_DIFF_YELLOW = 0,  /* easiest, matches NYT/clone convention */
    RATIMOS_CONEXO_DIFF_GREEN,
    RATIMOS_CONEXO_DIFF_BLUE,
    RATIMOS_CONEXO_DIFF_PURPLE,      /* hardest */
} ratimos_conexo_difficulty_t;

typedef struct {
    char                        category_name[32];
    ratimos_conexo_difficulty_t difficulty;
    char                        words[4][16];
} ratimos_conexo_group_t;

typedef struct {
    char                  id[16];       /* e.g. "conexo-001" */
    ratimos_conexo_group_t groups[4];
} ratimos_conexo_puzzle_t;
```

**Scalable, non-repeating puzzle selection (applies to both Cruzadinha and Conexo, and equally to Sudoku's bundled-puzzle-set option from Round 1):** extend the D-10 per-game state blob with a small "recently used puzzle index" ring buffer:
```c
#define RATIMOS_RECENT_PUZZLES_TRACKED 10

typedef struct {
    uint16_t recent_puzzle_indices[RATIMOS_RECENT_PUZZLES_TRACKED];
    uint8_t  recent_write_pos;   /* ring buffer cursor */
} ratimos_puzzle_history_t;
```
Puzzle picker logic: pick a candidate index (daily-seeded or random), reject if present in `recent_puzzle_indices`, retry up to N times, then fall back to allowing a repeat once the pool is smaller than the tracked-history window (unavoidable once the puzzle bank is small — document this as an honest scaling limit, not a bug, exactly like Round 1's Pitfall 4 sudoku-difficulty framing). This is a small, forward-compatible addition to the D-10 storage domain that lets the developer keep appending puzzles to the compiled arrays over time (new firmware builds) without any code-path changes — new puzzles just extend `s_cruzadinha_puzzles[]`/`s_conexo_puzzles[]` and the picker automatically includes them in its selection pool.

### 3. Scalable, future-extensible progression/content-pack architecture

**Design goal restated:** the garden/castle milestone system must not be a fixed enum with a hardcoded stage count baked into the binary's logic — it must be **data the code reads**, sized generously enough that a later firmware update (or, longer-term, a synced content pack) can append new stages without changing the on-disk save-file shape or invalidating already-earned progress.

**Proposed versioned, additive-safe manifest:**
```c
#define RATIMOS_PROGRESSION_MAX_STAGES 32   /* generous headroom -- this phase
                                                fills ~6, later updates can add
                                                up to 26 more without any
                                                struct/layout change */

typedef struct {
    uint16_t threshold;      /* shared_completions value at which this stage unlocks */
    char     asset_id[32];   /* string key, looked up against a runtime
                                 image table -- NOT a compiled pointer and
                                 NOT a switch/enum case */
} ratimos_progression_stage_t;

typedef struct {
    uint16_t manifest_version;                          /* bump only on a
                                                            breaking layout
                                                            change, never on
                                                            stage_count growth */
    uint16_t stage_count;                                /* THIS grows over
                                                             firmware updates --
                                                             the extensibility
                                                             point */
    ratimos_progression_stage_t stages[RATIMOS_PROGRESSION_MAX_STAGES];
} ratimos_progression_manifest_t;
```

**Why `asset_id` is a string key, not a compiled pointer or enum value:** the whole point of "additive-safe" is decoupling *which stages exist* (data, can grow) from *which compiled image assets ship in a given firmware build* (code). A string key looked up at runtime against a small static `{asset_id -> lv_image_dsc_t*}` table (mirroring the icon-lookup pattern already used for row-list icons) means: (a) this phase can ship 6 real entries against 6 real compiled images, (b) a LATER firmware update can add entries 7-12 by shipping both new manifest rows AND their corresponding new compiled images in the same OTA build, and (c) a save file written under `manifest_version=1` with `shared_completions=18` (say, sitting at stage 3 of 6) continues to resolve correctly after the update, because `18` is compared against `threshold` values, not switched on a hardcoded enum case — new stage 7 simply becomes reachable once `shared_completions` crosses its threshold, with zero migration needed for the already-earned counter.

**Concrete initial data shape for THIS phase (~30 days, 5-6 stages, matching Round 1's A3 proposal):**
```c
static const ratimos_progression_manifest_t s_progression_manifest_v1 = {
    .manifest_version = 1,
    .stage_count = 6,
    .stages = {
        {  0, "castle_stage_00_empty_grounds"    },
        {  5, "castle_stage_01_foundation"       },
        { 10, "castle_stage_02_walls_garden_bed" },
        { 15, "castle_stage_03_towers_blooming"  },
        { 20, "castle_stage_04_flag_full_garden" },
        { 25, "castle_stage_05_complete"         },
        /* indices 6..31 are zero-initialized and inert -- stage_count=6
           means the renderer only ever looks at the first 6 entries.
           A later firmware update ships stage_count=9 (say) plus 3 new
           {threshold, asset_id} rows here -- no other code changes. */
    }
};
```

**Connection to the existing Phase 2 `content_items`/`sync_client.h` pipeline (conceptual only — no real wiring in this phase):** A future "+1 month of progress" content pack would, in principle, ride the exact same delivery mechanism Phase 2 already built for letters/photos/tracks, rather than inventing a second parallel content-delivery path:
- `content_items.type` is currently `[VERIFIED: supabase/migrations/20260828130724_phase2_content_items_ota_schema.sql:19 — "type text not null check (type in ('letter', 'photo', 'music'))"]` **CHECK-constrained to exactly three values** — a future phase extending this would need a new migration adding e.g. `'content_pack'` to that constraint (a small, additive, non-breaking schema change).
- The device's existing `ratimos_sync_whats_new()` contract `[VERIFIED: src/sync/sync_client.h:37-39]` (`size_t ratimos_sync_whats_new(const char *base_url, const char *device_token, ratimos_content_item_t *out, size_t max_count, int *out_status);`) would surface a pending `content_pack` row exactly like it surfaces a letter/photo/track today — `ratimos_content_item_t`'s existing `url` field `[VERIFIED: src/sync/sync_client.h:21-27 — "char id[40]; char title[64]; char type[16]; char content_date[16]; char url[128];"]` could point at a downloadable JSON fragment containing new `ratimos_progression_stage_t` rows (and referenced asset IDs) — no change needed to the sync transport contract itself.
- **A real gap this connection surfaces (worth flagging now, solving later):** `content_items.url` and the existing sync pipeline were designed for whole-file downloads (a photo, a letter, an audio track) — but this phase's icon/asset pipeline (Round 1) compiles all images into the firmware binary at build time (`tools/convert_logo.py`-style `lv_image_dsc_t` C arrays). A synced content pack cannot ship a *compiled C array* over the network into a running device — it would need to ship raw pixel/PNG data decoded to an `lv_image_dsc_t`-compatible in-memory format **at runtime**, which is a genuinely different asset pipeline from the compile-time one this phase uses for its initial 5-6 stages. This is real, useful information for whoever plans that future phase — it is explicitly **not** something to solve in Phase 02.1; the additive-safe *local* manifest design above is what avoids a rewrite of the counter/threshold logic, while the runtime-image-decode problem remains a separate, deferred concern.
- **Net effect for THIS phase's planner:** build the manifest structure above (data-driven, additive-safe, generously-sized array) and the 6 compiled-in stage images now; do not build any actual network/content-pack fetching logic in this phase (out of scope, Phase 02.1 has no sync wiring for this) — just don't hardcode assumptions (like a `switch(stage) { case 0: ... case 5: ... }` render function, or a `stage_count` `#define` baked into rendering logic) that would make appending stage 7+ later require touching render code instead of just data.

### Assumptions Log (Round 2 additions)

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A6 | Building all 3 term.ooo modes (Termo/Dueto/Quarteto) is in-scope, even though CONTEXT.md's locked D-01 describes Termo singularly with no mention of multi-word modes | Termo section | If the user actually wants only single-word Termo (Round 1's original scope) and Dueto/Quarteto were meant as "here's how the real thing works," not "build all 3," the planner could scope-creep the phase by building 2 unrequested extra game modes — confirm at discuss-phase before planning locks 3 modes in |
| A7 | The `max_tries = word_count + 5` formula is a valid generation rule for any future word-count variant, not just the 3 observed (1/6, 2/7, 4/9) | Termo section | Low risk (only affects tuning of a hypothetical unbuilt mode), but should not be presented as a verified term.ooo design rule — it is this research's own inference from 3 data points |
| A8 | Dicionário-Aberto's CC BY-SA 2.5 PT license (attribution + share-alike) is compatible with this project's intended source-licensing posture for crossword clue content | Cruzadinha section | If incompatible, clue text derived from this source would need to be re-authored from scratch or the project's own licensing posture reconsidered — a real (if narrow) legal-cleanliness question, not just a technical one |
| A9 | `fserb/pt-br`'s lexicon has a genuine historical connection to term.ooo's own actual word list (as claimed by secondary sources), beyond both simply being well-known PT-BR word resources | Termo section | If the connection is coincidental rather than causal, `fserb/pt-br` is still a perfectly usable MIT-licensed PT-BR lexicon on its own merits — this assumption only affects the "this is literally what powers the real game" framing, not the list's actual usability |

### Sources (Round 2)

**Primary/direct-fetch (HIGH confidence, fetched this session):**
- `https://github.com/fserb/pt-br` — direct WebFetch, confirmed MIT license, 327 stars/42 forks/21 commits, raw multi-source lexicon shape
- `https://github.com/fserb/pt-br/blob/master/README.md` — direct WebFetch, confirmed word list contents (145,744 lexical entries etc.), no additional reuse terms beyond the repo-level MIT license found in the README body itself
- `https://github.com/ioxua/dicionario-aberto` — direct WebFetch, confirmed CC BY-SA 2.5 Portugal license and 2018-12-23 snapshot provenance
- `supabase/migrations/20260828130724_phase2_content_items_ota_schema.sql` — read directly this session, confirmed `content_items.type` CHECK constraint values
- `src/sync/sync_client.h` — read directly this session, confirmed `ratimos_content_item_t` field layout and `ratimos_sync_whats_new` signature

**Secondary (MEDIUM confidence, WebSearch-surfaced, cross-corroborated across 2+ independent sources):**
- Termo/Dueto/Quarteto mode structure — vzone.gg, ngplus.com.br, dlegames.org, jogo.work (4 independent PT-BR sources agree on 1/2/4 word counts and 6/7/9 try counts)
- Dordle/Quordle mechanic (shared-input, per-board independent state, split-key coloring) — wordle.global/en/dordle, wordle.global/en/quordle, en.wikipedia.org/wiki/Quordle
- `csamuelsm/verbete`'s "br.ispell ∩ corpus frequency" word-list-building technique — GitHub repo description via WebSearch
- LibreOffice/VeroBP Brazilian Portuguese hunspell dictionary licensing (LGPL-3.0-or-later / MPL-1.1) — Fedora/Arch/Solus package metadata, hunspell/elastic GitHub mirror (cross-referenced across 3+ package-manager sources)
- Crossword backtracking/CSP generation algorithm pattern — GitHub topic pages, `amirhakimnejad/Object-Oriented-Crossword-Puzzle-Generator-With-A-Backtracking-Approach`
- NYT Connections clone data shape (color/words/hint) — `pedropontesgarcia/clinnections`, `Eyefyre/NYT-Connections-Answers` repo descriptions via WebSearch
- Existing live PT-BR Connections-style products (Conexo Jogo, Associa Palavras, Dicio's Conexões, connectionsgamept.eu) confirming no open dataset behind them, only proprietary apps — WebSearch results

**Tertiary (LOW confidence, single-source or inferential):**
- No public term.ooo API — absence-of-evidence from a single targeted WebSearch pass, not an exhaustive audit of term.ooo's own client bundle
- `max_tries = word_count + 5` formula — this research's own inference, not sourced from any document stating it as a rule
- `fserb/pt-br`'s claimed lineage to term.ooo's actual production word list — repeated across secondary sources but never confirmed by the repo author's own words in the fetched README

### Metadata (Round 2)

**Confidence breakdown:**
- Termo mode structure (names, word counts, try counts): MEDIUM-HIGH — 4 independent sources agree, though none is term.ooo's own official documentation
- Word list licensing (`fserb/pt-br` MIT, Dicionário-Aberto CC BY-SA, VeroBP hunspell LGPL/MPL): HIGH — all 3 license claims verified via direct fetch or cross-referenced package metadata this session
- Crossword/Connections content-sourcing conclusion ("no shortcut exists, hand-author"): MEDIUM-HIGH — a real, targeted search was made for both, both came back empty of usable datasets, and the negative claim is now cross-checked from two angles (direct PT-BR dataset search, and English-archive-as-reference-shape search) rather than a single pass
- Progression/content-pack architecture proposal: LOW/proposal-only — this remains a novel design surface for this project (as flagged in Round 1), this round adds concreteness (versioned manifest shape, connection to the real `content_items` schema) but is still a recommendation for the planner/user to confirm, not a verified best practice

**Research date:** 2026-08-28
**Valid until:** Same validity window as Round 1 — the licensing/repo findings are stable facts (re-check only if adopting these repos much later, in case licenses or repo status changed), the game-mode/architecture proposals should be confirmed at discuss-phase/planning regardless of elapsed time
