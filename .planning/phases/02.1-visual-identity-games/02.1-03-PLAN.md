---
phase: 02.1-visual-identity-games
plan: 03
type: execute
wave: 3
depends_on: ["02.1-01", "02.1-02"]
files_modified:
  - assets/progress/manifest.json
  - assets/progress/README.md
  - src/ratimos/progress_images.c
  - src/ratimos/progress_images.h
  - src/ratimos/progression.c
  - src/ratimos/progression.h
  - src/ratimos/apps/castelo_app.c
  - src/ratimos/apps/castelo_app.h
  - src/ratimos/home_screen.c
  - test/test_progression/test_progression.c
  - docs/visual-identity/README.md
autonomous: true
requirements: [PROGRESSAO-01, JOGOS-02]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Completing any game's daily mode grows a persistent visual reward: the castle-and-garden scene advances through discrete stages driven by the shared completions counter, and the advance survives closing and relaunching the simulator (PROGRESSAO-01)."
    - "Progress is shared across all five games' daily modes — the same counter advances no matter which game was completed (PROGRESSAO-01, D-05)."
    - "Each game additionally carries one exclusive decorative unlock that only appears after that specific game's daily mode has been completed at least once (PROGRESSAO-01, D-05)."
    - "Progression has a defined end state: reaching the final stage threshold shows `castelo completo! parabens` instead of an unbounded counter (D-04)."
    - "The progression manifest is versioned and additive-safe: appending a stage row raises stage_count without changing any struct layout, without touching render code, and without invalidating a save written under the previous stage_count (D-06a)."
    - "The garden and the castle are one combined scene, not two competing visuals — every stage asset depicts castle grounds with the garden growing inside them (D-03)."
    - "The home screen carries a full-width progression tile below the existing 2x2 app grid showing the current stage thumbnail plus a one-line status, tappable to open the dedicated castelo screen (UI-SPEC media / castelo-progression-display)."
    - "Status copy tracks the stage: zero completions reads `o castelo esta apenas comecando`, mid-range reads `dia X de N`, and the end state reads `castelo completo! parabens`."
    - "ratimos_progression_stage_index() returns the highest stage whose threshold is less than or equal to the completions count, so a counter between two thresholds resolves deterministically to the lower stage."
    - "A progression state whose shared_completions exceeds the final threshold still resolves to the final stage rather than reading past the stage array."
    - "Locked exclusive-unlock icons render dimmed rather than being hidden, so the player can see what is still available to earn."
    - statement: "The six stage images read as a legible, coherent progression at the 300px content width on the 320x480 panel."
      verification: backstop
  artifacts:
    - path: "src/ratimos/progression.c"
      provides: "Versioned additive-safe stage manifest, threshold-to-stage resolution, asset-id to image lookup, end-state predicate"
      contains: "ratimos_progression_manifest"
    - path: "src/ratimos/progression.h"
      provides: "ratimos_progression_stage_t, ratimos_progression_manifest_t and the resolution API"
    - path: "src/ratimos/progress_images.c"
      provides: "Generated stage and unlock artwork descriptors plus the string-id lookup"
    - path: "src/ratimos/apps/castelo_app.c"
      provides: "Dedicated castelo screen (cache-once) with stage image, day counter, and the conquistas row"
      contains: "s_castelo_screen"
    - path: "test/test_progression/test_progression.c"
      provides: "Threshold resolution, additive-safety, shared-counter and per-game-exclusive isolation tests"
  key_links:
    - from: "src/ratimos/apps/castelo_app.c"
      to: "src/storage/content_api.h"
      via: "reads the persisted counter through ratimos_storage_get_progression()"
      pattern: "ratimos_storage_get_progression"
    - from: "src/ratimos/progression.c"
      to: "src/ratimos/progress_images.h"
      via: "resolves a manifest asset_id string to a compiled image descriptor at runtime"
      pattern: "ratimos_progress_image_by_id"
    - from: "src/ratimos/home_screen.c"
      to: "src/ratimos/apps/castelo_app.h"
      via: "progression tile click callback opens the castelo screen"
      pattern: "ratimos_castelo_show"
  prohibitions:
    - "The progression system must never use loss-aversion, guilt, or shaming framing — no streak-at-risk warnings, no `voce nao jogou hoje` nags, no countdown pressure. The reward exists to delight the recipient, not to retain her."
    - "Earned progression must never regress: a missed day, a lost game, or a per-game reset must never decrement shared_completions or clear an already-earned exclusive unlock."
    - "No RatimOS visual asset — including every progression stage and unlock sprite produced here — may be copied, traced, or derived from the colombiaOS reference project; each must be project-authored with its provenance recorded."
---

<objective>
Turn the shared counter that plan 02.1-01 already persists into the visible reward PROGRESSAO-01
asks for: a garden growing inside castle grounds (D-03), advancing through a fixed number of
discrete stages to a defined end state (D-04), shared across every game's daily mode with one
exclusive decorative unlock per game (D-05), driven by a versioned, additive-safe manifest so a
future firmware update can extend the castle into a larger kingdom without invalidating progress
already earned on the device (D-06a).

Purpose: this is the phase's one genuinely new design surface, flagged by the user as needing real
research (D-06). The research produced a concrete shape; this plan implements exactly that shape and
nothing more speculative.

Output: `src/ratimos/progression.*`, generated `src/ratimos/progress_images.*`, the dedicated
`castelo` screen, the home-screen entry tile, and a Unity suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-visual-identity-games/02.1-CONTEXT.md
@.planning/phases/02.1-visual-identity-games/02.1-PATTERNS.md
@.planning/phases/02.1-visual-identity-games/02.1-UI-SPEC.md
@.planning/phases/02.1-visual-identity-games/02.1-01-SUMMARY.md
@.planning/phases/02.1-visual-identity-games/02.1-02-SUMMARY.md
@src/storage/content_api.h
@src/ratimos/home_screen.c
@src/ratimos/apps/jogos_app.c
@src/ratimos/icons.h
@src/ratimos/theme.h
@tools/convert_images.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive-safe progression manifest and stage resolution</name>

  <read_first>
    - `src/storage/content_api.h` — `ratimos_progression_state_t` and `ratimos_storage_get_progression()` as delivered by plan 02.1-01; the manifest must read this state, never redefine it
    - `.planning/phases/02.1-visual-identity-games/02.1-RESEARCH.md` — "Follow-up Research (Round 2)" section 3, which contains the exact manifest struct shape, the reason `asset_id` is a string key rather than an enum, and the six initial stage rows
    - `.planning/phases/02.1-visual-identity-games/02.1-CONTEXT.md` — D-03 (garden inside castle), D-04 (defined end state), D-05 (shared meter plus per-game exclusives), D-06a (scalable manifest, roughly 30 days, 5-6 stages)
    - `src/storage/games.c` — the compiled-const-array-plus-accessor shape a data-only module in this codebase follows
    - `test/test_storage/test_storage.c` — Unity suite structure
  </read_first>

  <files>src/ratimos/progression.c, src/ratimos/progression.h, test/test_progression/test_progression.c</files>

  <behavior>
    - `ratimos_progression_stage_index(0)` returns 0; `(5)` returns 0; `(6)` returns 1; `(29)` returns 4; `(30)` returns 5; `(9999)` returns 5 (clamped to the last populated stage, never past `stage_count`).
    - `ratimos_progression_stage_asset_id(12)` returns the third stage's asset id string.
    - `ratimos_progression_target_completions()` returns 30, the highest populated threshold.
    - `ratimos_progression_is_complete(29)` is false; `(30)` and `(31)` are true.
    - Every populated stage's `threshold` is strictly greater than its predecessor's, and every
      `asset_id` is non-empty, unique, and resolvable through the image lookup.
    - Raising `stage_count` in the test by pointing the resolver at a locally-built manifest with a
      seventh row changes only which stage a high counter resolves to — no other assertion in the
      suite changes, proving stage growth needs no render-code change.
  </behavior>

  <action>
    Implement the data-driven progression model exactly as RESEARCH round 2 specifies, so that adding
    stage seven later is a data edit rather than a code edit (D-06a).

    **`src/ratimos/progression.h` (new).** Declare
    `#define RATIMOS_PROGRESSION_MAX_STAGES 32` (generous headroom so future rows append without any
    struct or on-disk layout change), `ratimos_progression_stage_t { uint16_t threshold; char
    asset_id[32]; }`, and `ratimos_progression_manifest_t { uint16_t manifest_version; uint16_t
    stage_count; ratimos_progression_stage_t stages[RATIMOS_PROGRESSION_MAX_STAGES]; }`. Public API:
    `const ratimos_progression_manifest_t * ratimos_progression_manifest(void);`
    `size_t ratimos_progression_stage_index(uint16_t shared_completions);`
    `const char * ratimos_progression_stage_asset_id(uint16_t shared_completions);`
    `uint16_t ratimos_progression_target_completions(void);`
    `bool ratimos_progression_is_complete(uint16_t shared_completions);`
    `const char * ratimos_progression_unlock_asset_id(ratimos_game_kind_t game);`
    Add a header comment stating the extensibility contract in plain terms: `stage_count` is the
    growth point, `manifest_version` is bumped only on a breaking layout change, `asset_id` is a
    runtime-resolved string key so a stage can be added without any switch statement or enum growing,
    and a save written under a smaller `stage_count` stays valid because progress is compared against
    thresholds rather than switched on a stage number.

    **`src/ratimos/progression.c` (new).** Define the v1 manifest as a file-scope `const` with
    `manifest_version = 1`, `stage_count = 6`, and these six rows in ascending threshold order:
    threshold 0 / `castle_stage_00_terreno_vazio`; 6 / `castle_stage_01_alicerce`;
    12 / `castle_stage_02_muros_canteiro`; 18 / `castle_stage_03_torres_florindo`;
    24 / `castle_stage_04_bandeira_jardim_cheio`; 30 / `castle_stage_05_completo`. Remaining array
    slots stay zero-initialised and inert. This is D-06a's confirmed target (roughly 30 days, six
    discrete stages).

    Define the per-game exclusive unlock asset ids, one per `ratimos_game_kind_t` in enum order
    (D-05, exact mapping is Claude's discretion): `unlock_sudoku_roseira`,
    `unlock_paciencia_bandeira`, `unlock_termo_arvore`, `unlock_cruzadinha_fonte`,
    `unlock_conexo_portao`. `ratimos_progression_unlock_asset_id()` bounds-checks its argument
    against `RATIMOS_GAME_COUNT` and returns `NULL` for an out-of-range game.

    `ratimos_progression_stage_index()` walks the first `stage_count` rows and returns the index of
    the highest row whose `threshold` is less than or equal to the argument, never reading past
    `stage_count` and never past `RATIMOS_PROGRESSION_MAX_STAGES`. `ratimos_progression_is_complete()`
    is true when the argument is at least `ratimos_progression_target_completions()`.

    **`test/test_progression/test_progression.c` (new).** One test per bullet in `<behavior>`, copying
    `test/test_storage/test_storage.c`'s structure. Include a structural invariant test that walks the
    manifest asserting ascending thresholds, non-empty unique asset ids, and `stage_count` no greater
    than `RATIMOS_PROGRESSION_MAX_STAGES` — this test is what goes red if a future stage row is
    appended incorrectly.
  </action>

  <verify>
    <automated>pio test -e native_sim -f test_progression &amp;&amp; pio run -e native_sim</automated>
  </verify>

  <acceptance_criteria>
    - `pio test -e native_sim -f test_progression` exits 0 with at least 8 tests run.
    - `pio run -e native_sim` exits 0.
    - `grep -c 'RATIMOS_PROGRESSION_MAX_STAGES 32' src/ratimos/progression.h` equals 1.
    - `grep -c 'castle_stage_05_completo' src/ratimos/progression.c` is at least 1.
    - `grep -c 'stage_count = 6' src/ratimos/progression.c` equals 1.
    - `grep -cE 'switch *\(.*stage' src/ratimos/progression.c` equals 0 — stage handling is data-driven, never a per-stage branch.
    - `grep -c 'unlock_conexo_portao' src/ratimos/progression.c` is at least 1.
  </acceptance_criteria>

  <done>
    A versioned, additive-safe stage manifest resolves any completions count to exactly one stage and
    one asset id, the end state is defined at 30 completions, per-game unlock ids exist for all five
    games, and a Unity suite pins both the resolution rules and the manifest's structural invariants.
  </done>
</task>

<task type="auto">
  <name>Task 2: Garden-inside-castle stage artwork and the unlock sprites</name>

  <read_first>
    - `tools/convert_images.py` — the manifest-driven pipeline built in plan 02.1-02, including its exact CLI flags (`--manifest`, `--out-c`, `--out-h`, `--guard`) and its JSON manifest schema
    - `assets/icons/manifest.json` — the manifest format to mirror for progression artwork
    - `src/ratimos/icons.c` — the generated-output shape, including the `_by_id` lookup this task's generated file also gets
    - `src/ratimos/progression.c` — the six stage asset ids and five unlock asset ids the artwork must match one-for-one
    - `.planning/phases/02.1-visual-identity-games/02.1-UI-SPEC.md` — "Progression System UI", especially the asset-naming rule that variable names track the manifest asset_id strings one-to-one
    - `docs/visual-identity/README.md` — the provenance table this task's assets must be added to
  </read_first>

  <files>assets/progress/manifest.json, assets/progress/README.md, src/ratimos/progress_images.c, src/ratimos/progress_images.h, src/ratimos/progression.c, src/ratimos/progression.h, docs/visual-identity/README.md</files>

  <action>
    Produce the artwork D-03 asks for: one combined scene where the garden grows **inside** the castle
    grounds, not two separate visuals competing for the same screen.

    **1. Author six stage PNGs** at 288x180px (fits the 300px content width with margin, and leaves
    room on the castelo screen for the caption and the conquistas row), in the same 16-bit pixel-art
    treatment and the same restricted RatimOS palette family the icons use (`RATIMOS_COLOR_ACCENT`
    #e6010f, `RATIMOS_COLOR_PANEL` #2a123f, `RATIMOS_COLOR_PANEL_ACTIVE` #4e2277,
    `RATIMOS_COLOR_TEXT` #f5f2f8 plus a small number of garden greens), on a `#000000` background.
    The six scenes are a single continuous view of the same plot of ground, each adding to the
    previous rather than replacing it: bare walled grounds; foundation stones laid; walls up with the
    first garden bed turned; towers raised with the first blooms; flag flying over a full garden;
    complete castle with the garden at its fullest. Every stage must show both castle and garden
    elements in one frame from stage 02 onward — that is D-03's whole point.

    **2. Author five unlock sprites** at 32x32px matching the icon set's treatment, one per game, with
    filenames and ids matching `progression.c` exactly: `unlock_sudoku_roseira` (a rose bush),
    `unlock_paciencia_bandeira` (a pennant), `unlock_termo_arvore` (a small tree),
    `unlock_cruzadinha_fonte` (a fountain), `unlock_conexo_portao` (a gate).

    **3. `assets/progress/manifest.json`** with `symbol_prefix` set to `ratimos_progress` and eleven
    entries (six stages, five unlocks), each `id` matching the corresponding string in
    `progression.c` character for character. Add `assets/progress/README.md` recording that these are
    project-authored source PNGs and how to regenerate.

    **4. Generate `src/ratimos/progress_images.c` / `.h`** by running the existing
    `tools/convert_images.py` with `--guard RATIMOS_PROGRESS_IMAGES_H`. Do not modify the script — if
    it needs a change to handle these sizes, that is a bug in the script and should be fixed there,
    not worked around here. Commit source PNGs, manifest, and generated C together.

    **5. Wire the lookup into `progression.c`.** Add
    `const lv_image_dsc_t * ratimos_progress_image_by_id(const char * asset_id);` to
    `progression.h`, implemented in `progression.c` as a thin delegation to the generated
    `ratimos_progress_by_id()` so callers depend on the progression module rather than on the
    generated file directly. It returns `NULL` for `NULL` or an unmatched id, and callers must treat
    `NULL` as "render nothing" rather than dereferencing it.

    **6. Extend `docs/visual-identity/README.md`'s provenance table** with one row per new asset
    (source PNG path, generator script, project-authored, license), keeping the existing rows intact.
  </action>

  <verify>
    <automated>python3 tools/convert_images.py --manifest assets/progress/manifest.json --out-c src/ratimos/progress_images.c --out-h src/ratimos/progress_images.h --guard RATIMOS_PROGRESS_IMAGES_H &amp;&amp; pio run -e native_sim &amp;&amp; pio test -e native_sim -f test_progression</automated>
  </verify>

  <acceptance_criteria>
    - The `convert_images.py` invocation above exits 0 and is idempotent (a second run produces byte-identical output).
    - `pio run -e native_sim` exits 0 and `pio test -e native_sim -f test_progression` exits 0.
    - `ls assets/progress/*.png | wc -l` equals 11.
    - `grep -c 'extern const lv_image_dsc_t' src/ratimos/progress_images.h` equals 11.
    - Every asset id in `progression.c` resolves: for each of the six stage ids and five unlock ids,
      `grep -c "<id>" src/ratimos/progress_images.h` is at least 1.
    - `grep -c 'ratimos_progress_image_by_id' src/ratimos/progression.h` equals 1.
    - `grep -c 'nao editar a mao' src/ratimos/progress_images.c` is at least 1.
    - `git diff --stat tools/convert_images.py` is empty — the pipeline was reused unmodified.
    - `grep -ci 'unlock_sudoku_roseira' docs/visual-identity/README.md` is at least 1 (provenance extended).
  </acceptance_criteria>

  <done>
    Eleven project-authored progression assets exist as compiled LVGL images with ids that match the
    manifest one-for-one, resolvable at runtime by string key, with their provenance recorded.
  </done>
</task>

<task type="auto">
  <name>Task 3: Castelo screen and the home-screen progression tile</name>

  <read_first>
    - `src/ratimos/apps/jogos_app.c` — the cache-once screen pattern plus `ratimos_app_shell_create()` usage every new screen copies; `castelo_app.c` follows this shape, NOT `home_screen.c`'s hand-built chrome
    - `src/ratimos/home_screen.c` — the tile-construction helper, the 2x2 grid, and the ~130px of unused vertical slack below it where the new full-width tile lands; also the icon ids adopted in plan 02.1-02
    - `src/ratimos/progression.h` — stage resolution, end-state predicate, unlock ids, and the image lookup from Tasks 1 and 2
    - `src/storage/content_api.h` — `ratimos_storage_get_progression()` and `ratimos_progression_state_t`
    - `src/ratimos/theme.h` — palette tokens plus `ratimos_panel_create()` and `ratimos_badge_create()`
    - `src/ratimos/fonts/ratimos_fonts.h` — the Heading and Display pixel fonts from plan 02.1-02
    - `.planning/phases/02.1-visual-identity-games/02.1-UI-SPEC.md` — "Progression System UI" (tile size ~300x100px, dedicated screen layout, status-copy-by-stage table)
  </read_first>

  <files>src/ratimos/apps/castelo_app.c, src/ratimos/apps/castelo_app.h, src/ratimos/home_screen.c</files>

  <action>
    Make the reward visible. Until this task the counter advances invisibly; after it, a daily win
    changes something the player can see.

    **1. `src/ratimos/apps/castelo_app.c` / `.h` (new).** Expose
    `void ratimos_castelo_show(lv_event_t * e);`. Copy the cache-once static-pointer shape from
    `jogos_app.c` exactly — a `static lv_obj_t * s_castelo_screen` built once by
    `build_castelo_screen()` and reloaded with `lv_screen_load()` on every visit. Build chrome with
    `ratimos_app_shell_create("castelo", "seu jardim")`.

    Because the scene changes as the counter grows, the cached screen must be **refreshed**, not
    rebuilt: keep static pointers to the stage `lv_image`, the caption label, and the five unlock
    icons, and add a `refresh_castelo_screen()` that `ratimos_castelo_show()` calls on every visit
    after loading the screen. Rebuilding the screen per visit is the LVGL heap-exhaustion bug Phase 1
    already fixed and must not be reintroduced.

    Content inside `shell.content`, per UI-SPEC: the current stage image full width via
    `lv_image_set_src()` against `ratimos_progress_image_by_id(ratimos_progression_stage_asset_id(state.shared_completions))`,
    skipping the image entirely if the lookup returns `NULL`; below it a caption in the Heading-tier
    pixel font (`ratimos_font_title_16`) whose text follows the status-copy table — zero completions
    reads `o castelo esta apenas comecando`, any value below the final threshold reads
    `dia {shared_completions} de {ratimos_progression_target_completions()}`, and the end state reads
    `castelo completo! parabens` in the Display-tier font (`ratimos_font_title_20`); below that a row
    labelled `conquistas` holding five 32x32 images, one per game in `ratimos_game_kind_t` order,
    sourced from `ratimos_progression_unlock_asset_id()`. An unlocked entry renders at
    `LV_OPA_COVER`; a locked entry renders at roughly `LV_OPA_40` so it stays visible as something
    still to earn rather than being hidden. Never render a state where the counter would be presented
    as at risk, expiring, or lost — the copy is celebratory only.

    **2. Home-screen entry tile (`home_screen.c`).** Add one full-width tile (`RATIMOS_SCREEN_W - 20`
    wide, 100px tall) below the existing 2x2 grid, using `ratimos_panel_create()` and the same
    clickable-panel treatment as the existing tiles, laid out as a row: a 28x28 badge built with
    `ratimos_badge_create("home_castelo")` on the left, and a column on the right with the title
    `castelo` and a muted one-line status using the same copy rules as the dedicated screen. Wire its
    click callback to `ratimos_castelo_show`. Do not shrink or reflow any existing tile — the UI-SPEC
    screen-budget math shows the slack is already there. Read the progression state at build time and
    refresh the status label in `ratimos_home_screen_show()` so returning from a game shows the
    updated day count without rebuilding the home screen.

    **3. Register the boot dependency.** `castelo_app.c` reads progression state that
    `ratimos_storage_index_game_state()` prepares during the splash, which plan 02.1-01 already wired
    — no new splash step is needed. Confirm this by reading `splash.c` rather than assuming it.
  </action>

  <verify>
    <automated>pio run -e native_sim &amp;&amp; pio test -e native_sim</automated>
    <human-check>Launch `.pio/build/native_sim/program`. Confirm the home screen shows a new full-width castelo tile below the 2x2 grid with none of the existing tiles reflowed, that tapping it opens a castelo screen showing the current stage scene with a garden inside castle grounds, and that the five conquistas icons render dimmed except for conexo (already unlocked in plan 02.1-01's testing). Play and win conexo's daily puzzle, return home, and confirm the day count went up by one and the scene either stayed on the same stage or advanced.</human-check>
  </verify>

  <acceptance_criteria>
    - `pio run -e native_sim` exits 0 and `pio test -e native_sim` exits 0 across all suites.
    - `grep -c 's_castelo_screen' src/ratimos/apps/castelo_app.c` is at least 1 (cache-once pattern).
    - `grep -c 'ratimos_app_shell_create("castelo"' src/ratimos/apps/castelo_app.c` equals 1.
    - `grep -c 'ratimos_castelo_show' src/ratimos/home_screen.c` is at least 1 (tile wired).
    - `grep -c 'home_castelo' src/ratimos/home_screen.c` is at least 1.
    - `grep -c 'ratimos_progression_stage_asset_id' src/ratimos/apps/castelo_app.c` is at least 1.
    - `grep -c 'castelo completo' src/ratimos/apps/castelo_app.c` is at least 1 (end-state copy present).
    - `grep -ciE 'sequencia perdida|nao jogou|voce vai perder|expira' src/ratimos/apps/castelo_app.c src/ratimos/home_screen.c` equals 0 — no loss-aversion or shaming copy anywhere in the progression UI.
    - Manually setting `shared_completions` to 30 in `assets/save/progression.bin` (via the test helper) and relaunching shows the final stage image and the completion copy, not a `dia 30 de 30` line.
  </acceptance_criteria>

  <done>
    A daily win in any game visibly grows a garden inside castle grounds: the home screen carries a
    live progression tile, a dedicated castelo screen shows the current stage, day count, and the
    five per-game achievement icons, and the whole display is driven by the additive-safe manifest so
    future stages need only new data and new artwork.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → progression UI | `shared_completions` and the unlock array come from `assets/save/progression.bin` and are used to index the stage array and the unlock array. |
| generated image table → renderer | `asset_id` strings resolve to compiled image descriptors at runtime; an unmatched id must not produce a dereference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-02 | Tampering | `ratimos_progression_stage_index()` / unlock array indexing | high | mitigate | Task 1 walks only the first `stage_count` rows and never past `RATIMOS_PROGRESSION_MAX_STAGES`; `ratimos_progression_unlock_asset_id()` bounds-checks against `RATIMOS_GAME_COUNT`; plan 02.1-01 already clamps `shared_completions` and normalises unlock bytes on load |
| T-02.1-10 | Denial of Service | `ratimos_progress_image_by_id()` returning NULL for a stage whose artwork is missing from the build | medium | mitigate | Task 2 requires every manifest id to resolve, pinned by an acceptance grep; Task 3 requires callers to treat `NULL` as render-nothing rather than dereferencing |
| T-02.1-11 | Repudiation | future firmware update silently invalidating already-earned progress | high | mitigate | D-06a's additive-safe design: thresholds are compared, never switched on; `manifest_version` is bumped only on a layout break; Task 1's structural invariant test goes red if a future stage row breaks ascending order |
| T-02.1-08 | Spoofing | progression artwork resembling the reference project | medium | mitigate | Task 2 assets are project-authored and added to `docs/visual-identity/README.md`'s provenance table |
</threat_model>

## Artifacts this phase produces

**Files:** `src/ratimos/progression.c`, `src/ratimos/progression.h`,
`src/ratimos/progress_images.c`, `src/ratimos/progress_images.h`,
`src/ratimos/apps/castelo_app.c`, `src/ratimos/apps/castelo_app.h`,
`assets/progress/` (eleven PNGs, `manifest.json`, `README.md`),
`test/test_progression/test_progression.c`.

**Types:** `ratimos_progression_stage_t`, `ratimos_progression_manifest_t`.

**Functions:** `ratimos_progression_manifest`, `ratimos_progression_stage_index`,
`ratimos_progression_stage_asset_id`, `ratimos_progression_target_completions`,
`ratimos_progression_is_complete`, `ratimos_progression_unlock_asset_id`,
`ratimos_progress_image_by_id`, `ratimos_progress_by_id`, `ratimos_castelo_show`.

**Macros:** `RATIMOS_PROGRESSION_MAX_STAGES`.

**Asset ids:** `castle_stage_00_terreno_vazio`, `castle_stage_01_alicerce`,
`castle_stage_02_muros_canteiro`, `castle_stage_03_torres_florindo`,
`castle_stage_04_bandeira_jardim_cheio`, `castle_stage_05_completo`,
`unlock_sudoku_roseira`, `unlock_paciencia_bandeira`, `unlock_termo_arvore`,
`unlock_cruzadinha_fonte`, `unlock_conexo_portao`.

<verification>
- `pio run -e native_sim` and `pio test -e native_sim` both exit 0.
- `pio test -e native_sim -f test_progression` pins threshold resolution, end state, and the
  manifest's structural invariants.
- Simulator: home tile and castelo screen both render the current stage; winning a daily game raises
  the day count and eventually advances the stage; the advance survives a process restart.
</verification>

<success_criteria>
- PROGRESSAO-01 is satisfied: completing a daily-mode game grows a persistent visual reward that
  survives across sessions.
- The reward is one combined garden-inside-castle scene with a defined end state at 30 completions.
- Progress is shared across all games, with one exclusive unlock per game.
- Adding a seventh stage later requires only a manifest row plus artwork — no render-code change.
</success_criteria>

<output>
Create `.planning/phases/02.1-visual-identity-games/02.1-03-SUMMARY.md` when done.
</output>