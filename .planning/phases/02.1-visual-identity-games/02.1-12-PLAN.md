---
phase: 02.1-visual-identity-games
plan: 12
type: execute
wave: 2
depends_on: ["02.1-09", "02.1-10", "02.1-11"]
files_modified:
  - src/ratimos/apps/jogos/sudoku.c
  - src/ratimos/apps/jogos/paciencia.c
  - src/ratimos/apps/jogos/termo.c
  - src/ratimos/apps/jogos/cruzadinha.c
  - src/ratimos/apps/jogos/conexo.c
  - src/ratimos/apps/jogos_app.c
  - src/ratimos/apps/musica_app.c
  - src/ratimos/apps/album_app.c
  - src/ratimos/apps/cartas_app.c
  - src/ratimos/apps/config_app.c
  - src/ratimos/apps/castelo_app.c
  - src/ratimos/home_screen.c
  - docs/visual-identity/README.md
  - src/ratimos/status_bar.c
autonomous: true
gap_closure: true
requirements: [VISUAL-01, JOGOS-01, JOGOS-02, JOGOS-03, JOGOS-04, JOGOS-05, PROGRESSAO-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Toda tela do app mostra um caminho crescente real (ex: './home/jogos/conexo', './home/musica') na sectionbar, não mais um rótulo plano de uma palavra."
    - "Trocar de modo no termo (termo/dueto/quarteto) atualiza a sectionbar para o caminho completo do novo modo, sem quebrar a leitura estrutural por índice que outras partes do arquivo dependem."
    - "Os 4 gaps do UAT (G-02.1-1, G-02.1-2, G-02.1-3, G-02.1-4) estão fechados e confirmados via build+teste+UAT manual, com o registro de proveniência atualizado cobrindo todo asset novo desta rodada de gap-closure."
    - "O topbar (ratimos_topbar_create) mostra o fundo degradê ditherizado por baixo, translúcido (não mais 100% opaco) -- achado de UAT ao vivo 2026-09-11: a usuária comparou o app rodando com o sketch 004 e apontou que o header não ficou transparente como no design aprovado."
  artifacts:
    - path: "docs/visual-identity/README.md"
      provides: "Registro de proveniência/distinção atualizado cobrindo o bitmap de fundo, o logo peão-torre, e a seção de distinção revisada (o badge circular vermelho descrito ali não existe mais)"
      contains: "system_rook"
  key_links:
    - from: "src/ratimos/apps/jogos/termo.c"
      to: "src/ratimos/status_bar.h (ratimos_sectionbar_set_path)"
      via: "troca de modo chama o helper de atualização em vez de lv_label_set_text bruto, mantendo a formatação de cor do breadcrumb centralizada em status_bar.c"
      pattern: "ratimos_sectionbar_set_path"
  prohibitions:
    - "Nenhum arquivo de jogo deve ter sua lógica de motor/regras alterada nesta tarefa -- só a string de rótulo passada para ratimos_app_shell_create() e, no caso do termo, a chamada de atualização do título."
---

<objective>
Fechar o rollout final da fase de gap-closure 02.1: propagar o caminho de breadcrumb real
("./home/X" ou "./home/jogos/X") para toda tela que constrói seu chrome via
`ratimos_app_shell_create()`/`ratimos_sectionbar_create()` (construídos em plans 02.1-09/10),
corrigir a leitura estrutural por índice em `termo.c` que os dois planos anteriores
deslocaram, completar o registro de proveniência/distinção com os novos assets, e rodar a
verificação final de todos os 4 gaps do UAT como um sistema único.

Purpose: plans 02.1-09/10/11 construíram os mecanismos (ícone sem badge, painel bevel, fundo
ditherizado, breadcrumb recolorido, bug do castelo corrigido) de forma isolada e paralela;
este plano é o único lugar onde a integração completa entre eles é exercitada e onde a
usuária realmente vê o resultado combinado.

Output: toda tela do app mostrando um breadcrumb real, `termo.c` com sua leitura estrutural
corrigida, o registro de proveniência atualizado, e uma confirmação final (build + teste +
UAT manual) de que os 4 gaps do UAT estão fechados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-visual-identity-games/02.1-UAT.md
@.planning/phases/02.1-visual-identity-games/02.1-09-SUMMARY.md
@.planning/phases/02.1-visual-identity-games/02.1-10-SUMMARY.md
@.planning/phases/02.1-visual-identity-games/02.1-11-SUMMARY.md
@src/ratimos/status_bar.h
@src/ratimos/app_shell.h
@docs/visual-identity/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Breadcrumb rollout -- the 5 jogos sub-screens</name>

  <read_first>
    - `.planning/phases/02.1-visual-identity-games/02.1-10-SUMMARY.md` -- confirm
      `ratimos_sectionbar_create()`'s new path-string contract and
      `ratimos_sectionbar_set_path()`'s exact signature as actually landed (the plan defines
      it, this reads what was actually built)
    - `src/ratimos/apps/jogos/sudoku.c` line 440, `paciencia.c` line 642, `cruzadinha.c` line
      584, `conexo.c` line 470 -- each has exactly one
      `ratimos_app_shell_create("<name>", "<hint>")` call to update
    - `src/ratimos/apps/jogos/termo.c` lines 42-44 (`TERMO_MODE_LABELS`), 164-170
      (`switch_to_mode()`, the dynamic title-update call site), and 475-489 (the initial
      `ratimos_app_shell_create()` call plus the structural `lv_obj_get_child()` reads into
      the sectionbar) -- the one file in this task needing more than a one-line string swap
  </read_first>

  <files>src/ratimos/apps/jogos/sudoku.c, src/ratimos/apps/jogos/paciencia.c, src/ratimos/apps/jogos/termo.c, src/ratimos/apps/jogos/cruzadinha.c, src/ratimos/apps/jogos/conexo.c</files>

  <action>
    In `sudoku.c`, `paciencia.c`, `cruzadinha.c`, and `conexo.c`: change the single
    `ratimos_app_shell_create()` call's first argument from the bare game name to the full
    3-segment path -- `"./home/jogos/sudoku"`, `"./home/jogos/paciencia"`,
    `"./home/jogos/cruzadinha"`, `"./home/jogos/conexo"` respectively. This is the entire
    change in each of these 4 files -- do not touch anything else, especially not any game
    engine/state/solver code.

    In `termo.c`: add a small static helper (e.g. `static void format_termo_path(char * buf,
    size_t buf_size, ratimos_termo_mode_t mode)`) that writes `"./home/jogos/%s"` formatted
    with `TERMO_MODE_LABELS[mode]` into a caller-supplied buffer via `lv_snprintf`. Use it at
    the initial `ratimos_app_shell_create()` call site (replacing the bare
    `TERMO_MODE_LABELS[s_state.mode]` argument with the formatted path). In `switch_to_mode()`,
    replace the direct `lv_label_set_text(s_section_title_label, TERMO_MODE_LABELS[mode])`
    call with `ratimos_sectionbar_set_path(s_section_title_label, <formatted path>)` -- this is
    the one dynamic-title-update case in the whole app, and it must go through the shared
    helper (built in plan 02.1-10) rather than setting raw text directly, so the muted/current
    color split stays correct after a mode switch. Do NOT touch the mode-picker pill labels at
    line ~521 (`lv_label_set_text(label, TERMO_MODE_LABELS[m])`) -- those are a separate UI
    element (the tab buttons), not the sectionbar, and must keep showing the bare mode word.

    Also in `termo.c`: fix the structural `lv_obj_get_child()` index reads (currently
    `lv_obj_get_child(shell.screen, 1)` for the sectionbar row, then
    `lv_obj_get_child(sectionbar_row, 1)` for the title label). Both plan 02.1-09 (which adds
    a background image as `scr`'s new first child, shifting every top-level chrome child up by
    one) and plan 02.1-10 (which removes the sectionbar's old leading checkmark object,
    leaving the breadcrumb label as the sectionbar row's only child) change this arithmetic.
    Read the two referenced SUMMARY files above to confirm exactly how each landed, then update
    both index values to match -- update the explanatory comment above these two lines to
    describe the new structure precisely (do not leave the old "filho 0 = o check LV_SYMBOL_OK"
    wording, which will be stale).
  </action>

  <verify>
    <automated>pio run -e native_sim &amp;&amp; pio test -e native_sim -f test_jogos_termo &amp;&amp; pio test -e native_sim -f test_jogos_sudoku &amp;&amp; pio test -e native_sim -f test_jogos_paciencia &amp;&amp; pio test -e native_sim -f test_jogos_cruzadinha &amp;&amp; pio test -e native_sim -f test_jogos_conexo</automated>
  </verify>

  <acceptance_criteria>
    - `pio run -e native_sim` exits 0.
    - All five listed test suites exit 0 (game engine/state logic untouched and still green).
    - `grep -c './home/jogos/sudoku' src/ratimos/apps/jogos/sudoku.c` is at least 1.
    - `grep -c './home/jogos/paciencia' src/ratimos/apps/jogos/paciencia.c` is at least 1.
    - `grep -c './home/jogos/cruzadinha' src/ratimos/apps/jogos/cruzadinha.c` is at least 1.
    - `grep -c './home/jogos/conexo' src/ratimos/apps/jogos/conexo.c` is at least 1.
    - `grep -c './home/jogos/' src/ratimos/apps/jogos/termo.c` is at least 2 (initial build + mode-switch format sites).
    - `grep -c 'ratimos_sectionbar_set_path' src/ratimos/apps/jogos/termo.c` is at least 1.
  </acceptance_criteria>

  <done>
    All five jogos sub-screens show a real 3-segment breadcrumb, termo's mode switch updates
    it correctly through the shared helper, and termo's structural child-index reads match
    the actual chrome structure landed by the two upstream plans -- with zero changes to any
    game engine/state file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Breadcrumb rollout -- jogos launcher and the 4 top-level apps</name>

  <read_first>
    - `src/ratimos/apps/jogos_app.c` line 65 -- `ratimos_app_shell_create("jogos", ...)`
    - `src/ratimos/apps/musica_app.c` line 16, `cartas_app.c` line 22, `config_app.c` line 16
      -- each one `ratimos_app_shell_create()` call
    - `src/ratimos/apps/album_app.c` line 31 -- currently passes `"galeria"` (not `"album"`) as
      its label; preserve that existing leaf wording, do not rename it to match the internal
      app id
  </read_first>

  <files>src/ratimos/apps/jogos_app.c, src/ratimos/apps/musica_app.c, src/ratimos/apps/album_app.c, src/ratimos/apps/cartas_app.c, src/ratimos/apps/config_app.c</files>

  <action>
    Change each file's single `ratimos_app_shell_create()` call's first argument to the full
    2-segment path, preserving each screen's EXISTING leaf wording exactly (do not rename any
    of them): `jogos_app.c` -> `"./home/jogos"`, `musica_app.c` -> `"./home/musica"`,
    `album_app.c` -> `"./home/galeria"` (keep "galeria", the current word, not "album"),
    `cartas_app.c` -> `"./home/cartas"`, `config_app.c` -> `"./home/config"`. This is the
    entire change in each of these five files.
  </action>

  <verify>
    <automated>pio run -e native_sim &amp;&amp; pio test -e native_sim</automated>
  </verify>

  <acceptance_criteria>
    - `pio run -e native_sim` exits 0 and `pio test -e native_sim` exits 0 across all suites.
    - `grep -c './home/jogos"' src/ratimos/apps/jogos_app.c` is at least 1.
    - `grep -c './home/musica"' src/ratimos/apps/musica_app.c` is at least 1.
    - `grep -c './home/galeria"' src/ratimos/apps/album_app.c` is at least 1.
    - `grep -c './home/cartas"' src/ratimos/apps/cartas_app.c` is at least 1.
    - `grep -c './home/config"' src/ratimos/apps/config_app.c` is at least 1.
  </acceptance_criteria>

  <done>
    The jogos launcher and all four top-level app screens show a real 2-segment breadcrumb,
    each preserving its existing leaf wording.
  </done>
</task>

<task type="auto">
  <name>Task 3: Topbar translucency over the dithered background (live-UAT finding, 2026-09-11)</name>

  <read_first>
    - `src/ratimos/status_bar.c` -- `bar_row_create()` (the shared row helper: sets
      `bg_opa = LV_OPA_COVER` unconditionally as its default), `ratimos_topbar_create()`
      (never overrides that default, unlike `ratimos_sectionbar_create()` and
      `ratimos_bottombar_create()`, which both call `lv_obj_set_style_bg_opa(row,
      LV_OPA_TRANSP, 0)` right after `bar_row_create()`)
    - `.claude/skills/sketch-findings-ratimos/references/header-navegacao.md` -- the approved
      design: topbar has a `rgba(0,0,0,0.4)` translucent dark scrim over the gradient, not a
      solid opaque panel color
  </read_first>

  <files>src/ratimos/status_bar.c</files>

  <action>
    Live UAT finding (2026-09-11): the user compared the running native_sim build against
    sketch 004 and reported the header does not look translucent the way the approved design
    does -- it renders as a solid opaque panel, fully hiding the dithered background behind
    it. Root cause confirmed by reading the code: `ratimos_topbar_create()` calls
    `bar_row_create()` (which hardcodes `LV_OPA_COVER`) and never overrides the opacity
    afterward, unlike the sectionbar and bottombar, which both explicitly set
    `LV_OPA_TRANSP` right after creation. No prior plan in this gap-closure batch (02.1-09,
    -10, -11, or the rest of this plan's tasks) touches `status_bar.c` again after plan
    02.1-10 first wrote it, so this was never caught.

    Fix: in `ratimos_topbar_create()`, after the `bar_row_create()` call, add an explicit
    `lv_obj_set_style_bg_opa(row, LV_OPA_60, 0)` (a dark translucent scrim over the gradient,
    matching the approved `rgba(0,0,0,0.4)`-ish intent from header-navegacao.md -- LV_OPA_60
    is the closest named LVGL opacity constant; use it rather than a raw magic percentage so
    it reads consistently with the rest of the codebase's opacity calls) so text/logo/battery
    stay legible while the gradient shows through subtly, matching the sketch. Do NOT touch
    `bar_row_create()` itself (its `LV_OPA_COVER` default may still be relied on by a future
    caller) -- override locally in `ratimos_topbar_create()` only, the same pattern already
    used by the sectionbar/bottombar functions.
  </action>

  <verify>
    <automated>pio run -e native_sim &amp;&amp; pio test -e native_sim</automated>
  </verify>

  <acceptance_criteria>
    - `pio run -e native_sim` exits 0.
    - `grep -A3 'void ratimos_topbar_create' src/ratimos/status_bar.c | grep -c 'lv_obj_set_style_bg_opa'` is at least 1 (confirms an explicit override was added, not left at the `bar_row_create()` default).
    - Manual/simulator check (via the `run` skill): the topbar shows the dithered background bleeding through behind the RatimOS logo/clock/battery, not a flat solid panel color.
  </acceptance_criteria>

  <done>
    The topbar is visibly translucent over the dithered background in the running simulator,
    matching the approved sketch 004 design -- text and icons stay legible.
  </done>
</task>

<task type="auto">
  <name>Task 4: Castelo/home breadcrumb, provenance record, and final phase-gate verification</name>

  <read_first>
    - `src/ratimos/apps/castelo_app.c` line 99 -- `ratimos_app_shell_create("castelo", ...)`
    - `src/ratimos/home_screen.c` line 106 -- `ratimos_sectionbar_create(scr, "home.mem")`
      (called directly, not through `ratimos_app_shell_create()`, since home builds its own
      chrome)
    - `docs/visual-identity/README.md` -- the existing Provenance table format (one row per
      asset, per plan 02.1-08's convention) and the Distinctness section's icon-style bullet,
      which currently describes the badge as "a solid `#e6010f` red circle" -- no longer true
      after plan 02.1-09
    - `.planning/phases/02.1-visual-identity-games/02.1-UAT.md` -- gaps G-02.1-1 through
      G-02.1-4, the exact truths this task's final verification must re-confirm
  </read_first>

  <files>src/ratimos/apps/castelo_app.c, src/ratimos/home_screen.c, docs/visual-identity/README.md</files>

  <action>
    In `castelo_app.c`: change the `ratimos_app_shell_create()` call's first argument to
    `"./home/castelo"`. In `home_screen.c`: change the `ratimos_sectionbar_create(scr,
    "home.mem")` call's second argument to `"./home"` (a single-segment path renders entirely
    in the current-segment color per plan 02.1-10's contract, matching the design's own
    example).

    Update `docs/visual-identity/README.md`'s Provenance table: add one row for the dithered
    background bitmap (source `assets/backgrounds/bg_dither.png`, generator
    `tools/generate_bg_dither.py` + `tools/convert_images.py`, "Procedurally drawn (4-stop
    gradient, ordered dither, scanlines), project-owned generator script, no external
    reference, plan 02.1-09" per the existing row format) and one row for the system rook logo
    (source `assets/icons/system_rook.png`, generator the preprocessing script from plan
    02.1-10 + `tools/convert_images.py`, "Original artwork provided by the project owner
    (chroma-keyed and quantized to fit the 16-color icon palette), plan 02.1-10").

    Update the Distinctness section's icon-style row and the "How they read differently"
    paragraph, which currently describe "a solid `#e6010f` red circle behind a white/violet
    two-tone glyph" -- that badge no longer exists after plan 02.1-09; describe the actual
    current treatment (bare pixel-art icon, no badge/container) instead. Re-run the existing
    verification command from the README's own "No copied assets" section
    (`grep -ric 'colombiaos' src/ tools/ assets/`) and confirm it still returns zero matches
    after this gap-closure round's new files.

    Finally, run the full phase-gate verification for this gap-closure batch: build and test
    the whole suite, then walk the UAT's four gaps in the simulator (use the `run` skill) in
    this order and record a pass/fail plus a one-line note for each in the plan SUMMARY: (1)
    G-02.1-1/G-02.1-2 -- open jogos from home, confirm the conexo row (and at least one other)
    navigates on the first tap with no circular badge visible; (2) G-02.1-3 -- open castelo
    after winning at least one daily round, confirm the stage art renders (not blank); (3)
    G-02.1-4 -- confirm the dithered background, square-cornered translucent cards, the
    translucent topbar (Task 3 above), and the breadcrumb sectionbar (with no emoji, no
    blinking cursor) are visible and coherent across at least: home, jogos, one game screen,
    castelo, and one top-level app screen.
  </action>

  <verify>
    <automated>pio run -e native_sim &amp;&amp; pio test -e native_sim</automated>
  </verify>

  <acceptance_criteria>
    - `pio run -e native_sim` exits 0 and `pio test -e native_sim` exits 0 across every suite in the project.
    - `grep -c './home/castelo"' src/ratimos/apps/castelo_app.c` is at least 1.
    - `grep -c '"./home"' src/ratimos/home_screen.c` is at least 1.
    - `grep -c 'system_rook' docs/visual-identity/README.md` is at least 1.
    - `grep -c 'bg_dither' docs/visual-identity/README.md` is at least 1.
    - `grep -ric 'colombiaos' src/ tools/ assets/` returns 0 (excluding this doc file's own single, already-approved mention).
    - The plan SUMMARY records an explicit pass/fail note for each of G-02.1-1 through G-02.1-4.
  </acceptance_criteria>

  <done>
    Every screen's breadcrumb is complete, the provenance/distinctness record accounts for
    every asset shipped across this gap-closure batch, and all four UAT gaps have a recorded
    pass/fail from an actual simulator walkthrough.
  </done>
</task>

</tasks>

<threat_model>
**Policy:** ASVS L1, block on high -- a threat rated `high` may never be dispositioned `accept`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cross-file breadcrumb string consistency | 12 files independently supply a path string to the same shared rendering function; a typo doesn't crash anything but silently ships an inconsistent/wrong breadcrumb |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-37 | Repudiation | A mistyped or stale breadcrumb path string in any of the 12 rollout call sites | low | accept | Purely cosmetic (a wrong label reads oddly but has no functional/security impact); acceptance-criteria greps in Tasks 1-3 spot-check the exact expected string per file |
| T-02.1-38 | Tampering | `termo.c`'s structural child-index fix landing wrong (silently reading the wrong widget after two upstream layout changes) | medium | mitigate | Task 1 requires reading both upstream plans' actual SUMMARYs (not assuming the design doc's arithmetic) before editing the index, and the full `test_jogos_termo` suite plus a manual mode-switch check in the final UAT pass (Task 3) both exercise this path |
| T-02.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no new packages |
</threat_model>

<verification>
- `pio run -e native_sim` exits 0 and `pio test -e native_sim` exits 0 across every suite in the project (not just this plan's own files).
- Every one of the 12 `ratimos_app_shell_create()`/`ratimos_sectionbar_create()` call sites shows a real path string, spot-checked by grep.
- `docs/visual-identity/README.md`'s provenance table and distinctness section account for this whole gap-closure batch's new/changed assets.
- All four UAT gaps (G-02.1-1 through G-02.1-4) have a recorded pass/fail from an actual simulator walkthrough.
</verification>

<success_criteria>
- The phase 02.1 visual-identity gap-closure batch is complete: all 4 UAT gaps closed, verified as one coherent system rather than four independently-fixed pieces.
- No game engine/logic file was modified anywhere in this gap-closure batch (plans 02.1-09 through 02.1-12).
- The provenance/distinctness record is current and accurate.
</success_criteria>

<output>
Create `.planning/phases/02.1-visual-identity-games/02.1-12-SUMMARY.md` when done.
</output>
