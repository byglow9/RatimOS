---
phase: 02.1-visual-identity-games
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/icons/manifest.json
  - assets/icons/system_rook.png
  - src/ratimos/icons.c
  - src/ratimos/icons.h
  - src/ratimos/status_bar.h
  - src/ratimos/status_bar.c
autonomous: true
gap_closure: true
requirements: [VISUAL-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O topbar mostra o peão-torre vermelho (logo do sistema, fornecido pela usuária e processado via chroma-key) em vez do glifo LV_SYMBOL_HOME."
    - "A bateria no topbar é um ícone pixel-art desenhado (retângulo+terminal+preenchimento), nunca mais o glifo LV_SYMBOL_BATTERY_FULL."
    - "A sectionbar mostra um caminho crescente estilo explorador de arquivos (ex: './home/jogos/conexo'), com os segmentos-pai em cor esmaecida e o segmento atual em destaque, sem cursor piscando."
    - "Nenhum objeto novo foi inserido como filho estrutural da sectionbar -- ela continua tendo exatamente um label de título, agora reaproveitado para o breadcrumb via lv_label_set_recolor(), preservando a suposição de índice que outras telas dependem."
  artifacts:
    - path: "src/ratimos/status_bar.c"
      provides: "ratimos_topbar_create() com logo/bateria pixel-art; ratimos_sectionbar_create() renderizando um caminho recolorido; novo helper ratimos_sectionbar_set_path() para atualização dinâmica"
      contains: "ratimos_sectionbar_set_path"
    - path: "src/ratimos/icons.h"
      provides: "Décimo-segundo ícone compilado (system_rook), mesmo padrão gerado das outras 11 entradas"
      contains: "ratimos_icon_system_rook_desc"
  key_links:
    - from: "src/ratimos/status_bar.c (ratimos_topbar_create)"
      to: "src/ratimos/icons.h"
      via: "o logo do sistema no topbar é resolvido via ratimos_icon_by_id(\"system_rook\")"
      pattern: "system_rook"
    - from: "src/ratimos/status_bar.c (ratimos_sectionbar_create)"
      to: "toda tela via ratimos_app_shell_create()/ratimos_sectionbar_create()"
      via: "o argumento de rótulo agora é interpretado como um caminho completo (ex: './home/jogos/conexo'); plan 02.1-12 atualiza os chamadores para passar caminhos reais"
      pattern: "lv_label_set_recolor"
  prohibitions:
    - "Nenhum emoji (nem glifo LV_SYMBOL_HOME/LV_SYMBOL_BATTERY_FULL usado como substituto de emoji) pode permanecer no topbar ou na sectionbar."
    - "A sectionbar não pode ganhar cursor piscando (nenhuma animação lv_anim_t de opacidade/blink) -- testado e explicitamente rejeitado na sessão de sketch."
    - "O logo/peão-torre fornecido pela usuária não pode ser copiado direto para o firmware sem passar pelo pipeline real (quantização para <=16 cores + tools/convert_images.py) -- é uma imagem com ~500 cores (chroma-key com bordas anti-aliased), incompatível como está com o formato LV_COLOR_FORMAT_I4 usado por todo o resto do icon set."
---

<objective>
Fechar a parte de header/navegação de G-02.1-4 do UAT da fase 02.1: o topbar troca o glifo
🏠 (`LV_SYMBOL_HOME`) pelo peão-torre vermelho fornecido pela usuária (processado através do
pipeline real de ícones, não copiado direto do sketch) e o glifo de bateria por um ícone
pixel-art desenhado; a sectionbar troca o título plano por um caminho estilo explorador de
arquivos ("./home/jogos/conexo") com segmentos-pai esmaecidos e o atual em destaque, sem
cursor piscando.

Purpose: `status_bar.c` é o ponto de edição único que `app_shell.c` e `home_screen.c` já
delegam para todo o chrome de topbar/sectionbar -- construir a lógica de renderização aqui
primeiro deixa plan 02.1-12 (que depende deste) livre para só trocar as strings de caminho em
cada tela, sem duplicar lógica de recoloração em cada arquivo de app.

Output: `ratimos_topbar_create()`/`ratimos_sectionbar_create()` reescritos, um novo helper
`ratimos_sectionbar_set_path()` para atualização dinâmica (usado por termo.c em plan
02.1-12), e o peão-torre processado como o 12º ícone compilado do pipeline real.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-visual-identity-games/02.1-UAT.md
@.claude/skills/sketch-findings-ratimos/SKILL.md
@.claude/skills/sketch-findings-ratimos/references/header-navegacao.md
@.claude/skills/sketch-findings-ratimos/references/icones.md
@src/ratimos/status_bar.h
@src/ratimos/status_bar.c
@src/ratimos/theme.h
@src/ratimos/icons.h
@assets/icons/manifest.json
@tools/convert_images.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Process the system rook logo through the real icon asset pipeline</name>

  <read_first>
    - `.claude/skills/sketch-findings-ratimos/references/header-navegacao.md` section "Logo do sistema" -- the source asset location and its already-applied chroma-key alpha
    - `.claude/skills/sketch-findings-ratimos/sources/themes/system_rook_32.png` and `system_rook.png` -- the two available resolutions of the sketch-processed source (throwaway sketch assets per the skill's own framing -- NOT to be copied directly into firmware)
    - `tools/convert_images.py` -- `quantize_to_indexed()` raises `ValueError` above 16 distinct RGBA colors; the sketch source has ~500 distinct colors from soft chroma-key edges, so it cannot go through this script unmodified
    - `assets/icons/manifest.json` and one entry in `docs/visual-identity/README.md`'s provenance table (e.g. `home_castelo`'s row) -- the exact manifest/provenance format this new entry must follow
  </read_first>

  <files>assets/icons/manifest.json, assets/icons/system_rook.png, src/ratimos/icons.c, src/ratimos/icons.h</files>

  <action>
    Preprocess the sketch-provided rook artwork into a clean, committed source PNG compatible
    with the existing icon pipeline -- this is asset preparation (posterizing/quantizing an
    externally-supplied reference image), not new art generation, so it does not need a
    dedicated `generate_*.py` script; a short one-off Pillow script is enough (keep it, do not
    delete it, so the step is reproducible -- e.g. `tools/prepare_system_rook.py`).

    Using Pillow: load `system_rook.png` (the higher-resolution source, better quality before
    downscaling than the 32px version). Threshold the alpha channel to fully opaque or fully
    transparent (e.g. cutoff 128) to remove the soft anti-aliased chroma-key edge -- this
    matches every other icon's hard-edged flat-pixel-art style (per
    `docs/visual-identity/README.md`'s own icon-style distinctness description: "drawn from
    geometric primitives... in exactly two colors"). Quantize the remaining opaque pixels to a
    small palette (target 4-8 distinct colors, comfortably under the 16-color I4 cap -- e.g.
    `Image.quantize(colors=6)` applied only to the opaque region, then recompose with the
    binary alpha mask). Resize with `Image.NEAREST` (never bicubic/bilinear -- that
    reintroduces anti-aliased in-between colors and breaks the color budget) to a small,
    topbar-appropriate size preserving the source aspect ratio (topbar row height is 26px;
    target roughly 16-20px tall). Save the result as `assets/icons/system_rook.png`, and
    record the exact final pixel dimensions chosen (needed for the manifest entry below).

    Add one entry to `assets/icons/manifest.json`: `{ "id": "system_rook", "file":
    "system_rook.png", "w": <chosen width>, "h": <chosen height> }` -- append after the
    existing 11 entries, do not reorder them (icons.c's table order does not matter for
    correctness, but preserving it keeps the diff minimal).

    Regenerate the compiled descriptors using the EXACT command already documented in
    `docs/visual-identity/README.md`'s "Regeneration" section (`tools/convert_images.py
    --manifest assets/icons/manifest.json --out-c src/ratimos/icons.c --out-h
    src/ratimos/icons.h --guard RATIMOS_ICONS_H`) -- do not modify `convert_images.py` itself.
  </action>

  <verify>
    <automated>python3 tools/convert_images.py --manifest assets/icons/manifest.json --out-c src/ratimos/icons.c --out-h src/ratimos/icons.h --guard RATIMOS_ICONS_H &amp;&amp; pio run -e native_sim &amp;&amp; pio test -e native_sim</automated>
  </verify>

  <acceptance_criteria>
    - The conversion command exits 0 without raising the 16-color `ValueError`.
    - `pio run -e native_sim` exits 0 and `pio test -e native_sim` exits 0 across all existing suites (no regression to the other 11 icons).
    - `grep -c '"system_rook"' assets/icons/manifest.json` equals 1.
    - `grep -c 'ratimos_icon_system_rook_desc' src/ratimos/icons.h` is at least 1.
    - `python3 -c "from PIL import Image; im=Image.open('assets/icons/system_rook.png').convert('RGBA'); print(len(im.getcolors(maxcolors=100000)))"` prints a number `<= 16`.
  </acceptance_criteria>

  <done>
    The rook logo is a committed, quantized, hard-edged source PNG that compiles through the
    unmodified existing icon pipeline as the project's 12th icon, resolvable via
    `ratimos_icon_by_id("system_rook")`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sectionbar rework -- file-explorer breadcrumb, no blinking cursor</name>

  <read_first>
    - `src/ratimos/status_bar.c` lines 44-64 (`ratimos_sectionbar_create()`) -- current implementation: a fixed `LV_SYMBOL_OK` checkmark label (child 0) plus a flat title label (child 1)
    - `.claude/skills/sketch-findings-ratimos/references/header-navegacao.md` -- validated variant A (two bars, mono breadcrumb), the exact muted/current color split, and the explicit rejection of a blinking cursor
    - `src/ratimos/apps/jogos/termo.c` lines 480-489 -- the ONLY structural child-index reader of the sectionbar (`lv_obj_get_child(shell.screen, 1)` for the row, then `lv_obj_get_child(sectionbar_row, 1)` for the title label); this plan changes what lives at those indices, but the actual index-arithmetic fix in termo.c is plan 02.1-12's job (it depends on both this plan and 02.1-09's background-image insertion) -- do not edit termo.c here
    - LVGL's `lv_label_set_recolor()` (`lv_label.h`) -- enables inline `#hexcolor text#` markup within a single label, the mechanism this task uses to color the muted prefix vs. the current segment without adding a second label object
  </read_first>

  <files>src/ratimos/status_bar.h, src/ratimos/status_bar.c</files>

  <!-- planner-discipline-allow: LV_SYMBOL_OK -->

  <behavior>
    - `ratimos_sectionbar_create(parent, "./home")` renders a single line reading `./home`, entirely in `RATIMOS_COLOR_TEXT` (a path with no parent segments has nothing to mute).
    - `ratimos_sectionbar_create(parent, "./home/jogos/conexo")` renders `./home/jogos/` in `RATIMOS_COLOR_TEXT_MUTED` and `conexo` (the text after the last `/`) in `RATIMOS_COLOR_ACCENT`, as one recolored label.
    - The returned/created sectionbar row contains exactly one child object (the breadcrumb label) -- the old `LV_SYMBOL_OK` checkmark object is removed entirely, matching the validated design (no leading icon in the sectionbar).
    - `ratimos_sectionbar_set_path(title_label, "./home/jogos/dueto")` updates an existing label created by `ratimos_sectionbar_create()` to show a new path with the same muted/current split, reusing the identical formatting logic (no duplicated color-markup code between create and update).
    - No `lv_anim_t`/timer-driven opacity toggle exists anywhere in the sectionbar code (no blinking cursor).
  </behavior>

  <action>
    Add `void ratimos_sectionbar_set_path(lv_obj_t * title_label, const char * path);` to
    `status_bar.h`, documented as: rebuilds the label's recolored breadcrumb text for a new
    path, for screens whose title changes after the sectionbar is built (termo.c's three-mode
    switch is the only current use case, wired in plan 02.1-12).

    In `status_bar.c`, factor the muted-prefix/current-leaf recolor formatting into one static
    helper (e.g. `static void format_breadcrumb(lv_obj_t * label, const char * path)`) that
    both `ratimos_sectionbar_create()` and the new `ratimos_sectionbar_set_path()` call --
    there must be exactly one place that builds the `#hexcolor ...#` markup string, so the two
    entry points can never drift apart. Inside it: call `lv_label_set_recolor(label, true)`
    once; find the last `/` in `path` via `strrchr()`; format the text as
    `"#<hex of RATIMOS_COLOR_TEXT_MUTED>...#"` for everything up to and including that `/`,
    followed by `"#<hex of RATIMOS_COLOR_ACCENT>...#"` for the remainder -- LVGL's recolor
    markup takes a literal hex string per span, so build it with `lv_snprintf` reading the hex
    digits out of the existing `RATIMOS_COLOR_TEXT_MUTED`/`RATIMOS_COLOR_ACCENT` macros (do
    not hardcode a second copy of either hex value). If `path` has no `/` at all, render the
    whole string as the current-segment color only (no muted prefix). Per the sketch's own
    text-mode choice, switch the breadcrumb label's font away from the pixel Heading tier
    (D-08) to the project's existing default body-tier font (D-08's other tier -- still within
    the locked two-tier typography system, do not introduce a new font asset) -- a full
    3-segment path in the pixel Heading font risks overflowing the ~300px sectionbar width,
    which is exactly the truncation risk header-navegacao.md itself flags as unconfirmed.

    Rewrite `ratimos_sectionbar_create(lv_obj_t * parent, const char * path)`: remove the
    `LV_SYMBOL_OK` checkmark label entirely (it is not part of the validated design --
    "Sectionbar: caminho crescente", nothing else). Create exactly one label as the row's only
    child, call `format_breadcrumb()` on it, and return/store it the same way the current
    title label is (so `ratimos_sectionbar_set_path()` can be handed that same pointer later).
    Update the function's parameter name/doc comment from `label` to `path` to reflect the new
    contract, and update `status_bar.h`'s top-of-file comment accordingly.

    Add tests to a new suite `test/test_status_bar/test_status_bar.c` (same headless-LVGL
    technique as `test_theme.c` in plan 02.1-09 -- `lv_init()` + a manual display buffer, no
    SDL) covering each bullet in the Behavior list above. Use `lv_label_get_recolor()` to
    confirm recolor is enabled and `lv_obj_get_child_count()` to confirm exactly one child.
  </action>

  <verify>
    <automated>pio test -e native_sim -f test_status_bar &amp;&amp; pio run -e native_sim</automated>
  </verify>

  <acceptance_criteria>
    - `pio test -e native_sim -f test_status_bar` exits 0 with at least 5 tests run.
    - `pio run -e native_sim` exits 0.
    - `grep -c 'ratimos_sectionbar_set_path' src/ratimos/status_bar.h` equals 1.
    - `grep -c 'lv_label_set_recolor' src/ratimos/status_bar.c` is at least 1.
    - `grep -c 'LV_SYMBOL_OK' src/ratimos/status_bar.c` equals 0.
    - `grep -c 'lv_anim' src/ratimos/status_bar.c` equals 0 (no blink animation).
  </acceptance_criteria>

  <done>
    The sectionbar renders a growing, muted-then-current-colored file-explorer breadcrumb from
    a single recolored label with no blinking cursor, plus a reusable update helper for
    screens that change their title after construction.
  </done>
</task>

<task type="auto">
  <name>Task 3: Topbar rework -- system logo, pixel battery, PC-clock placeholder</name>

  <read_first>
    - `src/ratimos/status_bar.c` lines 19-42 (`ratimos_topbar_create()`) -- current implementation: `LV_SYMBOL_HOME " RatimOS"` text brand, static `"--:--"` clock, `LV_SYMBOL_BATTERY_FULL` glyph
    - `.claude/skills/sketch-findings-ratimos/references/header-navegacao.md` sections "Logo do sistema", "Bateria em pixel-art", "Relógio" -- the exact treatment and the explicit native_sim placeholder guidance (battery/clock are legitimately blocked on Phase 4 PMIC/RTC hardware)
    - `.claude/skills/sketch-findings-ratimos/references/icones.md` "Uso do mesmo tratamento no logo do sistema" -- the pixel-shadow effect (a second, dark-recolored image offset 2px/2px behind the main icon) applies here; unlike the row/tile badge in plan 02.1-09, the topbar has no structural child-index reader anywhere else in the codebase, so this is safe to add
    - `src/ratimos/icons.h` (after Task 1) -- `ratimos_icon_by_id("system_rook")`
  </read_first>

  <files>src/ratimos/status_bar.c</files>

  <!-- planner-discipline-allow: LV_SYMBOL_HOME -->
  <!-- planner-discipline-allow: LV_SYMBOL_BATTERY_FULL -->
  <!-- planner-discipline-allow: "--:--" -->

  <action>
    Replace the `LV_SYMBOL_HOME " RatimOS"` brand label with the system rook logo. Create two
    stacked `lv_image_create(row)` objects using `ratimos_icon_by_id("system_rook")`'s
    descriptor: a shadow copy positioned 2px right and 2px down from the main icon, tinted
    dark via `lv_obj_set_style_image_recolor()` + `lv_obj_set_style_image_recolor_opa()` (hard
    pixel shadow, zero blur -- do not use `lv_obj_set_style_shadow_width`, which produces a
    blurred drop-shadow LVGL-side, explicitly rejected by the sketch), created BEFORE the main
    icon so it paints underneath. Keep the "RatimOS" wordmark as a text label next to the logo
    (the sketch keeps both logo and name, not a logo-only mark).

    Replace the `LV_SYMBOL_BATTERY_FULL` glyph with a small pixel-art battery drawn from
    primitive LVGL objects per header-navegacao.md's CSS reference: an outer rectangle
    (`lv_obj_create`, border only, no fill) sized ~16x8px with a small ~2x2px "terminal" nub on
    the right edge, and an inner fill rectangle whose width represents charge percentage. Per
    the project's own documented assumption (real charge % is blocked on Phase 4's AXP2101
    PMIC/XPowersLib integration, not available on native_sim): hardcode the fill at a fixed
    mock percentage (e.g. 70%) with a code comment stating this explicitly and pointing at
    Phase 4/POWER-01 as the real-data follow-up -- do not attempt to wire a live value that
    does not exist yet.

    Replace the static `"--:--"` clock label with the actual PC wall-clock time, read via
    standard `<time.h>` (`time()` + `localtime()`, both available on `native_sim`/PC and
    trivially portable), formatted `HH:MM`. Read it once at topbar-build time (screens are
    cache-built-once per the project's established pattern, so this will not tick live across
    a long session) and add a one-line comment stating this explicitly as a known limitation:
    Phase 4's PCF85063 RTC integration (SHELL-02/POWER-02) is the real, live-updating
    replacement -- do not build a live-ticking `lv_timer_create()` across every cached screen
    in this gap-closure pass, that is materially new scope beyond the UAT gap being closed
    here.
  </action>

  <verify>
    <automated>pio run -e native_sim &amp;&amp; pio test -e native_sim</automated>
  </verify>

  <acceptance_criteria>
    - `pio run -e native_sim` exits 0 and `pio test -e native_sim` exits 0 across all existing suites.
    - `grep -c 'LV_SYMBOL_HOME' src/ratimos/status_bar.c` equals 0.
    - `grep -c 'LV_SYMBOL_BATTERY_FULL' src/ratimos/status_bar.c` equals 0.
    - `grep -c 'system_rook' src/ratimos/status_bar.c` is at least 1.
    - `grep -c '"--:--"' src/ratimos/status_bar.c` equals 0.
    - `grep -c 'localtime' src/ratimos/status_bar.c` is at least 1.
    - `grep -c 'Phase 4' src/ratimos/status_bar.c` is at least 2 (one comment for the mock battery %, one for the non-live clock).
  </acceptance_criteria>

  <done>
    The topbar shows the rook logo with a pixel shadow, a drawn pixel-art battery at a
    documented mock percentage, and a real (if not live-ticking) PC-clock time, with zero
    emoji/LVGL-symbol-font substitutes remaining and the hardware-blocked assumptions recorded
    in-line.
  </done>
</task>

</tasks>

<threat_model>
**Policy:** ASVS L1, block on high -- a threat rated `high` may never be dispositioned `accept`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied reference image -> compiled firmware | The rook logo originates as an external reference photo/image provided by the project owner, chroma-keyed and quantized before compilation -- never network-fetched, never user-uploaded at runtime |
| shared chrome primitives -> every screen | `ratimos_topbar_create()`/`ratimos_sectionbar_create()` are called by every app screen via `app_shell.c`/`home_screen.c` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-33 | Repudiation | Sectionbar breadcrumb/battery-mock/clock-placeholder silently presenting mocked data as real | low | accept | Every mocked value (battery %, non-live clock) carries an explicit in-code comment naming the real Phase 4 follow-up; this is a documented, reversible native_sim-only placeholder, not a security-relevant deception (no auth/trust decision depends on it) |
| T-02.1-34 | Tampering | A future edit reintroducing the `LV_SYMBOL_OK` checkmark object into the sectionbar, silently shifting `termo.c`'s structural child-index read | medium | mitigate | Task 2's acceptance criteria assert `LV_SYMBOL_OK` is absent and the sectionbar has exactly one child; plan 02.1-12 owns the coordinated termo.c index fix once both this plan and 02.1-09's background-image insertion have landed |
| T-02.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no new packages -- Pillow is already a project dependency; `<time.h>` is C standard library, already linked |
</threat_model>

<verification>
- `pio run -e native_sim` exits 0 and `pio test -e native_sim` exits 0 (including the new `test_status_bar` suite).
- No emoji or `LV_SYMBOL_HOME`/`LV_SYMBOL_BATTERY_FULL`/`LV_SYMBOL_OK` glyph remains in topbar/sectionbar chrome.
- The rook logo compiles through the unmodified `tools/convert_images.py` pipeline as icon #12.
- The sectionbar renders a recolored, no-cursor breadcrumb from a single label object.
</verification>

<success_criteria>
- The header/navigation portion of G-02.1-4 is closed per the validated sketch findings.
- The battery/clock hardware-blocked assumptions are explicit, reversible, and documented in-line rather than silently faked or left blocking on unavailable hardware.
- No regression to `termo.c`'s existing structural child-index read (the fix itself is deferred, correctly, to plan 02.1-12).
</success_criteria>

<output>
Create `.planning/phases/02.1-visual-identity-games/02.1-10-SUMMARY.md` when done.
</output>
