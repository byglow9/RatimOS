---
phase: 02.1-visual-identity-games
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ratimos/theme.h
  - src/ratimos/theme.c
  - src/ratimos/row_list.h
  - tools/generate_bg_dither.py
  - assets/backgrounds/manifest.json
  - assets/backgrounds/bg_dither.png
  - src/ratimos/bg_images.c
  - src/ratimos/bg_images.h
  - test/test_theme/test_theme.c
autonomous: true
gap_closure: true
requirements: [VISUAL-01, JOGOS-01, JOGOS-02, JOGOS-03, JOGOS-04, JOGOS-05]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tocar em qualquer ícone de linha/tile (jogos, home, config, etc.) navega no primeiro toque -- ratimos_badge_create() nunca mais cria um lv_obj_create() clicável por baixo do ícone (closes G-02.1-1, G-02.1-2)."
    - "Nenhum badge circular vermelho aparece atrás de nenhum ícone em nenhuma tela -- o ícone (ou o rótulo de fallback) é o único objeto visível naquele slot."
    - "Todo painel/card criado via ratimos_panel_create() tem cantos 100% retos (radius=0) e fundo levemente translúcido, nunca mais radius=6 opaco."
    - "O fundo de toda tela (splash/home/apps) mostra o bitmap gradiente ditherizado (roxo->magenta->laranja) em vez do preto sólido RATIMOS_COLOR_BG, sem regressão de heap (a suíte nativa continua passando com o novo asset carregado)."
  artifacts:
    - path: "src/ratimos/theme.c"
      provides: "ratimos_badge_create() sem wrapper clicável; ratimos_panel_create() com bevel reto/translúcido; ratimos_theme_apply_screen() desenhando o fundo ditherizado"
      contains: "RATIMOS_COLOR_BEVEL_DARK"
    - path: "src/ratimos/bg_images.h"
      provides: "Descritor LVGL compilado do bitmap de fundo ditherizado, mesmo padrão gerado de icons.h/progress_images.h"
      contains: "ratimos_bg_dither_desc"
    - path: "tools/generate_bg_dither.py"
      provides: "Gerador determinístico do PNG fonte 80x120 do gradiente ditherizado (<=16 cores), mesmo padrão de generate_icon_art.py/generate_progress_art.py"
    - path: "test/test_theme/test_theme.c"
      provides: "Primeira suíte de introspecção de widget LVGL do projeto -- prova que o badge não é clicável e que o painel tem radius=0/translúcido"
  key_links:
    - from: "src/ratimos/theme.c (ratimos_badge_create)"
      to: "src/ratimos/row_list.c / src/ratimos/home_screen.c"
      via: "toda linha/tile do app delega o ícone a ratimos_badge_create(); a correção cascata automaticamente para jogos/config/cartas/musica/album sem tocar nesses arquivos"
      pattern: "ratimos_badge_create"
    - from: "src/ratimos/theme.c (ratimos_theme_apply_screen)"
      to: "src/ratimos/bg_images.h"
      via: "toda tela (splash/home/apps) chama ratimos_theme_apply_screen(); o novo bitmap de fundo aparece em todas elas de uma vez"
      pattern: "ratimos_bg_dither_desc"
  prohibitions:
    - "ratimos_badge_create() nunca deve envolver o ícone/rótulo num lv_obj_create() extra -- isso reintroduziria exatamente o bug de hitbox que esta tarefa fecha."
    - "O fundo ditherizado é um bitmap estático pré-gerado (Pillow -> PNG -> array C via convert_images.py), nunca um gradiente computado em runtime via lv_style_set_bg_grad -- essa variante foi explicitamente rejeitada na sessão de sketch."
    - "ratimos_panel_create() não deve inserir nenhum novo lv_obj_t filho dentro do painel retornado -- vários chamadores (row_list.c, jogos_app.c) leem filhos do painel por índice posicional; um filho extra quebraria essa leitura silenciosamente."
---

<objective>
Fechar os gaps G-02.1-1, G-02.1-2 e a parte de fundo/cards de G-02.1-4 do UAT da fase 02.1: o
badge circular vermelho por trás de cada ícone é removido (era um `lv_obj_create()` clicável
por padrão, interceptando o toque destinado à linha/tile pai -- causa raiz confirmada da
usuária precisar de ~10 toques para entrar num jogo), o painel/card compartilhado ganha o
tratamento bevel reto/translúcido validado no sketch, e o fundo preto sólido vira um bitmap
gradiente ditherizado pré-gerado. O ícone pixel-art em si (D-07) não muda -- só o container
clicável por trás dele é removido.

Purpose: `ratimos_badge_create()` e `ratimos_panel_create()` em `theme.c` são os dois pontos
de edição únicos que toda a UI do app já delega a eles (row_list.c, home_screen.c, e todo
painel/pill/overlay de jogo) -- corrigir os dois aqui cascata para o app inteiro sem tocar
nenhum outro arquivo de app.

Output: `ratimos_badge_create()` sem wrapper clicável, `ratimos_panel_create()` com bevel
reto/translúcido, um novo bitmap de fundo ditherizado compilado e aplicado via
`ratimos_theme_apply_screen()`, e a primeira suíte de teste do projeto que introspecciona
widgets LVGL reais (não só lógica pura).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-visual-identity-games/02.1-UAT.md
@.claude/skills/sketch-findings-ratimos/SKILL.md
@.claude/skills/sketch-findings-ratimos/references/icones.md
@.claude/skills/sketch-findings-ratimos/references/cards-superficies.md
@.claude/skills/sketch-findings-ratimos/references/fundo-e-ambiente.md
@src/ratimos/theme.h
@src/ratimos/theme.c
@src/ratimos/icons.h
@tools/convert_images.py
@docs/visual-identity/README.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Fix the icon hitbox bug -- remove the clickable badge wrapper</name>

  <read_first>
    - `src/ratimos/theme.c` lines 27-61 (`ratimos_badge_create()`) -- the current implementation, uses `lv_obj_create()` for the circular badge container
    - `src/ratimos/theme.h` lines 46-55 -- the doc comment describing the current circle+letter contract
    - `.claude/skills/sketch-findings-ratimos/references/icones.md` -- confirmed root cause: `lv_obj_create()` defaults to `LV_OBJ_FLAG_CLICKABLE` in LVGL, intercepting the touch meant for the parent row's `click_cb`
    - `src/ratimos/row_list.c` lines 4-20 (`ratimos_row_create()`) -- badge is child 0 of `row`, `text_col` is child 1; this ordering MUST NOT change
    - `src/ratimos/apps/jogos_app.c` lines 80-90 -- reads `lv_obj_get_child(row, 1)` for the subtitle label; depends on the badge staying a SINGLE object at child 0
    - `src/ratimos/home_screen.c` lines 18-36 and 65-96 -- the two other `ratimos_badge_create()` call sites (tile_create, castelo_tile_create), neither reads children by index, safe to leave untouched
  </read_first>

  <files>src/ratimos/theme.c, src/ratimos/theme.h, src/ratimos/row_list.h, test/test_theme/test_theme.c</files>

  <!-- planner-discipline-allow: lv_obj_create -->

  <behavior>
    - `ratimos_badge_create(parent, "game_sudoku")` (a real icon id) returns an object that is NOT `LV_OBJ_FLAG_CLICKABLE`.
    - The returned object has exactly the icon image as its content -- no intermediate `lv_obj_create()` container, no circular clip, no red fill background.
    - `ratimos_badge_create(parent, "unknown_id")` (unmatched id) falls back to a plain label showing the raw id text, also NOT `LV_OBJ_FLAG_CLICKABLE`.
    - `ratimos_badge_create(parent, NULL)` never dereferences NULL and still returns a valid, non-clickable object (falls back to a "?" label per the existing contract).
    - The object returned is always exactly ONE object (verifiable via `lv_obj_get_child_count(parent)` increasing by exactly 1 per call), so `ratimos_row_create()`'s child-index contract (badge=0, text_col=1) is preserved.
  </behavior>

  <action>
    Rewrite `ratimos_badge_create()` in `theme.c` to stop wrapping the icon/letter in a
    clickable `lv_obj_create()` container. On an icon match, create the icon directly via
    `lv_image_create(parent)` and set its source -- `lv_image_create()`'s own constructor
    already clears `LV_OBJ_FLAG_CLICKABLE` (confirmed in the vendored LVGL source), so no
    extra flag manipulation is needed once the `lv_obj_create()` wrapper is gone. On no
    match (NULL or unknown id), create the fallback directly via `lv_label_create(parent)`
    (also non-clickable by construction) showing the id text, matching the existing "never
    breaks an existing caller" fallback contract. Do not clip to a circle, do not fill a
    background color, do not add a border -- the icon/label is the whole visible content now
    (per icones.md's "ícone solto sem badge").

    Do NOT add the pixel-shadow effect from icones.md to this shared badge helper: it would
    require a second image object behind the icon, which would break the single-child-slot
    contract `ratimos_row_create()`/`jogos_app.c` depend on (see `<prohibitions>`). icones.md
    itself explicitly sanctions deferring the shadow once the badge/hitbox bug is fixed --
    the topbar system logo (plan 02.1-10) gets the shadow instead, where no such structural
    coupling exists.

    Update the doc comments in `theme.h` (`ratimos_badge_create()`) and `row_list.h` to
    describe the new bare-icon-or-label contract, replacing the stale "selo redondo" (round
    badge) wording.

    Write `test/test_theme/test_theme.c` as a new Unity suite (first LVGL-widget-introspection
    test in this codebase, not just pure-logic). Initialize LVGL headlessly: call `lv_init()`,
    create a display via `lv_display_create(320, 480)` with a manually allocated `uint8_t`
    buffer and `lv_display_set_buffers(disp, buf, NULL, sizeof(buf), LV_DISPLAY_RENDER_MODE_FULL)`,
    and a flush callback that just calls `lv_display_flush_ready(disp)` -- no SDL, no window.
    Create a plain `lv_obj_t * parent = lv_obj_create(NULL);` as the badge's parent for each
    test. Add one test per bullet in the Behavior list above using `lv_obj_has_flag(obj, LV_OBJ_FLAG_CLICKABLE)`
    and `lv_obj_get_child_count()`.
  </action>

  <verify>
    <automated>pio test -e native_sim -f test_theme &amp;&amp; pio run -e native_sim</automated>
  </verify>

  <acceptance_criteria>
    - `pio test -e native_sim -f test_theme` exits 0 with at least 4 tests run.
    - `pio run -e native_sim` exits 0.
    - `grep -c 'lv_obj_create(parent)' src/ratimos/theme.c` equals 0 inside `ratimos_badge_create()`'s body (the badge container is gone) -- verify by isolating the function body: `awk '/^lv_obj_t \* ratimos_badge_create/,/^}/' src/ratimos/theme.c | grep -c 'lv_obj_create'` equals 0.
    - `grep -c 'lv_image_create(parent)' src/ratimos/theme.c` is at least 1.
    - `grep -c 'LV_RADIUS_CIRCLE' src/ratimos/theme.c` equals 0 (the circular clip is gone).
  </acceptance_criteria>

  <done>
    `ratimos_badge_create()` returns a bare, non-clickable icon or label with no wrapper
    container, closing the hitbox bug across every row/tile in the app (jogos, home, config,
    cartas, musica all delegate to this one function), pinned by a new headless-LVGL test
    suite.
  </done>
</task>

<task type="auto">
  <name>Task 2: Card bevel rework -- square corners, translucent, dark outer border</name>

  <read_first>
    - `src/ratimos/theme.c` lines 13-25 (`ratimos_panel_create()`) -- current implementation: opaque fill, radius 6, 1px accent border
    - `.claude/skills/sketch-findings-ratimos/references/cards-superficies.md` -- validated variant C (bevel), the exact CSS values, and the "What to Avoid" list
    - `src/ratimos/apps/jogos/conexo.c` lines 420-580 -- the heaviest consumer of `ratimos_panel_create()` (up to 20 panel instances per screen: pills, category bands, 16 grid tiles), useful to sanity-check the new style doesn't look wrong at small sizes
  </read_first>

  <files>src/ratimos/theme.c, src/ratimos/theme.h</files>

  <action>
    Add one new macro to `theme.h`, next to the existing 6 chrome tokens, with a comment
    explaining this is a scoped D-17 revision (not reopening the locked palette -- the 6
    existing hex values are unchanged, only HOW `ratimos_panel_create()` applies them
    changes, plus one new token the sketch introduced for the bevel's outer edge):
    `RATIMOS_COLOR_BEVEL_DARK` = `lv_color_hex(0x0d0515)`.

    Rewrite `ratimos_panel_create()` in `theme.c` per cards-superficies.md's variant C:
    `lv_obj_set_style_radius(panel, 0, 0)` (was 6 -- zero border-radius everywhere is an
    explicit, twice-repeated user requirement, never reintroduce a nonzero radius here),
    `lv_obj_set_style_bg_color(panel, RATIMOS_COLOR_PANEL, 0)` unchanged, but
    `lv_obj_set_style_bg_opa(panel, LV_OPA_70, 0)` (was `LV_OPA_COVER` -- "levemente
    transparente" per the sketch, calibrated against the new dithered background from Task 3),
    `lv_obj_set_style_border_width(panel, 2, 0)` (was 1) with
    `lv_obj_set_style_border_color(panel, RATIMOS_COLOR_BEVEL_DARK, 0)` (was
    `RATIMOS_COLOR_ACCENT` -- the accent color remains the project's interactive/CTA color
    elsewhere, unchanged; the card border is now this new dedicated dark token per the
    validated sketch screenshot, not the accent).

    Do NOT add a second child object for the inner light frisode described in
    cards-superficies.md's CSS (`box-shadow: inset ...`, approximated in LVGL by a nested
    `lv_obj`) -- `ratimos_panel_create()`'s return value is read as a structural parent by
    several callers (`row_list.c` builds `text_col` as a sibling inside the row it returns;
    `jogos_app.c` reads that sibling by positional child index). Inserting any new child here
    would shift those indices and reintroduce the exact class of hitbox bug Task 1 just
    fixed. This is the single-border fallback cards-superficies.md itself explicitly
    sanctions ("aceitar uma única borda mais grossa como fallback mais simples") --
    document this choice in a one-line comment above `ratimos_panel_create()`.

    Update the `theme.h` doc comment for `RATIMOS_COLOR_PANEL`/`RATIMOS_COLOR_PANEL_ACTIVE`
    if it references the old opaque/radius-6 look.
  </action>

  <verify>
    <automated>pio run -e native_sim &amp;&amp; pio test -e native_sim -f test_theme</automated>
  </verify>

  <acceptance_criteria>
    - `pio run -e native_sim` exits 0.
    - `grep -c 'RATIMOS_COLOR_BEVEL_DARK' src/ratimos/theme.h` equals 1 (macro defined once).
    - `grep -c 'RATIMOS_COLOR_BEVEL_DARK' src/ratimos/theme.c` is at least 1 (used in the panel border).
    - `awk '/^lv_obj_t \* ratimos_panel_create/,/^}/' src/ratimos/theme.c | grep -c 'lv_obj_set_style_radius(panel, 0'` equals 1.
    - `awk '/^lv_obj_t \* ratimos_panel_create/,/^}/' src/ratimos/theme.c | grep -c 'lv_obj_create'` equals 1 (only the panel itself, no second child object).
  </acceptance_criteria>

  <done>
    Every panel/card in the app (game boards, overlays, pills, home tiles, list rows) renders
    with square corners, a translucent fill, and the dark bevel border from the validated
    sketch, via a single shared, zero-new-object edit to `ratimos_panel_create()`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dithered gradient background bitmap</name>

  <read_first>
    - `.claude/skills/sketch-findings-ratimos/references/fundo-e-ambiente.md` -- the 4-stop gradient palette, dithering direction, and the explicit "pre-generated static bitmap, not a runtime gradient" requirement
    - `tools/convert_images.py` -- the existing manifest-driven PNG-to-`lv_image_dsc_t` converter (`LV_COLOR_FORMAT_I4`, max 16 palette colors); reuse UNCHANGED, do not modify this script
    - `assets/icons/manifest.json` and `assets/progress/manifest.json` -- the exact manifest shape this new one must follow
    - `src/ratimos/theme.c` (`ratimos_theme_apply_screen()`, after Task 1/2 edits) -- currently only sets `bg_color`/`bg_opa`/padding/border on the screen object; this task adds a background image child
    - `src/ratimos/app_shell.c` and `src/ratimos/home_screen.c` -- both call `ratimos_theme_apply_screen(scr)` as the FIRST thing when building a screen, before topbar/sectionbar/content/bottombar are added as children
  </read_first>

  <files>tools/generate_bg_dither.py, assets/backgrounds/manifest.json, assets/backgrounds/bg_dither.png, src/ratimos/bg_images.c, src/ratimos/bg_images.h, src/ratimos/theme.c, test/test_theme/test_theme.c</files>

  <action>
    Write a new generator script `tools/generate_bg_dither.py` (Pillow, mirrors
    `tools/generate_icon_art.py`'s determinism contract -- running it twice produces
    byte-identical output) that produces an 80x120 source PNG at
    `assets/backgrounds/bg_dither.png`:

    - 4-stop vertical gradient using fundo-e-ambiente.md's exact hex stops (`#1c0a3d` at 0%,
      `#7a1560` at ~42%, `#c81f4c` at ~68%, `#e8630f` at 100%), interpolated per-row.
    - Ordered (Bayer 4x4) dithering choosing between the two nearest stop colors per pixel
      rather than blending to an intermediate RGB value -- this keeps the total distinct-color
      count small enough to fit `convert_images.py`'s 16-color `LV_COLOR_FORMAT_I4` budget
      (the 4 stop colors alone are well under 16; do not introduce continuous per-pixel
      interpolation, which would blow well past 16 distinct RGBA values and make
      `quantize_to_indexed()` raise).
    - Horizontal scanlines: darken every 3rd row by ~12% opacity-equivalent (a darkened
      variant of whichever stop color is present at that row) -- per fundo-e-ambiente.md's
      validated variant C. Keep this subtle; do not raise it past ~12%, which risks
      "sujar" small text per the skill's own caveat.
    - Generate at 80x120 (not 320x480) intentionally -- this is the load-bearing decision for
      RAM safety, not a shortcut: LVGL decodes an indexed image at its SOURCE resolution
      regardless of the on-screen target size, so an 80x120 source keeps the runtime decode
      buffer around ~19KB, while a naive 320x480 source would need a ~300KB decode buffer --
      more than half of this project's 512KB `LV_MEM_SIZE` (raised once already in Phase 1
      specifically because of an LVGL heap-exhaustion bug class -- do not reopen that risk).
      The final on-screen 320x480 size is reached via LVGL's runtime image scale/stretch
      (below), not by baking a larger bitmap.

    Create `assets/backgrounds/manifest.json` (`symbol_prefix: "ratimos_bg"`, one entry
    `{"id": "dither", "file": "bg_dither.png", "w": 80, "h": 120}`), then run the EXISTING
    `tools/convert_images.py --manifest assets/backgrounds/manifest.json --out-c
    src/ratimos/bg_images.c --out-h src/ratimos/bg_images.h --guard RATIMOS_BG_IMAGES_H` to
    produce the compiled descriptor `ratimos_bg_dither_desc` -- do not write a new converter,
    this manifest-driven script already does exactly this job for the icon and progression
    art.

    In `ratimos_theme_apply_screen(lv_obj_t * scr)`, after the existing bg_color/pad/border
    styling, add the background as the screen's first child:
    `lv_obj_t * bg = lv_image_create(scr); lv_image_set_src(bg, &ratimos_bg_dither_desc);
    lv_obj_set_size(bg, RATIMOS_SCREEN_W, RATIMOS_SCREEN_H); lv_obj_set_pos(bg, 0, 0);
    lv_obj_add_flag(bg, LV_OBJ_FLAG_FLOATING); lv_image_set_inner_align(bg,
    LV_IMAGE_ALIGN_STRETCH); lv_image_set_antialias(bg, false);` -- `LV_OBJ_FLAG_FLOATING`
    keeps it out of the flex-column layout every screen uses for topbar/sectionbar/content/
    bottombar (it must not consume a flex slot), and `lv_image_set_antialias(bg, false)`
    keeps the scale-up crisp/pixelated rather than LVGL smoothing it into a blur, which would
    defeat the retro-pixel look. Keep the existing `RATIMOS_COLOR_BG` solid fill on `scr` as
    the fallback drawn beneath this image (resilience if the image source is ever NULL/
    unavailable, never remove that line).

    IMPORTANT structural note for downstream plans: this makes the background image `scr`'s
    child 0, shifting topbar/sectionbar/content/bottombar each up by one index. This plan
    does not need to touch any call site for that shift (no file in this plan's scope reads
    screen children by position), but `src/ratimos/apps/jogos/termo.c` does
    (`lv_obj_get_child(shell.screen, 1)` for the sectionbar row) -- plan 02.1-12 (which
    depends on this plan) is responsible for updating that index; do not attempt it here.

    Verify RAM safety empirically, not just by the ~19KB estimate above: add
    `lv_mem_monitor_t mon; lv_mem_monitor(&mon);` calls bracketing a build-and-load of at
    least 2 different cached screens (e.g. home + one app screen) in a throwaway debug print
    inside the test suite from Task 1 (`test/test_theme/test_theme.c`) or a small addition to
    it, asserting `mon.free_size` stays comfortably positive (e.g. > 100000 bytes) after both
    screens are built and the background has been decoded/cached. Remove any throwaway
    `printf` before finishing; keep the assertion.
  </action>

  <verify>
    <automated>python3 tools/generate_bg_dither.py &amp;&amp; python3 tools/convert_images.py --manifest assets/backgrounds/manifest.json --out-c src/ratimos/bg_images.c --out-h src/ratimos/bg_images.h --guard RATIMOS_BG_IMAGES_H &amp;&amp; pio run -e native_sim &amp;&amp; pio test -e native_sim -f test_theme</automated>
  </verify>

  <acceptance_criteria>
    - `python3 tools/generate_bg_dither.py` run twice produces byte-identical `assets/backgrounds/bg_dither.png` (`md5sum` unchanged across two runs).
    - `python3 tools/convert_images.py --manifest assets/backgrounds/manifest.json --out-c src/ratimos/bg_images.c --out-h src/ratimos/bg_images.h --guard RATIMOS_BG_IMAGES_H` exits 0 without raising the 16-color `ValueError`.
    - `pio run -e native_sim` exits 0.
    - `pio test -e native_sim -f test_theme` exits 0, including the new heap-safety assertion.
    - `grep -c 'ratimos_bg_dither_desc' src/ratimos/theme.c` is at least 1.
    - `grep -c 'LV_IMAGE_ALIGN_STRETCH' src/ratimos/theme.c` equals 1.
    - `grep -c 'lv_style_set_bg_grad' src/ratimos/theme.c` equals 0 -- confirms the rejected runtime-gradient variant was never used.
  </acceptance_criteria>

  <done>
    Every screen shows the validated dithered gradient background instead of solid black,
    generated from a deterministic Pillow script through the existing icon/progression asset
    pipeline, decoded from a RAM-cheap 80x120 source and scaled to fill the 320x480 screen at
    draw time, with an automated heap-safety check.
  </done>
</task>

</tasks>

<threat_model>
**Policy:** ASVS L1, block on high -- a threat rated `high` may never be dispositioned `accept`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| asset pipeline -> compiled firmware | `tools/generate_bg_dither.py` and `tools/convert_images.py` run at build/dev time, never on-device; their output becomes static `const` C arrays baked into the firmware image |
| shared theme primitives -> every screen | `ratimos_badge_create()`/`ratimos_panel_create()`/`ratimos_theme_apply_screen()` are called by every app screen; a bug here is not isolated to one screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-30 | Denial of Service | `ratimos_theme_apply_screen()` decoding the background image on every screen build | high | mitigate | Task 3's 80x120 source (not 320x480) keeps the decode buffer around ~19KB instead of ~300KB against the project's 512KB `LV_MEM_SIZE`; pinned by an automated heap-safety assertion in `test_theme` |
| T-02.1-31 | Tampering | Icon/badge touch-target hijacking (the actual UAT-reported bug: a clickable overlay object silently swallowing taps) | high | mitigate | Task 1 removes the `lv_obj_create()` wrapper entirely; pinned by a headless test asserting `LV_OBJ_FLAG_CLICKABLE` is absent on the returned object |
| T-02.1-32 | Repudiation | A future edit reintroducing a child object inside `ratimos_panel_create()`, silently shifting structural child-index reads in `row_list.c`/`jogos_app.c` | medium | mitigate | Task 2's acceptance criteria greps confirm exactly one `lv_obj_create()` call inside the function body; the one-line comment documents why a second object must never be added here |
| T-02.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no new packages -- Pillow is already a project dependency (used by `tools/generate_icon_art.py`/`tools/generate_progress_art.py` since plan 02.1-02/02.1-03); no new Package Legitimacy Audit entry required |
</threat_model>

<verification>
- `pio run -e native_sim` exits 0 and `pio test -e native_sim -f test_theme` exits 0.
- `ratimos_badge_create()` returns a non-clickable single object for every call shape (icon match, fallback, NULL).
- `ratimos_panel_create()` renders square-cornered, translucent, dark-bordered panels with no new child object.
- The dithered background bitmap renders on every screen with a bounded, automatically-verified heap cost.
</verification>

<success_criteria>
- G-02.1-1 and G-02.1-2 are closed: tapping any icon row/tile navigates on the first tap, and no circular red badge remains anywhere in the UI.
- The background and card portions of G-02.1-4 are closed per the validated sketch findings.
- No regression to `row_list.c`/`jogos_app.c`/`home_screen.c`'s existing structural child-index reads.
</success_criteria>

<output>
Create `.planning/phases/02.1-visual-identity-games/02.1-09-SUMMARY.md` when done.
</output>
    <automated>python3 tools/generate_bg_dither.py &amp;&amp; python3 tools/convert_images.py --manifest assets/backgrounds/manifest.json --out-c src/ratimos/bg_images.c --out-h src/ratimos/bg_images.h --guard RATIMOS_BG_IMAGES_H &amp;&amp; pio run -e native_sim &amp;&amp; pio test -e native_sim -f test_theme