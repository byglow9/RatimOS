---
phase: 02-sync-protocol-security-model-cloud-backend
plan: 4
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - test/test_sync_integration/test_sync_integration.c
autonomous: true
requirements: [SEC-01, SEC-02]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

# assumption_delta: gsd_run assumption-delta scan flagged the pluralization
# signal "another" in ROADMAP's Phase 2 success criterion #2 ("...gets rejected
# (401) with no token or another device's token"). Resolved via add-alongside,
# not promote: this single-device project (D-12) is NOT generalized into a
# multi-device schema/architecture. Task 1 below tests "another device's
# token" with a syntactically valid but never-registered token literal
# (RESEARCH.md Pitfall 3, option b) rather than provisioning a second real
# device — the code path exercised (token_hash not found -> 401) is identical
# either way, and the single-device architecture from D-12 is fully preserved.

must_haves:
  truths:
    - "whats-new returns 200 + the 3 seeded pending items for the real registered TEST_DEVICE_TOKEN, verified from a PC-native build against the live Supabase project (ROADMAP Phase 2 success criterion #4)."
    - "whats-new returns 401 for both a missing token and a syntactically valid but never-registered token (ROADMAP Phase 2 success criterion #2 — resolved via add-alongside per the assumption_delta note above)."
    - "No source file under src/sync/ contains a plaintext-scheme URL literal (ROADMAP Phase 2 success criterion #3 / SEC-02)."
    - "An anonymous client holding only the publishable key cannot read any row from devices, content_items, or ota_releases (ROADMAP Phase 2 success criterion #1, T-2-06, verified live)."
  artifacts:
    - test/test_sync_integration/test_sync_integration.c
  key_links:
    - "test_sync_integration.c's full test list -> the register-device + whats-new stack built across Plans 1-3 -> the real hosted Supabase project"
  prohibitions:
    - statement: "MUST NOT treat a passing `pio test` run as sufficient proof of RLS without also running the direct-publishable-key curl check in Task 3 — a green test suite alone only proves the primary Edge-Function path works (D-09), not that the RLS default-deny backstop actually blocks a client that bypasses the Edge Functions entirely."
      verification: flagged-unverified
  assumptions:
    - "RESEARCH.md's VALIDATION.md draft marked the direct-PostgREST RLS check and the register-device admin-secret-rejection check as 'manual-only, best confirmed during phase-gate UAT.' This plan overrides that to fully automated (curl run by the executor), per the automation-first rule — a human should not be asked to run a check Claude can run itself via CLI."
---

<objective>
Close out the phase's 4 ROADMAP success criteria with real, automated, live-network verification: the full auth-reject matrix for whats-new, the HTTPS-only guarantee, and the RLS default-deny backstop — all run against the actual deployed Supabase project, not mocked.

Purpose: Every prior plan in this phase built one proven piece; this plan is where those pieces are proven to compose correctly, from the outside, exactly the way the ROADMAP's success criteria are worded.
Output: A complete, green `test_sync_integration/` suite plus one live curl-based RLS proof, satisfying all 4 of Phase 2's ROADMAP success criteria.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-sync-protocol-security-model-cloud-backend/02-CONTEXT.md
@.planning/phases/02-sync-protocol-security-model-cloud-backend/02-RESEARCH.md
@src/sync/sync_client.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: whats-new auth-reject matrix (SEC-01, ROADMAP success criterion #2 &amp; #4)</name>
  <files>test/test_sync_integration/test_sync_integration.c</files>
  <read_first>
    test/test_sync_integration/test_sync_integration.c (from Plan 1 — extend, do not replace, its existing register-device tests), src/sync/sync_client.h (from Plan 3), .planning/phases/02-sync-protocol-security-model-cloud-backend/02-RESEARCH.md "Common Pitfalls" Pitfall 3 (the "another device's token" testing strategy) and Pitfall 5 (unit/integration split, manual bootstrap precondition)
  </read_first>
  <action>
    Extend `test/test_sync_integration/test_sync_integration.c` (created in Plan 1) with 3 new tests calling `ratimos_sync_whats_new` against base_url `https://bhqscupdrgfuwitbtlui.supabase.co`.

    `test_whats_new_valid_token_returns_pending_items`: reads `TEST_DEVICE_TOKEN` via `getenv()` (bootstrapped in Plan 2, Task 3), calls `ratimos_sync_whats_new`, asserts `*out_status == 200` and the returned count is 3, with at least one returned `title` matching one of the 3 seeded titles from Plan 2's seed script.

    `test_whats_new_no_token_rejected`: calls `ratimos_sync_whats_new` with `device_token` as `NULL`, asserts `*out_status == 401` and the returned count is 0.

    `test_whats_new_wrong_token_rejected`: calls `ratimos_sync_whats_new` with a hardcoded, syntactically-plausible token literal that was never returned by register-device (e.g. a fixed random UUID-shaped string authored fresh for this test), asserts `*out_status == 401`. Per the assumption_delta note in this plan's frontmatter, this single-device project tests the "another device's token" success criterion via a never-registered literal rather than provisioning a second real device — the code path exercised (hash lookup finds no matching row) is identical either way.
  </action>
  <verify>
    <automated>pio test -e native_sim -f test_sync_integration</automated>
  </verify>
  <done>All 5 tests in test_sync_integration/ pass against the real deployed project: 2 register-device tests from Plan 1, 3 whats-new tests from this task.</done>
  <acceptance_criteria>
    - `pio test -e native_sim -f test_sync_integration` exits 0 with all 5 tests reported PASS (network required).
    - `grep -c "TEST_DEVICE_TOKEN" test/test_sync_integration/test_sync_integration.c` is greater than 0.
  </acceptance_criteria>
  <precondition>.env contains a non-empty TEST_DEVICE_TOKEN entry, written during Plan 2 Task 3's one-time device-registration bootstrap.</precondition>
</task>

<task type="auto">
  <name>Task 2: HTTPS-only guarantee — live transport check &amp; static scheme audit (SEC-02, ROADMAP success criterion #3)</name>
  <files>test/test_sync_integration/test_sync_integration.c</files>
  <read_first>
    src/sync/http/native_curl/http_client.c (from Plan 1 — the CURLOPT_SSL_VERIFYPEER/VERIFYHOST calls to confirm are present), test/test_sync_integration/test_sync_integration.c (extend further)
  </read_first>
  <action>
    Add `test_https_transport_succeeds` to `test/test_sync_integration/test_sync_integration.c`: call `ratimos_sync_http_get` against the real whats-new URL with `TEST_DEVICE_TOKEN`, assert the function's own return value is 0 (transport succeeded) and `out.status_code != 0` — this proves libcurl's default certificate verification against Supabase's real TLS certificate actually works end-to-end at run time, not merely that the right options are set in source.

    Separately, run a static audit across every file in `src/sync/`: no file may construct a request URL using the plaintext, unencrypted scheme — every URL literal or format string reaching `ratimos_sync_http_get`/`ratimos_sync_http_post_json` must use the encrypted scheme exclusively.
  </action>
  <verify>
    <automated>pio test -e native_sim -f test_sync_integration &amp;&amp; grep -rn '"http://' src/sync/ | wc -l</automated>
  </verify>
  <done>The live HTTPS transport test passes, and zero plaintext-scheme URL literals exist anywhere under src/sync/.</done>
  <acceptance_criteria>
    - `pio test -e native_sim -f test_sync_integration` exits 0 including the new test.
    - `grep -rn '"http://' src/sync/ | wc -l` is 0.
    - `grep -c "CURLOPT_SSL_VERIFYPEER" src/sync/http/native_curl/http_client.c` is greater than 0 and `grep -c "CURLOPT_SSL_VERIFYPEER, 0" src/sync/http/native_curl/http_client.c` is 0 (verification is never disabled).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: RLS default-deny live proof &amp; final rebuild (ROADMAP success criterion #1, T-2-06)</name>
  <files>(none — verification-only task; no source files are created or modified)</files>
  <read_first>
    .planning/phases/02-sync-protocol-security-model-cloud-backend/02-RESEARCH.md "Architecture Patterns" Pattern 3 (RLS locked-but-empty) and "Common Pitfalls" Pitfall 4 (pio test/pio run shared build output path)
  </read_first>
  <action>
    Run a live curl check against the Supabase REST endpoint for both `content_items` and `devices` using ONLY the publishable key (`apikey: sb_publishable_CLW_e0HlOSMlKfiBp6VdKQ__AZw07n1` header, no service-role key, no Authorization bearer token) at `https://bhqscupdrgfuwitbtlui.supabase.co/rest/v1/content_items` and `.../rest/v1/devices`. Confirm the response is EITHER an empty JSON array (RLS default-deny returns zero visible rows to this role) OR a schema-not-found style error (the table was never auto-exposed, per D-02's "Automatically expose new tables" being disabled) — either outcome proves no anonymous publishable-key client can read another device's rows; a response containing real rows from either table is the only failing outcome. Run the same check against `ota_releases` and additionally confirm it returns zero rows regardless of exposure (D-13).

    Finally, since `pio test` and `pio run` share the same native_sim build output path (RESEARCH.md Pitfall 4, a recurring issue from Phase 1), run `pio run -e native_sim` one more time after all test commands in this phase have finished, so the artifact left behind at `.pio/build/native_sim/program` is the real application, not the last test binary.
  </action>
  <verify>
    <automated>curl -s -H "apikey: sb_publishable_CLW_e0HlOSMlKfiBp6VdKQ__AZw07n1" https://bhqscupdrgfuwitbtlui.supabase.co/rest/v1/content_items &amp;&amp; pio run -e native_sim</automated>
  </verify>
  <done>The live curl checks confirm no anonymous client can read devices/content_items/ota_releases, and native_sim's build output is the real app, not a leftover test binary.</done>
  <acceptance_criteria>
    - The curl response body for content_items and devices is either `[]` or contains an error indicating the relation/table is not exposed to this role — never an array of real rows.
    - The curl response for ota_releases returns zero rows.
    - `pio run -e native_sim` exits 0 after this task, and manually running `.pio/build/native_sim/program` launches the RatimOS app (not Unity test output).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces

**Modified files:**
- `test/test_sync_integration/test_sync_integration.c` — extended from 2 tests (Plan 1) to 6 tests total (5 automated Unity tests + the live curl checks documented and run by Task 3)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Full device (PC-native client) → Supabase Edge Functions round trip | The complete untrusted-network path this whole phase secures, now exercised end-to-end by automated tests |
| Anonymous client (publishable key only) → Postgres via PostgREST | The defense-in-depth boundary D-09 describes; proven live in Task 3, not just asserted in migration SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-02 | Information Disclosure | whats-new bearer-token handling, end-to-end | high | mitigate (verified) | Task 1's 3 tests confirm the full auth-reject matrix (no token, wrong token, valid token) live against the real project. |
| T-2-03 | Tampering / Information Disclosure | HTTPS/TLS transport, end-to-end | high | mitigate (verified) | Task 2's live transport test confirms certificate verification actually succeeds against Supabase's real certificate, not just that the right curl options are set in source. |
| T-2-06 | Elevation of Privilege / Information Disclosure | RLS default-deny on all 3 tables | high | mitigate (verified) | Task 3's live curl check confirms an anonymous publishable-key client cannot read devices, content_items, or ota_releases — the actual defense-in-depth proof, not just the migration SQL's presence. |

</threat_model>

<verification>
- `pio test -e native_sim -f test_sync_integration` passes all 5 Unity tests against the live project.
- The static scheme audit (`grep -rn '"http://' src/sync/`) returns zero matches.
- The live RLS curl checks confirm no anonymous read access to any of the 3 tables.
- `pio run -e native_sim` leaves the real app binary as the last build artifact.
</verification>

<success_criteria>
All 4 of Phase 2's ROADMAP success criteria are proven true from a PC-native build against the real, live Supabase project: schema+RLS exists, the bearer-token auth-reject matrix works, every request is HTTPS-only, and whats-new returns real pending content for a real device token.
</success_criteria>

<output>
Create `.planning/phases/02-sync-protocol-security-model-cloud-backend/02-04-SUMMARY.md` when done
</output>
